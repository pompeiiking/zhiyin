/**
 * OpenAPI 生成物占位。
 *
 * 本文件由后端 OpenAPI 自动生成，禁止手写：
 *   npm run gen:api
 *
 * 后端一旦产出 /openapi.json，本文件会被覆盖为全部 DTO 的 TypeScript 类型，
 * 前端与后端之间的字段对齐从此由代码保证，不再靠人沟通。
 */

export type ApiTypes = Record<string, never>
