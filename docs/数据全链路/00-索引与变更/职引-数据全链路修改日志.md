# 职引 · 数据全链路修改日志

| 项目 | 内容 |
| --- | --- |
| 维护范围 | `docs/数据全链路/` 文档，以及与其直接对应的数据工程重要变更 |
| 负责人 | 袁明阳（数据能力全链路负责人） |
| 建立日期 | 2026-09-19 |
| 最近更新 | 2026-09-19 |
| 记录原则 | 已提交、当前工作区完成、待实施三种状态严格区分；密钥不入文档 |

## 一、状态说明

| 状态 | 含义 |
| --- | --- |
| 已提交 | 已进入 Git 正式提交，可用提交号复核 |
| 工作区已完成 | 当前工作区已有实现或文档，但尚未提交 |
| 已决待实施 | 方案已确定，但代码、配置或真实联调尚未完成 |

## 二、历史修改记录

| 日期 | 状态 | 提交/依据 | 修改内容 | 影响范围 |
| --- | --- | --- | --- | --- |
| 2026-09-15 | 已提交 | `31f9376` | 建立数据能力全链路工作清单、开发须知、完成情况记录和第一期 Redis 分库策略；完成第一期数据链路开发记录 | 第一期计划、规范、Redis、Memory、Asset、采集与验收 |
| 2026-09-15 | 已提交 | `93f85f3` | 修复第一期数据链路并补充数据库冻结规范，更新工作清单、开发须知、完成记录和 Redis 策略 | 第一期数据一致性、数据库边界和验收口径 |
| 2026-09-15 | 已提交 | `a816444` | 新增第一期未闭合项与 Mock 标注清单，明确完成数字与业务闭环范围的差异 | OPEN/MOCK 缺口管理 |
| 2026-09-15 | 已提交 | `ab6b8b7` | 随行为日志同刻排序修复更新未闭合项证据 | Behavior 数据顺序与第一期核验 |
| 2026-09-16 | 已提交 | `6187923` | 编排链路接通后补充资产、知识库回归结果并更新未闭合项状态 | Asset、Knowledge 与编排链路 |
| 2026-09-17 | 已提交 | `e630411` | 新增 PAMI 内置接入说明、招聘与公司评价采集设计；更新工作清单、开发须知、完成记录和第一期缺口状态 | 第二期数据基础设施、PAMI、采集与端到端测试 |
| 2026-09-17 | 已提交 | `e13d8b5` | 修复 CI 缺少 M3 测试依赖并记录二期门禁结果 | CI、M3 数据基础设施测试 |
| 2026-09-18 | 工作区已完成 | 当前工作区 | 新增数据能力重叠与架构分层审计；收敛重复检索契约，六域检索共享形状落入 Kernel | Kernel、Data SDK、Infrastructure、检索契约治理 |
| 2026-09-18 | 工作区已完成 | 当前工作区 | 新增第三期 RAG 检索内容与检索流程设计；完成稳定切片、RRF、权限/时效过滤、MySQL 回源、证据包、评测集与 Phase 3 门禁代码闭环 | 第三期 RAG 数据工程 |
| 2026-09-18 | 工作区已完成 | 当前工作区 | 将有效部署资产迁入 `zhiyin-src/template/deploy/`，同步修正文档路径；清理无效运行产物 | 部署资产和文档引用 |

## 三、2026-09-19 本次修改

| 状态 | 修改内容 | 结果/边界 |
| --- | --- | --- |
| 工作区已完成 | 将原来平铺的 9 份数据全链路文档整理为 `00` 至 `05` 六个分类目录 | 仅改变文档落位，不改变业务、架构和完成状态 |
| 工作区已完成 | 新增本目录总索引和修改日志 | 提供阅读顺序、分类入口、维护规则和历史追踪 |
| 工作区已完成 | 修复移动后相对链接，并更新 `docs/README.md`、`docs/AI阅读索引.md` 和 M3 决策登记表中的旧路径 | 避免目录整理造成文档断链 |
| 历史方案（已替换） | 曾计划采用火山方舟 `/api/v3/embeddings` 与 `doubao-embedding-text-240715` | 真实联调确认该模型已进入退役状态，保留记录仅用于追溯，不再作为当前运行方案 |
| 工作区已完成 | PAMI 在按模型记录查出配置后，以 `modelInfo.Model` 覆盖厂商请求中的 `model` 字段，并新增 Go 回归测试 | PAMI 路由继续使用内部 model ID，厂商请求使用真实模型名；Go `model-provider`/BFF 测试通过 |
| 工作区已完成 | 部署模板新增 `ZHIYIN_PAMI_BASE_URL=http://nginx:8081`，并把旧 `ZHIYIN_USE_PAMI_KNOWLEDGE` 更正为 `ZHIYIN_USE_PAMI_SEARCH` | Python 部署/PAMI/RAG 定向测试 22 passed，Phase 3 配置装配 `unmet=[]` |
| 工作区已完成 | 当前 Embedding 改为 PAMI 内的 OpenAI-compatible 通义文本向量模型，职引仅配置 PAMI 内部模型记录 ID | 厂商 Key 只保存在 PAMI 密钥配置；模型返回 1024 维，未修改 pgvector 结构 |
| 工作区已完成 | Docker Desktop 失效挂载点由用户处理并完成重启 | Docker Engine 恢复；统一 Compose 基线验证通过，带健康探针的常驻服务均为 `healthy` |
| 工作区已完成 | 新增仅构建 PAMI BFF 的 `Dockerfile.bff`，统一 Compose 将 BFF 替换为包含模型名转发修复的 `zhiyin/pami-bff:dev` | 镜像构建、容器重建和健康检查通过；其余 PAMI 服务仍使用冻结镜像 |
| 工作区已完成 | PAMI 请求日志增加递归敏感字段脱敏并补充 Go 回归测试 | API Key、密码、Token、Secret 等不再写入 BFF 请求日志；重建后容器日志扫描为 0 命中 |
| 历史联调失败 | 已在 PAMI 创建旧 Ark Embedding 模型记录并发起真实请求 | Ark 返回 `InvalidEndpointOrModel.NotFound`；随后改用当前可用模型，旧失败不再阻塞数据链路 |
| 工作区已完成 | PAMI → 通义 Embedding → MySQL `embed_task` → VectorSyncWorker → pgvector 全链路真实验收 | 单条及六知识域均完成真实写入、同步、检索、模型隔离和删除；向量维度 1024，探针数据清理后 MySQL/pgvector 残留均为 0 |
| 工作区已完成 | 重建职引 API 镜像并补齐 M3 运行依赖 | `cryptography` 等运行依赖进入镜像；API、pgvector 与 PAMI BFF 健康，Phase 3 门禁通过 |

## 四、2026-09-19 前端字段真实性复核（本轮追加）

| 状态 | 修改内容 | 结果/边界 |
| --- | --- | --- |
| 工作区已完成 | 新增[前端字段真实性清单](../04-实施与验收/职引-前端字段真实性清单-v1.0.md)，逐字段标注 真实 / 接口真实·内容 Mock / 前端演示 / 前端兜底 | 覆盖全部页面、store 与公共组件；落在 `04-实施与验收`；只读复核，未改任何业务代码 |
| 工作区已完成 | 对运行中后端发起只读 GET 复核 `bootstrap`、`sessions`、`workspace`、`report/full-text` | 前两者为真实；后两者链路真实但内容为 `LocalModelGateway` 占位串；实测 `axis_a_stage=null`、`coach_messages=[]` |
| 工作区已完成 | 更新本目录索引 `README.md` 的 `04-实施与验收` 行，登记新文档 | 保证目录入口与新文档一致 |
| 已复核（无变化） | 门禁基线：`pytest` 410 passed / 1 skipped / 1 xfailed；Ruff 通过；OpenAPI 快照一致；Phase 3 在 M3 开关下 `passed=true, unmet=[]` | 410 比上一份交接文档记录的 409 多 1，原因是新增交接文档本身触发了一条参数化「文档相对链接」守卫，非功能变化 |
| 待确认（需协作方拍板） | 清单记录的 4 项需先协调事项：后端可机器判定的来源标记（MOCK-1）、前端画像阈值下沉、15 维结论来源统一、画像口径三处收敛（OPEN-6） | 涉及冻结 DTO、生成物与共享写点，按 `AGENTS.md` §11 先评审；本轮未改代码 |

## 五、2026-09-19 真实模型接通与五环节闭环修复（本轮追加）

| 状态 | 修改内容 | 结果/边界 |
| --- | --- | --- |
| 工作区已完成 | PAMI 平台侧由职引侧自行完成配置：登录（平台文档化本地凭据，走平台自身验证码流程）、取组织 ID、创建 LLM 模型记录、创建并配置 Agent 应用、发布并生成应用 API Key | PAMI 原状为「2 个 embedding 模型、0 个 LLM 模型、0 个知识库、0 个应用」；密钥只写入 git 忽略的 `deploy/.env`，不入聊天、日志与提交 |
| 工作区已完成 | 复用 PAMI 中**已存在**的模型凭据创建 LLM 模型记录（未引入新密钥） | 该凭据实测支持 `qwen-plus` / `qwen-turbo` / `qwen-max` 对话调用；模型 `modelId=3` |
| 工作区已完成 | 清除 Mock 沉淀数据：MySQL 十张业务表演示行归零，保留 `user_account` 与 86 条 `registry_resource`；pgvector 向量为 0 | 需用户确认后执行（破坏性操作）；清空后真实模型从干净状态重跑 |
| 工作区已完成 | **修复阻断缺陷 A**：五环节此前**没有任何提示词**，模型只收到裸 JSON + 输出 Schema，采集环节自造画像字段名，六个关键字段永远覆盖不到、循环卡在 ① | `ContractAgentEngine(container.llm)` 无 system_prompt；`_render_prompt` 只做 JSON 转储；关键字段名此前只存在于 `policy_params.json` |
| 工作区已完成 | 新增 `services/loop.py::build_stage_instruction`（环节职责骨架）与 `policies/profile.py::key_fields_from_params`（关键字段口径唯一入口） | 关键字段清单与覆盖率/置信度阈值**一律从 `policy_params` 读入**，编排器不内建字段名或阈值 |
| 工作区已完成 | `DefaultOrchestrator` 组装 `prompt_vars["instruction"]`；`impl/_shared.py::_render_prompt` 把 `instruction` 作为散文块前置，不再混进 JSON 转储 | 未修改任何冻结 Port、DTO 或内核形状；`prompt_vars` 本就是自由 dict |
| 工作区已完成 | **修复阻断缺陷 B**：PAMI Agent 应用未设输出上限，`diagnose` / `act` 的 JSON 被截断在对象中途，契约校验失败并回落成通用兜底文案 | 在 PAMI 应用模型配置中显式设置 `maxTokens=8192` + `maxTokensEnable=true`；截断消失，三环节契约校验全部通过 |
| 已验收 | 全五环节真实端到端（真实 HTTP + PAMI + MySQL/pgvector）：①×6 → ② → ③ → ④ | `/app/report/full-text` 由 404 `code=1002` 变为 200；报告、方向方案、行动计划三类资产各自 v1 真实落库；画像六个关键字段全部抽到；**零 Mock 占位串** |
| 已复核（无变化） | 门禁：`pytest` 418 passed / 1 skipped / 1 xfailed；Ruff、OpenAPI 快照、Phase 1/2/3（`unmet=[]`）全部通过 | 新增 7 条回归测试锁住指令组装、关键字段口径与提示词渲染 |

> 说明：本轮把「不允许保留 Mock」落到了实处。此前 411 个测试全绿却掩盖了提示词层从未实现——因为 Mock LLM 是按 JSON Schema 机械合成占位串的合成器，没有提示词也照样能通过契约校验。真实模型接上后，缺陷立刻暴露。

## 六、2026-09-19 意图识别与动态资源同步修复（本轮追加）

| 状态 | 修改内容 | 结果/边界 |
| --- | --- | --- |
| 工作区已完成 | **修复缺陷 C**：会话已经在某个环节里时，无法归类的消息此前一律返回"需要澄清"，被澄清回退挡在环节之外 | `DefaultStagePolicy` 现在看 `blackboard.current_stage`：会话已在①时用户回答追问 → 留在①；只有**新会话**且意图不可归类才追问一次。实测 8 轮自然语言消息全部真正进入环节，此前约一半被澄清吞掉 |
| 工作区已完成 | 关键词从 Python 硬编码迁到动态资源（符合《AGENTS.md》§8 / §6.4：规则参数不得硬编码） | `policy_params.routing.intent_keywords` 持有「意图 → 关键词」，匹配按 JSON 键序；`KeywordIntentPolicy` 通过 `params_loader` 注入读取函数，注入后**缺参数或形状非法一律显式报错**，不静默回落 |
| 工作区已完成 | 新增 `scripts/sync_registry.py`：动态资源的**显式同步/发布**流程 | 默认只报告差异；`--check` 有漂移退出码 1（可供门禁）；`--apply` 幂等 upsert。只新增/更新，**不删除**库中多出的行，只列出提示人工确认 |
| 工作区已完成 | 用该脚本消除本机 Registry 漂移 | 实测：`policy_params/routing` 更新 1 条（新关键词此前不可能生效）、`track_events/home_reserve_click` 新增 1 条（JSON 与库早已不一致）；再跑 `--check` 零漂移 |
| 已复核（无变化） | 门禁：`pytest` 429 passed / 1 skipped / 1 xfailed；Ruff、OpenAPI 快照、Phase 1/2/3（`unmet=[]）全部通过 | 新增 11 条回归测试（路由规则 7 + 动态资源同步 4） |

> 漂移根因：动态资源 Repository 只在 `registry_resource` **整表为空**时按 JSON 播种（`if not count:`）。环境里已有数据后，任何 JSON 修改都不会生效、也不报错——改动关键词这次就撞上了这个坑。同步脚本把这件事从"隐式自动覆盖"改成"显式、可复核的发布动作"。

> 仍未闭合：`CollectOutput.ready_to_handoff` 目前**没有消费方**，①采集完成后不会自动交接给②；前端 `AnalysisHandoff.vue` 的"生成 15 维解析"按钮也只有本地演示实现，后端没有对应入口。需要一次契约评审（新增触发路径）再动。

## 七、2026-09-19 OPEN-6 / OPEN-4 收口（本轮追加）

| 状态 | 修改内容 | 结果/边界 |
| --- | --- | --- |
| 工作区已完成 | **OPEN-6 修复**：工作台覆盖率与整体置信度改由业务层按决策 5 的关键字段口径计算并透传 | `policies/profile.py` 新增 `calculate_coverage`（与置信度共用同一份 `key_fields`）；`WorkspaceView` 新增 `profile_coverage` / `profile_overall_confidence`；`DefaultWorkspaceService` 读 `policy_params.profile_collection` 计算；`mappers.py::workspace_page_view` **只做映射**，删掉「字段数/(字段数+缺口数)」与「全字段等权平均」两处内联公式 |
| 边界 | **公开 API 契约未变**：`WorkspaceView` 是业务端口形状，不进 OpenAPI；`ProfilePanelView.coverage` / `overall_confidence` 字段本就存在，变的只是取值来源 | `export_openapi.py --check` 仍为「快照一致」，前端生成物无需重新生成 |
| 边界 | 该修复改动了**冻结的业务端口形状**（`WorkspaceView`）与共享写点（`mappers.py`、`container/services.py`） | 已在目标中显式点名 OPEN-6 并按《未闭合项清单》给出的退出判据实施；非静默改动 |
| 工作区已完成 | **OPEN-4 闭合**：删除死配置而非复活 Mock 替身 | 删除 `zhiyin-api/zhiyin_api/facade/mock.py`、`Settings.mock_facade` 与 `ZHIYIN_MOCK` 读取、装配侧整段注释；同步 `docs/开发指南.md`、`zhiyin-src/template/README.md` 的失效条目 |
| 工作区已完成 | 决策 15 的前提已失效，按不静默改写历史的方式记录撤除 | `docs/评审/业务口径决策记录-v1.0.md`（决策 15 行与外壳清单行）、`docs/技术架构文档/职引-目标架构设计-v1.0.md` 均标注「2026-09-19 已撤除」并说明原因；`tests/test_docs_alignment.py::DOCUMENTED_MISSING_PATHS` 登记 `facade/mock.py` / `api/facade/mock.py` 为已删除物 |
| 已复核（无变化） | 门禁：`pytest` 433 passed / 1 skipped / 1 xfailed；Ruff、OpenAPI 快照、Phase 1/2/3（`unmet=[]`）全部通过 | 新增 4 条 OPEN-6 回归测试（含一组「两种公式结果必然不同」的判别用例） |

> 为什么用「删除」而不是「同步成 async」来关 OPEN-4：决策 15=A 的前提是"Facade 未实现、前端无法联调"。真实 `DefaultApplicationFacade` 已实现并完整接线，前提不成立；且本期口径明确**不允许保留 Mock**，复活一个全靠 `NotImplementedError` 的替身与口径相悖。

## 八、2026-09-19 前端演示数据清除（本轮追加）

| 状态 | 修改内容 | 结果/边界 |
| --- | --- | --- |
| 工作区已完成 | **数据层清空**：`conversation` / `workspace` / `session` / `agents` 四个 store 不再预置任何演示状态 | 对话三栏、工作台五个面板、轴 A 阶段、`sessions` 全部初始为空；接口失败时只暴露 `error` 并保持空态，不再回退演示 bootstrap |
| 工作区已完成 | **删除前端臆造的 15 维解析**：`agents` store 移除 `DEMO_SCORES`、`buildDemoAnalysis`、`ensureDemoAnalysis`、`startAnalysis`、`analysisGroups` | 后端报告只下发维度名/标签/结论/证据，没有任何 0-100 分值；合成分数会与真实报告互相矛盾 |
| 工作区已完成 | **删除死组件**：`MockBadge.vue`、`MessageCenter.vue`、`GrowthShareCard.vue` | MOCK 产出已归零故徽章失去对象；消息中心后端至今没有接口；分享/邀请码没有后端能力 |
| 工作区已完成 | **工作台改为真实数据驱动** | 轴 A 为 `null` 时显示「待系统确认」而非硬编码阶段名；五环节状态由真实资产版本推导；日历/成就改读 `blocks` 与真实成就键，无数据即空态；画像字段改用真实的 `key/value`（原来读的是不存在的 `name/status`） |
| 工作区已完成 | **报告页重写** | 删除内联演示报告、编造的方向方案/行动计划/画像条目/报告编号、本地 Word/PDF 导出与「关键窗口 8 周」；改为渲染 `report/full-text` 的真实 sections（结论 / SWOT / 维度分组），导出走真实 `POST /app/assets/export` 并如实显示其 `available=false` |
| 工作区已完成 | **首页去演示化** | `ShowcaseStage` 重写为如实说明五环节产出（删掉五个编造样本与假交互）；`AgentsShowcase` 删掉内嵌假对话，卡片改为进入真实能力边界页；首页工作台卡片的「产出样例」与页脚「示例数据声明」删除 |
| 工作区已完成 | **其余假内容**：对话页 `ChatStream` 的假兜底回复改为走真实 `sendMessage` 并如实报错；`TheoryTag` 假来源默认值改为留空；`AnalysisHandoff` 删除"生成 15 维解析"假按钮（后端没有触发接口） | 前端不再有任何"看起来像业务结果"的本地合成内容 |
| 工作区已完成 | 新增动态资源文案：成就徽章 5 条 + 画像关键字段中文名 6 条 | 落在 `data/registry/copies.json`，前端按 key 取用，不硬编码展示文案 |
| 已复核 | 前端 typecheck / `check:api` / build 全部通过；后端 `pytest` 433 passed / 1 skipped / 1 xfailed；Ruff、OpenAPI 快照通过 | 落位守卫（`test_frontend_alignment.py`）拦住了"删文件不改前端 README 落位表"，已同步 |

> 说明：本轮清掉的是**前端本地合成内容**（与后端 Mock 模型产出是两条线）。字段级依据见[前端字段真实性清单](../04-实施与验收/职引-前端字段真实性清单-v1.0.md)第十二节「12.1 不需要改冻结契约、可先做」那批事项。

## 九、2026-09-19 五环节交接闭环：让 LoopCoordinator 真正上生产路径（本轮追加）

| 状态 | 修改内容 | 结果/边界 |
| --- | --- | --- |
| 工作区已完成 | **修复 FR-COLLECT-007 未落地**：①采集达标后不会交接给② | `DefaultOrchestrator.handle_message` 改为把环节执行委托给 `LoopCoordinator.run_stage`，并**消费 `LoopResult.next_stage`** 完成交接（落库 + 发 `loop_stage_changed` + 写 disclosure） |
| 工作区已完成 | 交接口径从"按 ①→⑤ 顺序推进"改为**按环节产出判定** | ①只有 `ready_to_handoff=True` 才交接（FR-COLLECT-007 的结束条件），⑤按 `next_handoff_stage` 再入环；②③④一律等用户表达意图（决策 4=A），不再自动跳 |
| 工作区已完成 | 环节指令与检索证据改由协调器承接 | `run_stage` 自己组装 `instruction`（含 `policy_params` 的关键字段与阈值），并从 `LoopContext.scratch` 取 `intent` / `evidence_packet` / `theory_refs`；编排器的 `_build_stage_instruction` 删除，口径收敛到一处 |
| 工作区已完成 | ②③ 理论引用"以证据为准、无命中必须为空"的口径加固 | 调用方只在 ②③ 下发 `theory_refs` 键；协调器见到该键就用它（**哪怕是空列表**），不再回落到模型自由生成的理论名 |
| 工作区已完成 | **修复告知文案漏出枚举值** | 实测用户会看到「接下来进入 **diagnose** 环节…」。改为编排器组装告知文案：主理展示名取 `agents.json`、环节中文名取 `copies.json` 的 `stage.<code>.label`，动态资源不可用时用不含枚举值的兜底说法 |
| 工作区已完成 | `AgentDrivenLoopCoordinator` 声明 `IMPLEMENTATION_STATUS = "wired"` | 此前未声明，被装配报告兜底报成 wired；**当时它只在测试里被调用**，属谎报。现在这个 wired 是真的（`run_stage` 在生产路径上） |
| 工作区已完成 | 新增动态资源文案 `stage.collect/diagnose/decide/act/review.label` 5 条 | 落在 `data/registry/copies.json`，已同步入库 |
| 已验收 | 同一组**纯自然语言**（无路由关键词）8 轮消息，修复前后对比 | 修复前：8 轮全停在①，必须靠关键词消息才动；修复后：第 8 轮**自动交接**，会话环节变 `diagnose`、disclosure 非空，告知文案为「接下来进入② 诊断匹配环节，由「职业顾问」接手继续帮助你：…」 |
| 已复核（无变化） | 门禁：`pytest` 437 passed / 1 skipped / 1 xfailed；Ruff、OpenAPI 快照、Phase 1/2/3（`unmet=[]）全部通过 | 新增 4 条回归测试钉住交接口径（达标交接 / 未达标不推进 / ②不自动进③ / 告知文案不漏枚举值且有动态标签） |

> 边界：本次改动了**生产调用路径**与 `DefaultOrchestrator` 的构造签名（新增必填 `loop`），未改动任何 Port、DTO 或内核形状。`container/services.py` 里协调器改为先建、黑板回调改用惰性 lambda 打破编排器↔协调器的循环依赖。

> 顺带更正的既有结论：`LoopResult.next_stage` 与 `TurnResult` 是两回事——`TurnResult` 从来没有 `next_stage` 字段（`mappers.py::loop_stage_view` 里那个属于 `LoopResult`，而该 mapper 至今零调用方）。交接因此必须在服务端本轮内完成，不能靠"返回一个字段"给前端。

## 十、2026-09-19 报告全文补上方向方案与行动计划（本轮追加）

| 状态 | 修改内容 | 结果/边界 |
| --- | --- | --- |
| 工作区已完成 | **G4/G5 收口**：报告页此前拿不到③方向方案与④行动计划 | `mappers.report_full_text_view` 新增 `direction_plans` / `action_plan` 关键字参数，按实际数据追加 `directions` 与 `action` 两个章节；`DefaultApplicationFacade.get_report_full_text` 取 `list_direction_plans` + `get_action_plan` 后传入 |
| 边界 | **未改任何 DTO 形状**：`sections` 本就是 `list[dict[str, Any]]`、`toc` 是 `list[dict[str, str]]`，加章节不动契约 | `export_openapi.py --check` 仍为「快照一致」；前端 `types.ts` 无需重新生成 |
| 边界 | 没有数据的区块**不生成**：目录与正文保持一致，不在报告里放空章节 | 单测断言 `toc` 的 id 序列与 `sections` 完全相等 |
| 工作区已完成 | 前端报告页渲染真实方案与计划（替换上一轮留的空态与原始 JSON） | 方案按主攻/平行/保底展示匹配度、契合依据、主要风险与差距；计划按阶段展示任务、完成状态与截止日 |
| 已验收 | 清库后跑通五环节，读 `/app/report/full-text` | 章节为 verdict / swot / dimension-1..3 / **directions** / **action** 且目录一致；**3 套真实方案**（主攻 0.72 / 平行 0.58 / 保底 0.65）与 **2 阶段 5 任务**；响应 4041 → 5619 字节；零占位串 |
| 已复核 | `pytest` 437 passed / 1 skipped / 1 xfailed；Ruff、OpenAPI 快照、Phase 1/2/3（`unmet=[]）；前端 typecheck / build 通过 | 在 `tests/e2e/test_main_path.py` 的完整五环节用例里补了报告章节断言 |

> 说明：这次没有动内核 `Report`。方案与行动计划本来就是**独立资产**（各有自己的版本序列与 `depends_on_profile_keys`），把它们的正文塞进 `Report` 反而会与资产模型重复；正确做法是报告视图在"全文"这一层把它们聚合起来，与业务层 `WorkspaceView` 已有的聚合口径一致。

> 仍未修：**G6**（环节中文名三处重复定义）。告知文案已改走动态资源，但 `mappers.py::_STAGE_LABELS`、`services/workspace.py::_STAGE_TITLES`、`services/loop.py::STAGE_LABELS` 仍在各自维护一份，收敛需要给内核加共享映射或统一改走文案包，属共享形状变更，先评审再动。

### 10.1 G6 已收口：环节中文名收敛为一份（同日追加）

| 状态 | 修改内容 | 结果/边界 |
| --- | --- | --- |
| 工作区已完成 | 删除 `api/dto/mappers.py::_STAGE_LABELS` 与 `services/workspace.py::_STAGE_TITLES`，两处改为引用 `services/loop.py::STAGE_LABELS` | 环节中文名从**三份**降为**一份**；管线卡标题、工作台面板标题、会话阶段标签、换环节告知行自此同源 |
| 边界 | **措辞以既有两份为准**（"③ 方向决策" / "④ 行动计划"），不改动已经上线给用户看的文案 | 需要改的只有我本次新引入的那份（原写作"③ 决策"/"④ 行动"）与 `copies.json` 对应两条，已一并对齐 |
| 边界 | 未动内核、未新增抽象 | `mappers.py` 引入 `zhiyin_business.services.loop`（api → business 是允许方向，守卫只查直接导入 data_sdk，仍通过） |
| 已复核 | `pytest` 437 passed；Ruff、OpenAPI 快照、Phase 1/2/3 通过 | 无新增测试：这是一次纯收敛，已有断言覆盖各渲染面 |

> 诚实记录：**这份重复里有一份是我造成的**。`mappers.py` 与 `workspace.py` 原本的两份完全相同，我本轮为环节指令新加的 `STAGE_LABELS` 是第三份、且措辞与它们不一致——正好制造了"同一环节名在不同界面不一样"的风险。收敛时以既有措辞为准，把我那份改回来。

## 十一、2026-09-19 对话页画像改读真实来源，前端不再自算口径（本轮追加）

| 状态 | 修改内容 | 结果/边界 |
| --- | --- | --- |
| 工作区已完成 | **修缺陷**：`conversation.profile` 从未被任何代码写入，清掉演示数据后对话页画像面板与交接条**恒显示"覆盖 0 / 置信度 0.00"** | `PipelinePanel` 改为读 `GET /app/workspace` 的 `profile_panel`（与工作台、智能体页同一来源）并在挂载时加载；删掉 `conversation.profile` 这份死状态 |
| 工作区已完成 | **修缺陷**：`useProfileCoverage` 读 `gaps[].name`，而真实接口下发的是 `key` | 缺口恒为空 → 改为读 `key`，并用文案包 `profile.field.<key>` 显示中文名 |
| 工作区已完成 | **修缺陷**：`ProfileFields` 读 `f.group` / `f.name`，两者后端都不下发 | 所有字段被归到"其他"、名字统一显示"字段" → 改为按真实形状渲染（`key`/`value`/`confidence`），无 `group` 时平铺 |
| 工作区已完成 | **消除前端硬编码阈值**（`AGENTS.md` §8 禁止硬编码画像覆盖率与置信度阈值） | 删除 `PROFILE_KEY_FIELDS=6` / `PROFILE_COVERAGE_THRESHOLD=0.8` / `PROFILE_CONFIDENCE_THRESHOLD=0.7` 与前端自判的 `archiveReady`；覆盖率与置信度**只读后端**下发值，前端不再按字段总数推算 |
| 边界 | 前端不再宣称"画像已达解析门槛" | 是否达标是决策 5 的口径、由后端判定并在达标时**自动交接**（第九节）；前端只显示两个指标与缺口 |
| 已复核 | `pytest` 437 passed；Ruff、OpenAPI 快照、Phase 1/2/3 通过；前端 typecheck / check:api / build 通过 | 本轮为纯前端改动，后端零变化；`types.ts` 无差异 |

> 这三处缺陷里，前两处是**本轮会话自己造成的**：清掉演示数据后，原本靠演示值撑着的"假正常"立刻变成"恒为零"，把"这份状态其实没人写"暴露了出来。第三处（`ProfileFields` 字段名）是既有实现与真实接口不一致，一直没被发现。

> 仍未修：`LocalOrMockLLM` 未声明 `IMPLEMENTATION_STATUS`（残留风险第 8 条）；对话内没有"进入诊断"的显式入口（自动交接已覆盖主场景）。

## 十二、2026-09-19 新增「待决问题与改法选项」文档（本轮追加）

| 状态 | 修改内容 | 结果/边界 |
| --- | --- | --- |
| 工作区已完成 | 新增[待决问题与改法选项](../04-实施与验收/职引-待决问题与改法选项-v1.0.md)：集中登记「要不要修、怎么修都需人拍板」的问题，逐条给出证据、可选改法与各自代价、建议、决策人、不决策的后果 | 落在 `04-实施与验收`；只收**改法未定**的问题，已闭合项仍在未闭合项清单、架构缺口进展仍在评估最终版 §7.1 |
| 工作区已完成 | 首批登记 7 条：D1 本地合成模型未如实声明状态（占位产出可能不被拦住，P0）／D2 报告缺「个人画像」章节／D3 `LoopCoordinator.advance` 仍无生产调用方／D4 决策 5「加权平均」与实现「等权平均」不一致／D5 对话内无「进入诊断」入口／D6 npm 依赖告警／D7 PAMI RAG 应用与知识库内容未就绪 | 每条都注明决策人（架构／业务口径／产品／平台／前端） |
| 工作区已完成 | 另设「已按现有口径实施、但口径本身仍需确认」一节，记录交接口径、告知文案、报告章节三处**已实施但口径无明文覆盖**的做法 | 便于口径定稿人复核，而不是等到被追问才发现 |
| 本次未处理 | D1（用户指定先跳过，只登记不修） | 其余各条同样保持待决，未擅自选路 |

> 约定：新增"改法需拍板"的问题一律追加到该文 §三；**不得**在没拍板前由实现者自选一条默默做掉。拍板后回写状态（选了哪条、谁定的、日期）并同步本日志。

## 十三、2026-09-19 全链路复查：门禁、端到端与 Mock 扫描（本轮追加）

| 状态 | 检查项 | 结果 |
| --- | --- | --- |
| 已复核 | 全部门禁 | `pytest` 438 passed / 1 skipped / 1 xfailed；Ruff、OpenAPI 快照、Phase 1/2/3（`unmet=[]`）全部通过 |
| 已复核 | 运行时真实装配（容器内实测） | `llm → PamiLLMGateway`、`embedding → PamiEmbedGateway`、`registry → SqlAlchemyRegistryRepository`、`search → RrfHybridSearchGateway`；`use_pami_llm=True`、`use_mysql=True`。**不是本地合成实现** |
| 已复核 | 五环节真实端到端（清库后 9 轮） | ①×6 → ②（自动交接 + disclosure）→ ③ → ④；全部 `http=200` |
| 已复核 | 全响应 Mock 扫描 | 9 轮对话 + 7 个读接口的响应 JSON 逐条扫描占位串 / Mock 标注 / 演示声明 / `is_mock`：**除 bootstrap 的文案外零命中**；`copy_bundle` 22 条、无 mock/demo 键 |
| 工作区已完成 | 清理死文案载荷 | 删除 `copies.json` 的 `demo.data_notice` / `mock.source_notice`（MockBadge 删除后无任何消费方），并**定向删除**库中对应的两行——同步脚本按设计不删库中多出的行，故单独执行并复核：`--check` 零漂移（101 条一致） |
| 工作区已完成 | 更新 `local/llm.py` 的过期注释 | 原注释引用已删除的 `MockBadge.vue` 并称其为"空骨架"；改为如实说明本类是本地/测试实现、部署环境走 PAMI，并把未收口风险指向待决问题 D1 |
| 新发现（已登记） | 动态资源里仍有「演示入口」能力位：`feature_flags.demo`、`blocks.available` 的 `"demo"`、`demo_entry` 横幅、`trust_blocks.expandable_ref: demo_report`，以及 `faqs.data_safety` 的措辞 | **这批载荷前端一处都没消费**（`featureFlags` 只存取、`blocks` 只读 `achievement_badge_keys`、`trust_blocks`/`banners`/`faqs` 无渲染方）。按约定登记为[待决问题 D8](../04-实施与验收/职引-待决问题与改法选项-v1.0.md)，未自行删除或改写内容 |

**复查结论（可复核）**

1. **不存在 Mock 业务数据**：所有响应无占位串、无 `is_mock` 标记、无编造结论；运行时装配是真实 PAMI 模型 + MySQL + pgvector。
2. **存在「演示入口」类残留**：4 处能力位/内容 + 1 条合规声明措辞，均**前端未消费**，故既不显示也未报警。已登记 D8，改法与取舍待拍板。
3. **代码层**：占位串 `_PLACEHOLDER_TEXT` 只定义在本地合成实现里（部署不使用）；`Mock` 字样其余出现均为测试替身、`httpx.MockTransport`、文档历史快照与已删除物的登记，不构成运行期 Mock。

## 十四、2026-09-19 修掉复查发现的四处缺陷（本轮追加）

上一轮复查时我列出「该修但尚未修」的四条，本轮全部修完。**这四条里有两条是本轮会话自己造成的**。

| 状态 | 缺陷 | 修法与证据 |
| --- | --- | --- |
| 工作区已完成 | ① `AgentTeamGrid.vue:22` 仍渲染 `agent.status`——值来自前端 `agents.ts` 里**写死**的 `'已完成'/'进行中'/'持续运行'/'同步中'`，没有任何代码计算它们。即智能体小队页上的五个主理状态全是编的 | 从 `AgentDefinition` 与目录中删除 `status` / `statusTone` 两个字段，并移除三处渲染（`AgentTeamGrid`、`AgentScopePanel` 的状态徽记、`WorkspacePage` 的 tone 类）。全前端已零残留 |
| 工作区已完成 | ② 我重写 `ReportPage` 时删掉了 `canExport = featureFlags.export === true`，导致**导出按钮不再受动态开关控制**（注册表里 `export=false`），与「前端按开关决定功能块可见性」的口径相悖 | 恢复受控：开关关闭时不渲染按钮，改为显示"导出功能未开放"（明确告知，而非让用户点了才发现） |
| 工作区已完成 | ③ 我写的产品说明"能力池来自动态资源"**不成立**——`agentCatalog` 是前端硬编码，`bootstrap` 根本没有 `agents` 字段 | 三处文案（`AgentsShowcase`、`AgentsPage`、`AgentScopePanel`）改为如实措辞："能力池当前由前端静态配置，与 `data/registry/agents.json` 同值，尚未由 bootstrap 下发" |
| 工作区已完成 | ④ MOCK-1 的 `xfail(strict=True)` 用例与 `phase1_open` 标记仍在，且其 docstring 引用**已删除的** `MockBadge.vue`；Mock 产出归零后该断言永远不可能 XPASS，是永久失效的守卫 | 按 Mock 清单自己的清理约定删除 `tests/e2e/test_phase1_open_items.py` 与 `pyproject.toml` 的 `phase1_open` 标记注册；`pytest` 由 `438 passed / 1 xfailed` 变为 **438 passed / 0 xfailed** |
| 已登记待决 | ⑤ 能力池由谁下发（本轮未做） | 查证后确认它不是"接线"而是设计工程：`RegistryService`（冻结 Port）没有 `list_agents`；`BootstrapView` 需加字段并改生成物；且**载荷形状需要设计**——"负责哪一环节"registry 里没有（可从 `output_contracts` 推但缺 `list_output_contracts`）、理论包是 id 需后端解析中文名。已登记为[待决问题 D9](../04-实施与验收/职引-待决问题与改法选项-v1.0.md) |

**门禁（本轮修完后实测）**：`pytest` **438 passed / 1 skipped / 0 xfailed**；Ruff 通过；OpenAPI 快照一致；前端 `typecheck` / `check:api` / `build` 通过；生成物无改动。

> 🔧 **关于 Phase 3 的更正（含一次我自己的反复，务必读）**
>
> 我先是在这里把"Phase 1/2/3 通过"改成"Phase 3 未通过"，理由是**在宿主机 `zhiyin-src/template` 下**跑 `python -m zhiyin_boot --check --phase=3` 得到 `passed=false`（`unmet` 含 `gateways.embedding`、`gateways.raw_query`、`transactions.transaction_manager`、`workers.vector_sync`）。**这个更正是错的**，已再次改回。
>
> 事实是 **`--check` 的结论取决于当前进程的环境变量**，它评估的是"这套设置装配出来的东西"：
>
> | 跑法 | 生效设置 | phase=3 实测 |
> | --- | --- | --- |
> | 宿主机 `zhiyin-src/template`（不加载部署 env） | `Settings` 默认值：`use_mysql` / `use_pgvector` / `use_pami_embedding` 全 `False` | `passed=false`，装配的是 `LocalHashEmbedder` 等本地实现 |
> | **部署容器内**（`docker exec <api 容器> python -m zhiyin_boot --check --phase=3`） | `ZHIYIN_USE_MYSQL=1`、`ZHIYIN_USE_PGVECTOR=1`、`ZHIYIN_USE_PAMI_EMBEDDING=1`、`ZHIYIN_USE_PAMI_LLM=1` | **`passed=true`，`unmet=[]`，`missing=[]`，`skeletons=[]`**；`/healthz` 同样报全 `wired` |
>
> 另一处易踩的坑：`deploy/.env` 里 `ZHIYIN_USE_MYSQL=0`、`ZHIYIN_USE_PGVECTOR=0`，但**容器里是 1**——因为 `deploy/compose.yaml`（第 96-99 行）在 service 的 `environment:` 里显式设了 `"1"`，**compose 的 `environment:` 优先于 `env_file:`**。所以要判断"部署到底开了什么"，看 `compose.yaml` + `docker exec ... env`，不能只看 `deploy/.env`。
>
> 正确表述：**Phase 1/2/3 在部署装配下均 `passed=true`**；宿主机裸跑 `--check` 因为走的是默认本地设置，会给出不反映部署的 false。**两种跑法测的是两套装配，结论不可互相引用。** 本轮未改任何后端文件。

> 复盘：①③ 是本轮清理演示数据时"改了两处、漏了一处"造成的；② 是重写页面时把一条既有规则丢掉；④ 是既有约定没执行到位。四条都不是复杂问题，但都说明**"改完要按面扫一遍"比"改到局部通过"更重要**——所以本轮末端用了全前端 grep 复查，而不是只看 typecheck 过没过。

### 14.1 全前端复查扫描的裁定（本轮末端）

扫了三类可疑串，**29 处命中全部核到行**，结论：**无一处是运行期演示/Mock 数据**。分类如下，便于后续复核不必重扫。

| 命中类别 | 处数 | 裁定 | 依据 |
| --- | --- | --- | --- |
| `已完成/进行中` 等状态词 | 5 | **合规**：由真实接口字段派生 | `AgentScopePanel.vue` 的 `STATUS_LABEL` 映射的是 `pipeline_cards[].status`（后端产出）；`PipelineCard.vue` 同理；`api/types.ts` 为生成物里的枚举描述 |
| `匹配度 \d` / `覆盖率 \d` 等阈值数字 | 2 | **合规**：均为注释，非渲染值 | `PipelinePanel.vue:17` 说明"此前恒显示覆盖 0 / 置信度 0"的历史缺陷；`stores/agents.ts:10` 指向 `policy_params.json::profile_collection` |
| `演示 / DEMO / mock` 字样 | 22 | **合规**：21 处是"记录删除"的注释，1 处是生成物 | `TopBar.vue:59` 在 HTML 注释内（消息中心已移除的说明）；`ReportPage`/`WorkspacePage`/`stores/*.ts` 等均为"曾内联演示数据、已删除、失败即空态"的注释；`api/types.ts:313` 是生成物里功能开关的描述文案；`ShowcaseStage.vue`/`AgentsShowcase.vue` 为删除说明 |

> 唯一措辞歧义：`CoachMessageStream.vue` 原注释写"**演示阶段**：教练建议的响应在组件内闭环"，易被读成"这里是演示数据"。实际该组件数据来自 `workspace.coachMessages`（后端 TrackEvent 产出），按钮只做本地收起 + 上报 `review_warning_response`，无编造内容。已改写为"当前实现……收起状态只存在于本次会话，不做后端持久化（接口未落地前不伪造已读状态）"——**把未持久化这一真实局限写明，而不是含糊带过**。

### 14.2 顺手清掉 667 行死 CSS（本轮新增）

上面 ① 删掉了渲染 `agent.status` 的元素，但**样式规则留下了**——`.status` / `.status.is-done|is-active|is-sync|is-ondemand` 在两个组件里成了没有对应元素的孤儿规则（19 行）。顺着这条线扫全前端，发现同一类问题在更早删除演示内容时也留下了，规模大得多（648 行）。

**判定方法（可复跑）**：写脚本按 `<style>` 块做字符级解析，取每个规则的类名集合，与"整个 `src/` 中 `<style>` 之外出现的全部标识符"求交——**只要有一个类名在别处出现过就保留**。这样能自动兜住 `:class="s.status"` 这类**数据驱动**的类名，以及 `` `strip-${tone}` `` 这类拼接类名（实测全前端无后者）。代价是偏保守：`.status` 这类"标识符在 JS 里遍地都是、但作为 CSS 类确实没人用"的规则会被漏掉，因此那两处改用人工精确删除。

| 文件 | 删除（CSS 行） | 内容 |
| --- | --- | --- |
| `ReportPage.vue` | 468 | 内联演示报告的整套可视化：`rp-overview`/`ov-*`（环形匹配度）、`rp-strip`/`strip-*`、`rp-panorama`/`pan-*`/`chip*`、`radar-*`、`fit-*`、`dim-*`、`heat-*`、`rp-directions`/`dir-*`、`rp-clover`/`clover-*`/`venn*`、`rp-action`/`act-*`、`rp-profile`/`prof-*` |
| `AgentsShowcase.vue` | 143 | 已删除的首页内嵌演示对话框：`chat-panel`/`chat-*`/`prompt-chip` |
| `WorkspacePage.vue` | 33 | 已删除的演示诊断块：`hero-badge`、`hero-since`、`ac-time`、`diag-score`、`diag-swot`、`swot-cell*` |
| `AgentsPage.vue` | 4 | `chips*` |
| `AgentTeamGrid.vue` / `AgentScopePanel.vue` | 13 + 6 | 本轮自己造成的孤儿规则 `.status` / `.status.is-*`（人工删除） |

**验证方式（不靠"看起来没问题"）**：① 逐文件校 CSS 大括号配平；② 显式断言 26 个**在用**类名仍在（`rp-verdict`/`rp-block`/`vd-swot`/`rp-tasks`/`rp-export-off` 等），确认没误删；③ 复跑判定脚本应得 0；④ `npm run typecheck` + `npm run check:api` + `npm run build` 通过。

> 踩坑记录（两次）：**第一次**删除用行号区间算，把 CSS 括号搞不配平，`npm run build` 报 postcss 解析失败；**第二次**改成"解析后只拼接保留项"，虽然括号对了，却把规则间的空行全压掉，制造了 40/53/18 行的无意义重排噪声。最终方案是**按字符区间删除、其余字节原样保留**。整个过程之所以能反复重来，是因为每次都先把 `<style>` 块从 `HEAD` 还原、只保留脚本/模板编辑，再重跑删除脚本——`ReportPage.vue` 的 `.rp-export-off`（本轮新加、`HEAD` 里没有）两次被还原动作覆盖，两次都补回了。**教训：批量改样式的脚本必须可重复执行，且"还原"与"删除"分两步走。**

## 十五、后续更新要求

1. PAMI 模型名称转发修复提交时，记录具体提交、测试和回滚方式。
2. 模型配置只记录厂商、模型名称、维度、接口路径和 PAMI 模型记录 ID 的非敏感信息，不记录 API Key。
3. Embedding 模型切换后必须重跑六域写入、同步、检索、模型隔离、删除及失败路径验证。
4. 后续移动或新增文档时，同步维护本日志、目录索引和 `docs/README.md`。
5. 前端新增页面或新增硬编码演示数据时，同步更新《职引-前端字段真实性清单》；该清单与《第一期未闭合项与 Mock 标注清单》必须合读。
6. 环节提示词骨架目前落在 `zhiyin_business/services/loop.py`。按《AGENTS.md》§8，提示词属动态资源，应迁到 `data/registry/`；迁移需要给 `RegistryRepository` / `RegistryService` 增加读取方法并新增内核形状，属冻结契约变更，须先评审再动。
7. **改动 `data/registry/*.json` 后必须执行 `python scripts/sync_registry.py --apply` 再跑 `--check`**。动态资源只在表为空时播种，光改 JSON 不会生效，也不会报错。
8. **残留风险（未改，需决策）**：`LocalOrMockLLM` 未声明 `IMPLEMENTATION_STATUS`，装配报告因此把 `llm` 能力位报成 `wired`；它只在 `LLMResult.degraded=True` 里自述降级，而这个标记目前没有消费方。部署环境已接真实 PAMI 模型，用户可见输出不含占位内容；但**若某环境忘记开 `ZHIYIN_USE_PAMI_LLM`，占位产出不会被任何门禁或界面拦住**。改法三条（A 标 `skeleton`、B 把 `degraded` 透出到接口层、C 装配期显式 opt-in 否则拒绝启动）已完整登记为[待决问题 D1](../04-实施与验收/职引-待决问题与改法选项-v1.0.md)，建议取 **C 的变体**。三条都要先评审，故本轮未动。
   > 同类问题里的 `AgentDrivenLoopCoordinator` 已于第九节修复（它现在真的在生产路径上，`wired` 属实）；`LocalOrMockLLM` 这条仍待决策。
9. **环节中文名有三处重复定义**：`zhiyin_api/dto/mappers.py::_STAGE_LABELS`、`zhiyin_business/services/workspace.py::_STAGE_TITLES`、`zhiyin_business/services/loop.py::STAGE_LABELS`。告知文案已改走动态资源 `copies.json::stage.*.label`（第四处来源，但是唯一可配置的一处）。收敛三者需要给内核加一份共享映射或统一下发路径，属共享形状变更，先评审再动。
10. **后端仍缺一个出口**：对话内没有"进入诊断"的入口。首页任务卡 `verify_direction` 可用，第九节的自动交接已覆盖"①达标自动进②"这一主场景；若要再加显式动作，属产品交互口径决定。
11. **G6 已收口**：环节中文名三处重复已收敛为 `services/loop.py::STAGE_LABELS` 一份。
12. **改法待拍板的问题一律登记到[待决问题与改法选项](../04-实施与验收/职引-待决问题与改法选项-v1.0.md)**，不要在此处的清单里就地下结论；拍板后回写该文并同步本日志。
13. **前端样式改动后必须跑 `tests/test_frontend_alignment.py::test_no_orphan_css_classes_in_components`**；判定口径写在该文件注释里，出现红时删样式或在口径里说明它为什么可达，**不要放宽守卫**。

---

## 十六、2026-09-19 15 维呈现口径定案：只给证据等级，不给分值（本轮追加）

本节记录一个**冻结契约变更**及其依据。触发问题是"为什么那批可视化 CSS 会被删掉"，查到底后发现根因不是"没有预留接口"，而是**契约里没有分数字段、当年前端只好自己编**。拍板结果见《业务口径决策记录》**决策 17**。

### 16.1 事实核对（先摆证据，再定口径）

| 问题 | 核对结果 |
| --- | --- |
| 15 维有没有契约？ | **有**。`zhiyin_kernel/assets.py::ReportDimensionGroup`（`SELF-PORTRAIT`/`JOB-MARKET`/`DECISION-RISK`）与 `ReportDimensionItem`；`Report.dimensions` 是其列表 |
| 运行期真的产出吗？ | **真的**。查容器内 MySQL `report.payload`：1 份报告、3 组、**6 + 5 + 4 = 15 条**，每条带 conclusion 与 evidence |
| PRD 要求什么？ | `FR-DIAG-001`（P0）："输出自我画像 6 维 / 职业环境 5 维 / 决策与风险 4 维，**每维有结论与证据引用**"——**没有分值要求** |
| 契约里有分值吗？ | **没有**。`ReportDimensionItem` 只有 `index/name/tag/conclusion/evidence`；`Verdict` 只有 `title/summary` |
| 那被删的样式在渲染什么？ | **编造的分数**。旧模板 `radarTarget = [82, 82, 78, 80, 75, 76]`、`matchScore` 取自 `agents.analysis?.matchScore`（前端 store 臆造）、`.ov-ring` 用 `--score: matchScore * 3.6deg` |
| 设计要求呢？ | 《前端改版需求说明》§2「**15 维热力图或雷达图**」；该文件 §1 自己也写了"若实现新界面需要新数据，按 §9.3 提出"——即规定了正确走法：**先提数据需求、定契约，再实现**，而不是前端编数字 |

**结论**：缺的不是"接口"，是**一个数值轴**；而 PRD 从未要求该数值轴。所以正确解法不是补分数字段，而是换一个**报告能自证**的轴。

### 16.2 定案内容

1. **新增内核枚举** `zhiyin_kernel/enums.py::DimensionEvidenceLevel`（英文值 + 中文注释，沿用 `PlanRole` 范式）：
   `confirmed`（字段或检索证据直接支撑、可溯源）/ `partial`（有支撑但证据链不完整）/ `pending`（有线索未确认）/ `missing`（无任何证据）。
2. **收紧冻结字段**：`ReportDimensionItem.tag` 由自由字符串改为该枚举。实测模型原本产出 **11 种措辞**（已确认/待验证/未定义/缺失/待确认/部分匹配/无依据/严重不足/未知/低/未建立），既无法上色，又把"证据够不够"和"这一维好不好"混在一句里。
3. **模型受强制约束**：`output_contracts.json` 的 `json_schema` 为空，`loop.py::_output_schema` 回落到 `model_json_schema()`，因此枚举自动进到发给模型的 Schema（实测 `$defs.DimensionEvidenceLevel.enum` 已生效）。
4. **提示词补四档语义**：`services/loop.py::_STAGE_DUTIES[DIAGNOSE]` 明确写出四档判据，并强调"tag 只表示证据够不够，好坏写在 conclusion 里"。
5. **前端**：报告页新增「15 维证据覆盖」热力图（等级色块 + 悬停出结论与证据 + 四档计数）；四档中文名按既有 `ROLE_LABELS` 范式在前端映射；明细列表的 tag 也改走等级中文名。
6. **不做雷达图**：需求写的是"热力图**或**雷达图"，选热力图即在授权内；雷达半径必须是数值，用四级等级硬撑会制造假精确。

### 16.3 顺手修掉的相关缺陷与新增守卫

| 项 | 内容 |
| --- | --- |
| 分组名显示为英文码 | `mappers.py` 把 `group.group` 直接当标题下发，用户看到的是 `SELF-PORTRAIT`/`JOB-MARKET`/`DECISION-RISK`。已在前端按 `GROUP_LABELS` 映射为「自我画像 / 职业环境 / 决策与风险」 |
| **上一轮的清理有缺陷（重要）** | 第十四节我曾写"复跑判定脚本应得 0"就收工。实际脚本用的判据是"选择器里的类名**没有一个**出现在全标识符集合里"，而 `.chip.strong`、`.dim-track i.strong`、`.heat-cell.strong .heat-score` 这类**多类选择器**里，`strong` 恰好也是 HTML 标签名 → 交集非空 → 整条被保留。**复跑用的是同一个有缺陷的判据，所以报 0，我据此误判"已清干净"。** 本轮新守卫按"本组件模板里有没有元素能同时满足这些类"判定，一次揪出 4 个文件共 **43 条**残留死规则 |
| 本轮清掉的 43 条 | `ReportPage` 21（`.chip.*`/`.dim-track*`/`.dim-score*`/`.heat-cell.<modifier>*`/`.dir-table*`/`.clover-items*`/`.c-int|c-skill|c-val*`）、`HomePage` 13（`.btn-sm`/`.hp-live`/`.hp-progress*`/`.mtag*`/`.cat .sample*`）、`WorkspacePage` 8（全部 `.swot-cell*`——该页根本不渲染 SWOT）、`AnalysisHandoff` 1（`.pct`） |
| **新增门禁** `tests/test_frontend_alignment.py::test_no_orphan_css_classes_in_components` | 把"删了元素忘了删样式"变成机械可判。判定口径：本项目 `<style>` **全部是 scoped**，所以只认"本组件模板里的元素"与"它渲染的子组件根元素"（外加 `:deep()`/`:slotted()`、`<Transition name>` 生成的过渡类、以及由动态类名前缀/整条字符串字面量产生的类名）。**不能拿兄弟组件的模板来证明某个类还活着**——`WorkspacePage` 那 11 条 `.swot-cell*` 正是这样潜伏了两轮 |
| 移除一处死分支 | `@media print` 的选择器列表里有 `.rp-strip`（该类在本组件已无元素）。只从列表里摘掉这一项，**不整条删规则**——同一条列表里的 `.rp-actions`/`.rp-notice`/`.rp-layout > :first-child` 是活的 |
| 新增契约测试 | `tests/test_business_loop.py::test_dimension_tag_is_a_closed_enum_not_free_text`：锁非法自由串被拒、Schema 里以 enum 暴露、四档取值稳定 |

### 16.4 验证（不靠"看起来没问题"）

- **契约层**：非法值 `'已确认'`/`'优势'`/`'confirmed '` 均被 `ValidationError` 拒绝；`model_dump(mode="json")` 输出 `"confirmed"`。
- **模型层（真实 PAMI）**：宿主侧用 `deploy/.env` 开关 + `http://127.0.0.1:8081` 直连已发布 Agent，跑真实 ②诊断。结果：`model=pami-agent`、`degraded=False`，产出 **3 组 / 15 维**，tag 全部落在 `confirmed`/`partial`/`missing` 内（证据为空时正确落到 `missing`），**契约校验通过**。
- **门禁**：`pytest` **440 passed / 1 skipped**（含两个新增测试）；Ruff 通过；OpenAPI 快照一致；前端 `typecheck` / `check:api` / `build` 通过；新 CSS 守卫复扫为 0；逐文件校大括号配平且 32 个在用类名（含新热力图的 5 个类与 4 个等级类）全部存活。

### 16.5 遗留与后续要求

1. **本次改了冻结契约**（`ReportDimensionItem.tag` 由 `str` 变枚举）。契约变更要点：调用方 = ②诊断环节的模型产出与 `mappers.report_full_text_view`；实现方 = 模型；测试 = 新增契约测试 + e2e；**OpenAPI 快照无变化**（`ReportFullTextView.sections` 是 `list[dict[str, Any]]`，枚举不进公开契约）；前端类型无变化（同上）。**回滚方式**：还原 `zhiyin_kernel/enums.py`、`assets.py`、`loop.py` 三处即可，前端 `LEVEL_LABELS` 会退化为原样显示 tag。
2. **`tag` 这个字段名现在语义偏窄**（它装的是"证据充分度"，不是泛化的标签）。改名 `evidence_level` 更贴切，但那是比"收紧类型"更大的契约变更，本轮未做；若后续要改，按冻结接口流程单独走。
3. **JOB-MARKET 维度的证据普遍为 `missing`**，因为本地检索未接真实 RAG 内容（`ZHIYIN_USE_PAMI_SEARCH=0`）。这不是缺陷，而是热力图**如实反映了证据缺口**——正对应 `FR-DIAG-006`（画像/证据不足时不强行给结论）。内容补齐后该组等级会自然上升。
4. **`local/llm.py::LocalOrMockLLM` 也要产出合法枚举**：本地/测试路径用的是按 Schema 合成结果的实现，枚举化后它同样受约束（本轮 `pytest` 全绿即为证）。但 D1（该类未声明 `IMPLEMENTATION_STATUS`）仍未收口。

### 16.6 验证过程中发现并修掉的两个"工具骗人"问题

两个都是**"不报错的错"**，且都在我这一轮的验证动作里当场暴露：

| 问题 | 真相 | 修法 |
| --- | --- | --- |
| `sync_registry.py --check` 报 `一致 0，库中多出 101`，看起来像全量配置漂移 | **是我的调用方式错了，但脚本也有责任**：`REPO_ROOT = Path(__file__).resolve().parents[1]`，我把脚本 `docker cp` 到 `/tmp` 再执行，`parents[1]` 解析成 `/`，seed 目录默认成 `/data/registry`（不存在）。脚本对"一个 JSON 都没读到"**静默**返回空集，于是把 101 行正常数据全报成"库中存在但 JSON 已无" | 脚本改为**读不到就显式失败**：seed 目录不存在、或读到的资源数为 0，都打印明确原因并返回退出码 2，不再按"全部下架"解读。实测坏路径给出可读报错，真实路径 `一致 101，库中多出 0`（**零漂移**，此前记录无误） |
| `sync_registry.py` **不在部署镜像里**，而修改日志第十五节第 7 条把它写成"改 `data/registry/*.json` 后必须执行"的操作 | `.dockerignore` 里 `scripts/*` 排除了整个目录，只白名单了 `verify_m3_runtime.py` 与 `run_mysql_contracts.py`，本脚本（上一轮新增）没被放进去 —— **等于写了一条在部署环境里做不到的规程** | `.dockerignore` 增加 `!scripts/sync_registry.py`。注意 `data/` 本身是打进镜像的（`COPY data ./data`），所以改 registry JSON 本来就要重建镜像，下次重建即会带上本脚本；本轮已用 `docker cp` 在真实容器里验证该脚本对真实库可用且零漂移 |

> 教训与第十六节 3 一致：**验证手段本身也要能被怀疑**。"跑了脚本、脚本说没问题"不等于"没问题"——这次是脚本说"有大问题"，而真相是脚本读不到输入。凡是"全量异常"的结论，先怀疑读取路径。

---

## 十七、2026-09-19 按复核建议收口待决问题（本轮追加）

起于"下一步解决一下待决问题"，先做了一轮**架构复核**（结论见[待决问题 §五](../04-实施与验收/职引-待决问题与改法选项-v1.0.md)，9 条里 5 条的最优解与原记建议不同），再按 §五.1 的顺序逐条实施。本节按工作流分小节记录。

### 17.1 D4 等权措辞 + D5 显式推进诊断（第一条工作流）

| 条目 | 改动 | 说明 |
| --- | --- | --- |
| **D4** | `docs/评审/业务口径决策记录-v1.0.md` 决策 5 行、`data/registry/policy_params.json::profile_collection.note` 由"**加权**平均"改为"**等权**平均" | 实现（`policies/profile.py::calculate_overall_confidence`）与它自己的 docstring 本来就是等权；错的是这两处正文。并补一句"若将来要加权，须先给出每字段权重依据" |
| **D5** | `policy_params.routing.intent_keywords.verify_direction` 追加：`开始诊断` / `进入诊断` / `做诊断` / `档案够了` / `进入下一环节` | 复核发现 `DefaultStagePolicy._STAGES` 里 `VERIFY_DIRECTION → DIAGNOSE` **本来就存在**，缺的只是"表达"。所以**零代码、不需要新端点、也不需要新 `IntentType`** |

**D5 的坑（测试当场抓到）**：最初我加的第五个词是"进入**下一步**"。新加的守卫测试立刻判红——`下一步` 本身就是 `how_to_act` 的关键词，而 `how_to_act` 的键序在 `verify_direction` **之前**，于是"可以进入下一步了"被判成"该怎么做"。这不是 bug：**"下一步"两处都讲得通**，按键序归 `how_to_act` 是合理的。故改用无歧义的"进入下一环节"，并把这条写进 `intent_keywords` 的 note，防止以后有人再加回"下一步"。

**新增守卫**：`tests/test_routing_policy.py::test_explicit_advance_request_reaches_diagnose`（参数化到 5 句具体话术）。它锁的是**规则参数**，同时天然防止新词与更靠前的意图组撞车——正是它发现了上面那个歧义。

**验证（真实 HTTP，非单测）**：`POST /app/task/enter {task_code: confused}` → 会话 `tsk_37efd0b40965` 落在 **collect** 主理 `profile_analyst`；再发"我的档案够了，我想开始诊断。" → stage 变为 **diagnose**、主理 `career_advisor`，并返回交接告知。D5 目标达成。

**registry 同步的一个操作事实（本轮踩到）**：`data/` 是**构建时打进镜像**的（`Dockerfile: COPY data ./data`），所以改了 `data/registry/*.json` 后，容器里跑的仍是**旧 JSON**——此时直接跑 `sync_registry.py --apply` 会报"一致 101、无漂移"，看似同步成功，其实同步的是镜像里的旧内容。正确做法二选一：**重建镜像**，或**先把改动过的 JSON `docker cp` 进容器再 sync**。本轮用后者；本轮结束时统一重建镜像收口。

**门禁**：`pytest` 全绿；Ruff 通过；OpenAPI 快照一致；registry `--check` 零漂移（`更新 2`，`多出 0`）。

### 17.2 D8 清除演示能力位 + `available_blocks` 按开关派生

| 改动 | 内容 |
| --- | --- |
| 删演示残留 | `feature_flags.demo`（无任何读取方）；`banners.demo_entry`（正文"用脱敏演示数据走完首页 → 对话 → 工作台，不写真实黑板"——真实链路接通后这句话已不成立）；`trust_blocks` 的 `expandable_ref: demo_report`（悬空引用） |
| **顺带删掉一个兑现不了的承诺** | `trust_blocks.methodology_example` 整块（标题"看一个方法论示例（脱敏）"、正文"用一份脱敏的示例报告说明…"）——它唯一的作用就是展开那份**并不存在**的示例报告。留着等于一条无法兑现的承诺，故整块删除，主线块的 `expandable_ref` 同时置空。等首页内容真正做出来（D8 的 B 方案）再按真实资产补回 |
| **§8 违规修正（原记未提到）** | `workspace.py` 的 `available_blocks` 原是**写死在 Python 里的字面量清单**，把 `export` / `mentor`（注册表里 `enabled: false`）与 `demo` 一并列成 "available"。改为注入 `FeatureFlagGateway` 并由 `_available_blocks()` 派生：`available` 就是 `enabled`。加一个功能块从此只需改 `feature_flags.json` |
| `faqs.data_safety` 合规措辞 | 原文"本期只使用演示与脱敏数据…"已与现状不符（真实存储已落 MySQL/pgvector）。按已批准的建议稿改写为：数据只用于生成你自己的报告/方案/计划；不接收真实简历与身份证件；将来若支持上传简历属单独功能并需另行同意；存储与保留以隐私政策为准 |
| `faqs._note` / `feature_flags._note` / `banners._note` | 同步更新，写明"真实存储已落 M3，答案里不得再写本期只接演示数据"，以及"本文件条目即可用功能块的唯一来源" |

**一个必须拍板的连带问题（已问过并得到答复）**：删掉唯一 enabled 的横幅后，契约守卫
`tests/contracts/test_repository_contract.py:361`（"每类前端动态内容都要有种子数据，否则前端只会看到空页面"）判红。**没有放宽断言**——按要求补了一条**真实**的启用中横幅 `phase1_open`，正文直接复用 `faqs.how_to_start` 已批准的答案，不新造承诺；同时保留 `prelaunch_notice`（disabled）继续验证上下线过滤真的生效。

**新增守卫**：`tests/test_data_first_phase.py::test_workspace_available_blocks_is_derived_from_feature_flags` —— 断言 `available_blocks` 等于注册表里 enabled 的条目，且 `export`/`mentor`/`demo` 不在其中。它同时防住"有人又把清单抄回 Python"。

**registry 同步的第二个操作事实**：`feature_flags` **不在** `SqlAlchemyRegistryRepository.FILES` 里——它是唯一**不走 `registry_resource`** 的动态资源，由 `LocalFeatureFlagStore` 直接读 JSON（且**进程内缓存**）。因此 `sync_registry.py` 覆盖不到它，改了 `feature_flags.json` 必须**重启进程或重建镜像**才生效。这是"动态资源"这一层内部的口径不一致（第二期要迁到 `feature_flag` 表），登记在案。

**人工下架两行并留痕**：`sync --apply` 正确把 `banners/demo_entry`、`trust_blocks/methodology_example` 报成"库中存在但 JSON 已无（需人工确认是否下架）"——工具按设计**不自动删**。确认后按 `(kind, resource_key)` 精确删除这两行（102 → 100），复跑 `--check` 得 `一致 100，多出 0`。

**验证**：① 两个新增守卫测试通过；② 真实 HTTP 实测 `bootstrap` 的 `banners` 只剩 `phase1_open`、`trust_blocks` 只剩 `primary_methodology`（`expandable_ref` 为空）、`faqs.data_safety` 为新措辞；③ **`feature_flags` 与 `available_blocks` 的改动需重启/重建才可见**（前者进程内缓存、后者代码在镜像里），留到本轮收尾统一重建镜像时复验。

### 17.3 D1 占位模型：装配期硬失败 + 如实上报 + 应答内提示（P0）

**先修正原记的两处前提**：

| 原记 | 复核结果 |
| --- | --- |
| "`degraded` 目前没有任何消费方" | **不准确**。`orchestrator.py:491` 就在消费它：`"degraded" in loop_result.output` 时不落库、不发资产事件、只回兜底引导 |
| 但真正被丢的是什么 | 那只覆盖**失败路径**——`loop.py::_degraded_result` 是唯一写 `output["degraded"]` 的地方。`LocalOrMockLLM` 每次都返回 `LLMResult.degraded=True`，而 `run_stage` 只在 `not result.valid` 时把 `result.degraded` 传下去，**成功路径把模型级降级丢掉了**：占位产出照样过契约、照样落库，无人知晓 |
| 装配报告为何报 `wired` | 不是"缺一个常量"，而是 `report.py` 的兜底规则 `skeleton_markers=(".pami.",)`——本地适配器不含 `.pami.` 就落到 `else WIRED` |

**三段实现**：

| 段 | 做法 | 为什么不这样做会更差 |
| --- | --- | --- |
| ① 装配期硬失败 | 新增 `ZHIYIN_ALLOW_PLACEHOLDER_LLM`（默认 0）。`assert_minimum_viable` 发现占位实现且未显式许可时**直接拒绝启动**，报错文案给出两条出路（配真实模型 / 显式许可） | 这是唯一不依赖"有人去看报告"的拦截点。**判定不用"env 名字"**：部署环境的 `ZHIYIN_ENV` 就是 `local`（见 `deploy/compose.yaml`），按环境名放行等于没拦 |
| ② 如实上报 | `AssemblyReport` 新增 `placeholders` 字段与 `serves_fabricated_content`；`describe_assembly` 按 `IS_PLACEHOLDER` 标记收集；`/healthz` 在占位时返回 `degraded`；`--check` 的 JSON 里也能看到 | **不把它标 `skeleton`**：它是 LLMGateway 的完整实现（方法体齐全、过产出契约），骨架的含义是方法体未填——标 skeleton 是另一种不如实。三态（`missing` / `skeleton` / `placeholder`）是三件事，必须分开 |
| ③ 应答内提示 | `LoopResult` 新增 `model_degraded` 并在**成功路径**上赋值；编排器在它为真时把动态文案 `copies.json::notice.placeholder_output` 追加到应答 | 只在日志里记等于用户看不到；而"看起来正常的演示产出被当成诊断结论"正是这一步要堵的。文案走动态资源（§8），不硬编码 |

**拦截点选在 `assert_minimum_viable` 而不是 `build_container`**：宿主机 `python -m zhiyin_boot --check --phase=N` 是《AGENTS.md》§13 的文档化必跑命令，它只构建容器不做启动校验——放在 `build_container` 会连只读检查一起打断。实测本条改动后宿主机 `--check --phase=1/2` 仍 `passed=true`。

**改动面**：`settings.py`（新开关）、`local/llm.py`（`IS_PLACEHOLDER` + `IMPLEMENTATION_STATUS = "wired"` 如实声明 + 重写文档串）、`api/runtime.py`（`placeholders` + `serves_fabricated_content`）、`boot/report.py`（收集占位）、`api/app.py`（`/healthz` 降级）、`boot/container/__init__.py`（启动校验）、`business/ports/loop.py`（`model_degraded`）、`business/services/loop.py`（成功路径赋值）、`business/services/orchestrator.py`（应答提示 + `_placeholder_notice`）、`data/registry/copies.json`（新文案）、`deploy/.env.example`（开关说明），以及 **9 处测试夹具显式打开许可**。

**新增守卫**：`test_placeholder_llm_is_refused_without_explicit_consent`（默认拒绝 + 仍如实标 `wired` 但列入 `placeholders`）、`test_placeholder_llm_allowed_with_explicit_consent`、`test_success_path_carries_model_degraded`（成功路径必须带标记）、`test_placeholder_model_output_is_marked_in_the_reply`（应答里真的出现提示文案）。

**验证**：默认配置下启动校验报 `以下能力位装配了**占位实现**：['llm']…`；显式许可后放行且 `placeholders` 仍为 `['llm']`、`/healthz` 报 `degraded`；宿主机 `--check` 输出含 `"placeholders": ["llm"]` 且 phase 1/2 仍通过。部署环境走 `ZHIYIN_USE_PAMI_LLM=1`，不需要许可、也不会出现提示。

**门禁**：`pytest` **450 passed / 1 skipped**；Ruff 通过；OpenAPI 快照一致；registry `新增 1、一致 100、多出 0`。

### 17.4 D3 删除 `LoopCoordinator.advance`（原记的"留文档"太软）

**复核发现**：`advance` 的实现只做三件事——`sessions.update_stage` + 返回新上下文（外加一个与目标环节无关的 `_resolve_lead` 兜底）。而一次真正的阶段变更必须同时做五件：按 `policies/teaming.py` 定主理、落库、写会话记忆、发 `loop_stage_changed`、组告知文案。`DefaultOrchestrator._perform_handoff` 本来就在做这五件，**且它自己直接调 `sessions.update_stage`**——两处各写一遍同一件事。

**所以最优解是删除而不是留文档**：`advance` 是"落库但不发事件"的半职责入口，天然制造不一致；而未来若要由独立 Worker/调度器推进环节，正确入口是**冻结的 `Orchestrator.handoff`**（已是公开、语义完整的阶段变更 API）。全仓调用面只有两处测试。

**改动**：删除 `ports/loop.py` 的抽象方法与 `services/loop.py` 的实现，并在原处留下"为什么删、以后走哪"的注释；同时删掉只服务于它的 `_resolve_lead`（那个兜底按任务入口取默认主理，与目标环节无关，本身就不是正确口径）。`ports/loop.py` 里写明：**阶段变更的唯一入口是 `Orchestrator.handoff`**。

**测试改动（说明清楚，避免被误读成"删测试让门禁变绿"）**：

| 原用例 | 处置 | 理由 |
| --- | --- | --- |
| `test_advance_keeps_inherited_assets` | **改写**为 `test_inherited_assets_come_from_the_blackboard` | 它真正的断言是"前序资产自动继承发生在读黑板这一步"（该断言在 `advance` 之前就已成立），末尾两行才依赖 `advance`。改写后把断言移到仍然存在的两个入口 `start()` 与 `resume()` 上——**覆盖没有变少，反而补上了续接路径** |
| `test_advance_uses_explicit_lead_when_given` | **删除并替代** | 被测对象已按决策移除。它的规则（"交接主理由规则层决定，不由状态机猜"）在**正确层级**重新钉住：新增 `test_handoff_is_the_only_stage_transition_path`，断言 `handoff` 后主理为 `companion_coach`（来自 `policies/teaming.py`，不是调用方随手写的）、且真的落到会话上 |

**验证**：`grep '\.advance\(\|def advance\|_resolve_lead'` 在整个 `zhiyin-src` 里只剩 `tests/test_redis_first_phase.py` 的同名**时间推进**辅助函数（与本次无关）。`pytest` **450 passed / 1 skipped**（净变化 0：改写 1、删 1、增 1）；Ruff 通过；OpenAPI 一致。

### 17.5 D2 报告加「个人画像」——而且是**冻结快照**（原记的 B 不成立）

**先纠正原记**：D2 原建议 B（"把 DTO 描述改成不含画像"）**与设计文档冲突**——《前端页面设计》§4.4 第 243 行明确要求报告全文"含 15 维全景、方案、行动计划、**个人画像**"，且该文档自述是页面结构**唯一口径**；第 245 行还写明"报告全文页**读取资产版本**"。所以画像要有，而且**不能实时读当前画像**。

**定案**：`Report` 增加 `profile_snapshot: list[ProfileField]`——`ProfileField` 已是内核形状，**不需要新造**。②诊断生成报告时把**当次所用画像**冻结进去（构造点 `orchestrator.py` 里 `profile` 变量本来就在手边，正是本次诊断用的那一份）；`mappers.report_full_text_view` 在快照非空时追加 `profile` 章节；前端渲染为「本版本生成时的画像快照」并显示生成日期。旧报告该字段为空 → 不生成章节，**向后兼容、无需数据迁移**。

**顺序**：画像章节放在**最后**，与 §4.4 的枚举顺序（"15 维全景、方案、行动计划、个人画像"）一致；目录与正文由同一个 mapper 产出，天然同步。

**为什么"快照"是这件事的关键**：报告是版本化只读资产、画像是活状态。实时读会让用户在 v1 报告里看到今天的画像，与工作台对不上——这正是原记里识别出的风险，只是原记的结论（不做画像）选错了方向。

**改动面**：`zhiyin-kernel/zhiyin_kernel/assets.py`（加字段 + 文档串；顺手确认 `blackboard.py` 只依赖 `enums`，不成环）、`business/services/orchestrator.py`（填快照）、`api/dto/mappers.py`（追加章节）、前端 `ReportPage.vue`（`profileSnapshot` / `profileSnapshotAt` / 字段名译自 `copyBundle` / 值格式化沿用 `ProfileFields` 口径 / 加入 `RENDERED_IDS`）。

**新增守卫**：`test_report_carries_a_frozen_profile_snapshot` —— 生成报告后断言快照非空；**随后给画像新增一个字段，再读已存的那一版报告，快照必须不变**；并断言报告正文里有 `profile` 章节且字段来自快照。这条测试锁的正是"冻结"这个语义，而不只是"有个字段"。

**契约影响**：`Report` 加了字段（`extra="forbid"` 下属契约变更，但纯增量、有默认值）；`ReportFullTextView.sections` 是 `list[dict[str, Any]]`，所以 **OpenAPI 快照与前端生成类型都没有变化**（实测 `export_openapi.py --check` 一致）。

**门禁**：`pytest` **451 passed / 1 skipped**；Ruff 通过；OpenAPI 一致；前端 `typecheck` / `check:api` / `build` 通过；新 CSS 守卫无需新样式（复用既有 `.rp-block`/`.rp-item` 等类，故 0 新增）。

### 17.6 D9 能力池改由 bootstrap 下发（原记认为的两个形状难点，有一个不存在）

**复核**：原记把"`stages`（负责哪一环节）"列为首要难点，认为要给 `RegistryRepository` 新增 `list_output_contracts`。**实际不需要**——用**已有的**冻结方法 `get_output_contract(agent_id, stage)` 逐环节探测（5 智能体 × 5 环节）即可推出，实测结果与文档里记的映射完全一致：`profile_analyst→[collect]`、`career_advisor→[diagnose, decide]`、`path_planner→[act]`、`companion_coach→[review]`、`info_scout→[]`。理论包中文名也有现成的 `list_theory_cards`（16 张卡）。所以不必先做 B（守卫比对），**直接做 A**。

**中途的一处分层纠正（值得记下）**：第一版我把"逐环节探测"写进了 `Facade.bootstrap`，运行即报 `DefaultRegistryService` 没有 `get_output_contract`——**这个错报得对**：反推"谁负责哪个环节"是业务读侧的翻译工作，不该落在 BFF；而且散到 BFF 就会出现第二份口径。于是收敛成**一个**聚合方法：`RegistryService.list_agent_capabilities()`（也顺带避免了给冻结 Port 加三个零散方法）。新增内核形状 `AgentCapability{agent, stages, theories}` 承载它。

**最终形状**：

| 层 | 内容 |
| --- | --- |
| 内核 | `AgentCapability`（跨层共享形状，放 `zhiyin_kernel/registry.py`） |
| 业务 Port | `RegistryService.list_agent_capabilities()` **一个**方法，注释写明"为什么不是 list_agents + 让调用方自己拼" |
| 业务实现 | `DefaultRegistryService`：`list_agents()` + 并发探测产出契约 + `list_theory_cards(theory_ids)` 翻中文名 |
| 公开 DTO | `BootstrapView.agents: list[AgentView]`；`AgentView{id,name,role_summary,stages,theories[{id,name}],tools,not_to_do}`；`no`/`shortName`/`theme` **不进契约**，由前端按下标与名称派生 |
| 前端 | `stores/agents.ts` 删掉整份硬编码目录，改为 `useAgentCatalog()` / `useAgentById()` 从 `session.agents` 派生；6 个消费组件（AgentTeamGrid / AgentsShowcase / WorkspacePage / AgentContextRail / AgentScopePanel / AgentDetailPage）同步改写；环节中文名取 `copyBundle::stage.<code>.label`，不主理任何环节时取新增文案 `agent.stage.none` |

**这次动了生成物**：`contracts/openapi.json` 与 `zhiyin-web/src/api/types.ts` 按规程重新生成（`python scripts/export_openapi.py` + `npm run gen:api`），`src/api/schema.ts` 补 `AgentView` / `AgentTheoryView` 两个取值别名。另有**一条既有守卫要求同步登记**：`test_every_bootstrap_field_has_a_business_source` 会检查 `BootstrapView` 每个字段都有业务出口，新增字段时它直接判红——这正是它存在的意义，已在 `BOOTSTRAP_FIELD_SOURCES` 里把 `agents` 指向 `list_agent_capabilities`。

**新增守卫 2 条**：
- 后端 `test_agent_capability_pool_is_resolved_server_side`：锁 5 个智能体的 id 顺序、`career_advisor` 同时管②③、`info_scout` 如实为空、理论名已翻中文（`name != id`）；
- 前端 `test_agent_capability_pool_is_not_hardcoded_in_the_frontend`：扫 `stores/agents.ts`，一旦出现任何智能体 id 或展示名就判红——**包括"先加硬编码兜底、以后再接线"这种半接头写法**。

**验证**：真实调用 `facade.bootstrap()` 得到 5 条能力池，环节与理论名均正确（如职业顾问 `['diagnose','decide']`、理论前三项为"帕森斯 · 匹配 / 发展状态（CD）/ 三叶草"）。registry `新增 1、一致 101、多出 0`；`pytest` **453 passed / 1 skipped**；Ruff 通过；OpenAPI 一致；前端 `typecheck` / `check:api` / `build` 通过。

### 17.7 D6 前端依赖升级：告警归零（分两步，各自独立提交）

**审计结论（`npm audit --json` 实测）**：4 个告警包（vite 5.4.21 / esbuild 0.21.5 / undici 5.29.0 / openapi-typescript 6.7.6）**全部是 dev-only**——`src/` 里没有任何一处 import 它们，`dist/` 产物也不含 undici/esbuild 痕迹，`package-lock` 全部标 `dev: true`。`fixAvailable.isSemVerMajor` **全为 true**，即**没有任何非破坏性修复**；三者都是各自 major 的最后一版，没有 backport。所以"要不要升"不是安全取舍，而是"要不要承担大版本破坏性变更"。

| 步 | 改动 | 清掉的告警 | 破坏性风险 |
| --- | --- | --- | --- |
| 1 | `openapi-typescript` 6.7.6 → **7.13.0** | undici 的 1 高危 + 1 中危（v7 改用原生 `fetch`，undici 直接从依赖树消失） | 低：只影响 `npm run gen:api` 的输出格式 |
| 2 | `vite` 5.4.21 → **7.3.6** + `@vitejs/plugin-vue` 5.0.0 → **6.0.9** | vite 高危（`server.fs.deny` 绕过 / `.map` 遍历 / launch-editor UNC-NTLM）+ 传递依赖 esbuild 中危 | 中高：`vite.config.ts` 用的是 `plugins` / `resolve.alias` / `server.proxy`，v7 均保留，无需改配置；plugin-vue 6.0.9 的 peer 覆盖 vite 5–8 |

**结果**：`npm audit` 从 `2 moderate + 2 high` 变为 **`found 0 vulnerabilities`**。

**两处必须说明的事**：
1. **生成物被重新生成**：`openapi-typescript` v7 重新排版了生成的类型，`src/api/types.ts` 有约 2400 行格式变化（内容等价）——按规程由工具生成，未手改；`typecheck` / `build` 均通过。`package.json` 除版本外还有 npm 自身的**字母序重排**，无功能影响。
2. **本地 dev server 需要重启**：`127.0.0.1:5173` 上正在运行的进程是升级**之前**启动的，内存里仍是 vite 5（`node_modules` 已被替换成 v7）。本轮所有门禁结论来自 `npm run build`（新工具链）；dev server 要真正跑在 v7 上必须重启。**未擅自重启用户的常驻进程**，留待其决定。

**未做**：`npm audit fix`（实测无效）与 `npm audit fix --force`（会把 plugin-vue 的 peer 打坏）。

**门禁**：前端 `typecheck` / `check:api` / `build` 通过；后端 `pytest` 453 passed / 1 skipped；Ruff 通过；OpenAPI 一致。

### 17.8 本轮收口小结

**按 §五.1 的顺序完成 7 个工作流、9 个提交**（`f5cd892` → `9e6c558`），逐条独立提交、各自跑门禁：

| 顺序 | 条目 | 提交 | 一句话结果 |
| --- | --- | --- | --- |
| 1 | D4 + D5 | `f5cd892` | 加权→等权措辞改正；「开始诊断」等表达零代码接通② |
| 2 | D8 | `16af131` | 演示能力位清除 + `available_blocks` 改按开关派生 |
| 3 | D1（P0） | `f04a6a1` | 占位模型装配期硬失败 + 如实上报 + 应答内提示 |
| 4 | D3 | `b256f76` | 删除半职责的 `advance`，阶段变更只走 `handoff` |
| 5 | D2 | `f4959d5` | 报告加个人画像，且是**冻结快照** |
| 6 | D9 | `bcd0b1a` | 能力池改由 bootstrap 下发，前端硬编码清零 |
| 7 | D6 | `3d3d28a` + `9e6c558` | 依赖告警归零（分两步） |

**未做**：D7（PAMI RAG 内容与平台排期，非代码）。**D7 的状态变化**：15 维改证据等级后，JOB-MARKET 五维实测普遍 `missing`，**证据缺口从"看不见"变成"用户能看见"**——这正是 `FR-DIAG-006` 要的行为，也让 D7 的优先级有了可见依据。

**本轮新增加固（4 类守卫）**：① `test_no_orphan_css_classes_in_components`（死 CSS，选择器级 + scoped 作用域口径）；② `test_dimension_tag_is_a_closed_enum_not_free_text`、`test_success_path_carries_model_degraded`、`test_placeholder_*`（契约与占位模型）；③ `test_available_blocks_is_derived_from_feature_flags`、`test_agent_capability_pool_is_resolved_server_side`、`test_agent_capability_pool_is_not_hardcoded_in_the_frontend`（D8/D9 的"单一事实来源"）；④ `test_report_carries_a_frozen_profile_snapshot`、`test_handoff_is_the_only_stage_transition_path`、`test_explicit_advance_request_reaches_diagnose`（本轮定下的语义）。

**这轮反复出现的一个模式（值得记住）**：多次是**"我的验证手段本身有问题"**在骗我——旧死 CSS 判据把 HTML 标签名当类名（漏掉 43 条）、`sync_registry` 读不到 JSON 时静默返回空集（谎报 101 条漂移）、`--check` 换个环境就换结论、`edit` 脚本的 ASCII 引号写坏 JSON。**结论：凡是"全量异常"或"全绿"的结论，先怀疑读取路径和口径，而不是先相信它。**

### 17.9 收尾实测（重建镜像后，在真实运行环境逐项复验）

重建 `zhiyin-api` 镜像并重启后，对 5 条改动做了端到端复验。**全部通过**：

| 条目 | 实测结果 |
| --- | --- |
| **D1** | `/healthz`：`healthy=true`、`missing=[]`、`skeletons=[]`、**`placeholders=[]`**、`gateways.llm=wired` —— 部署走真实 PAMI 模型，没有占位实现，所以不降级、应答里也不出现占位提示（实测 `"本地演示模型" in 应答 = False`）。这正是期望：占位机制只在漏配环境或本地开发时介入 |
| **D8** | `feature_flags` 已无 `demo`；`banners` 只剩 `phase1_open`；`trust_blocks` 只剩 `primary_methodology`（`expandable_ref` 为空）；**`blocks.available == 开关里 enabled 的集合`**，逐项相等：`['achievements','calendar','report_full_text']`——既没有 `export`/`mentor`（`enabled:false`），也没有 `demo` |
| **D9** | `bootstrap.agents` = **5 条**能力池，环节与理论名均正确：`profile_analyst→['collect']`（5 个理论）、`career_advisor→['diagnose','decide']`（6 个）、`path_planner→['act']`（3 个）、`companion_coach→['review']`（3 个）、`info_scout→[]`（0 个，按需调用）；`tools` / `not_to_do` 齐备 |
| **D5** | 会话落在 `collect` → 发「我的档案够了，我想开始诊断。」→ stage 变为 **`diagnose`** |
| **D2** | 报告正文 sections 末位出现 **`profile`**：`snapshot_of = 2026-09-19T11:26:46Z`，`fields = [career_interest, ability_strength, value_anchor, target_direction, decision_window, real_constraint]` —— 正是生成那一版时冻结的画像 |

**同时确认 `.dockerignore` 修复已落地**：`/app/scripts/` 下现在有 `sync_registry.py`，并且**从镜像内的正规路径**执行 `python scripts/sync_registry.py --check` 得到 `新增 0，更新 0，一致 102，库中多出 0`（零漂移）。也就是说第七节第 7 条那条"改 registry 后必须执行"的规程，现在在部署环境里**真的可执行**了（此前脚本不在镜像里，写了也做不到）。

**遗留（不由本目标处理）**：D7（PAMI RAG 内容与平台排期）。另外**本地 dev server（5173）需要重启**才会跑在 vite 7 上——本轮门禁结论来自 `npm run build`。

---

## 十八、2026-09-19 D7 拆解与平台侧实测（把"做不到"变成"知道卡在哪"）

### 18.1 先修正我自己的分类错误

上一轮我把 D7 整条归为"非代码、内容与平台排期"就跳过了。**这个分类太粗**：D7 是 8 个子项，其中多数是代码或平台工作，而且平台开通我自己就有权限（本轮的 Agent 应用就是我建的）。逐项核过之后的实际归属：

| 子项 | 现状 | 归属 |
| --- | --- | --- |
| 平台发布 RAG 应用 + 建知识库 + 导内容 | 本轮已建库并导入 22 份文档（见 18.2） | **我能做** |
| 理论卡补齐：方法步骤/边界/示例/来源 | 16 张卡只有 `name/school/summary/product_usage` | 我起草 → **需你定稿** |
| 职业/专业/行业/政策来源台账 | 已有 4 条 `sources.json` | 我扩写 → 你审 |
| **招聘来源：条款/robots/字段/频率/保存期审核** | 未做 | **需你或合规**，不能自批 |
| JD 过期与下架机制 | 未见实现 | 我能做（**本轮未做，见 18.4**） |
| 五环节真实检索场景 | 依赖 RAG 可用 | 受 18.3 阻塞 |
| ES/Embedding/pgvector/Rerank 故障注入 | 未见专门测试 | 我能做（**本轮未做**） |
| 引用追溯到来源+版本 | 未核 | 我能做（**本轮未做**） |
| 评测门槛冻结 | §12.3 已有建议值；未跑基准 | 我跑基准 → 你冻结（**本轮未做**） |

### 18.2 平台侧：知识库建成了，22 份文档导入成功，但**解析全部失败**

平台源码就在仓库里（`platform/wanwu`，完整 Go 工程），所以我**从路由注册与请求模型直接读出了接口形状**，而不是猜：

| 步骤 | 端点（`/user/api/v1/`） | 结果 |
| --- | --- | --- |
| 建知识库 | `POST /knowledge` | ✅ `knowledgeId=2101248408802037760`，绑定 embedding 模型 id=2 |
| 分片上传 | `POST /file/upload`（multipart） | ✅ 字段名必须是 **`files`**（见 `service/file_upload.go:189`），不是 `file` |
| 合并 | `POST /file/merge` | ✅ 返回 minio `filePath` |
| 导入 | `POST /knowledge/doc/import` | ✅ `docType` 必须带点且在白名单内：`.docx;.txt;.pdf;.xlsx;.csv;.pptx;.html;.doc;.xls`（`.md` **不支持**） |
| 导内容 | 同上 ×22（16 张理论卡 + 3 条理论 + 3 条职业） | ✅ **22/22 导入成功** |

**但 21 份文档解析失败**（`know_doc_parsing_service_error`，DB status=55）。解析由平台自己的 `rag-wanwu` 服务经 Kafka `doc-rag` 主题完成，其日志给出根因：

```
Error getting embeddings: ModelConfigure(model_type=TEXT_EMBEDDING, model_id=2,
  model_name=qwen3.7-text-embedding-flash, provider=OpenAI-API-compatible,
  model_args={'apiKey': 'useless-api-key',
              'endpointUrl': 'http://172.17.0.1:6668/callback/v1/model/2'})
```

`172.17.0.1` 是 **Docker 宿主网关**，而 RAG 容器在 compose 网络里。实测（同网络容器内）：

| 地址 | 结果 |
| --- | --- |
| `http://172.17.0.1:6668/callback/v1/model/2` | **Connection refused** |
| `http://bff-service:6668/callback/v1/model/2` | **200** |

来源是平台的配置 `platform/wanwu/configs/microservice/bff-service/configs/config.yaml:8` → `callback_url: http://172.17.0.1:6668`。

### 18.3 为什么我没有自己去改它

改这一行（`callback_url` 指向 compose 服务名）就能让整条 RAG 链路通。**但平台是 vendored 的第三方工程**（`platform/wanwu` 带 `UPSTREAM.md` 与 `.zhiyin-vendor.json`），《AGENTS.md》§10 明确"职引侧不得自行修改外部平台契约"；而且 `agentscope` 的配置里 `172.17.0.1` 还用于别处，改动的波及面要平台侧确认。

所以这里**不是"我做不到"，而是"不该由我单方面改"**——需要你或平台负责人拍板。同理，我**没有创建 RAG 应用**：知识库解析全失败，此时发布一个 RAG 应用只会制造一个"看起来配好了、一问全是空"的陷阱（谁把 `ZHIYIN_USE_PAMI_SEARCH` 打开都会踩）。

### 18.4 本轮未完成的部分（不掩饰）

拆解出来的 4 项代码/测试工作**本轮没做**：JD 过期与下架机制、ES/Embedding/pgvector/Rerank 故障注入、引用溯源核对、基准评测报告。原因是我把这一轮的时间用在了平台侧逆向与实测上（读 Go 源码定位接口、追 Kafka 消费日志到 embedding 回调），且这部分的结论依赖平台侧先放开。**这 4 项与平台无关、随时可做**，我会接着完成。

### 18.5 需要你确认的两件事（明确交接）

1. **理论卡内容定稿**：16 张卡需要补「方法步骤 / 适用边界 / 示例 / 来源」。这属职业咨询领域内容，我可以按现有 `name/school/summary/product_usage` 起草，但**必须由你或领域负责人定稿**才能进知识库（现在的 22 份文档里，理论卡那 16 份就是只有摘要级内容）。
2. **招聘来源合规审核**：`sources.json` 里 4 条来源台账已含 `allowed` / `access_basis` / `retention` / `removal_method` 字段，但**条款、robots、字段范围、抓取频率、保存期限这五项审核未做**，按第三期清单属于放行前置。我不碰招聘来源抓取。
3. 附带一个**平台侧待修项**：上面 18.2 的 `callback_url` 配置。

---

## 十九、2026-09-19 D7 代码侧收口：④JD 过期下架、⑥溯源到版本、⑦检索基准

### 19.1 ④ JD 过期与下架机制

设计口径（第三期设计 §4.3）：已过截止时间、来源下架、连续复核失败的记录必须标记 `expired`，不得作为"现在可以申请"的证据；历史 JD 可保留用于要求分析，但要明确标记为历史样本。

**核心判断**：`_visible()` 早已让过期文档**查不出来**，但记录本身仍是 `enabled`——于是"过期内容不能进入生产索引"**在索引侧不成立**（向量库仍留着它），报表与审计也看不出它已过期，只剩"查不到"这一个现象。所以要**落状态**，不能只靠查询过滤。

| 触发 | 实现 |
| --- | --- |
| 已过截止时间 | `RetrievalDocumentStore.expire_due()`：`status=enabled` 且 `expire_at <= now` → `expired`，并写 `expired_reason` / `expired_at` / **`historical_sample=True`** |
| 来源下架 | `RetrievalDocumentStore.expire_by_source(source_id)`：供采集/治理流程调用（来源台账的 `removal_method` 就是按 `source_id` 下架） |
| **连续复核失败（未做）** | 依赖"复核"流程本身，而招聘来源的条款/robots/频率/保存期审核尚未完成。在复核机制落地前实现只能靠猜，故留空并在模块 docstring 写明**不做看起来有、实际没有的东西** |

**落位**：`infrastructure/workers/document_expiry.py`。它是纯数据治理管道（扫表落状态、无业务判断、无阈值、不发通知），按《AGENTS.md》§3 属 `infrastructure/workers`（与 `vector_sync` 同类），因此**不需要新增跨层 Port**。

**新增能力位必须整体同步**（§5/§11）——这是本轮真正花时间的地方：`WORKER_PORTS`、`build_workers` 装配（与 `vector_sync` 分开装配：过期治理不该因为向量链路没开而停摆）、`assembly_gates.json` phase 3/4 的 `require.workers`、`ownership.json` 归属、以及三处守卫登记（`WORKER_SHELL` 落位表、`SKELETON_PORTS` 基类、**`PORT_NOT_A_CONTAINER_FIELD`**——worker 按自身 `name` 寻址、不是 container 字段）。另有一条文档守卫顺带抓到了《开发指南》里"3 Worker"的硬编码计数，已改为 4。

### 19.2 ⑥ 检索引用溯源：补上**版本**

**已就绪的部分**（复核确认，无需改动）：`RetrievalEvidence` 自带 `source_id` / `source_url` / `version`；`_report_sources()` **已经做引用核对**——模型自述的来源必须出现在证据包里才允许进入资产。

**真正的缺口**：`Report.sources` 只是一串来源字符串，而权威文档的唯一键是 `(namespace, source_id, version)`——**没有版本就指不到具体那一版**，"可追溯"只做到一半。故新增 `Report.source_versions: dict[str, int]`（纯增量、有默认值、向后兼容），由 `_report_source_versions()` 从证据里取：

- 键的取法与 `_report_sources` **完全一致**（`source_url or source_id or evidence_id`）——否则两个字段对不上，反而更难查；
- 同一来源出现在多版证据里时取**最大版本**（报告依据的是它当时看到的最新一版）；
- 测试直接锁这条不变式：`set(Report.sources) ⊆ set(Report.source_versions)`。

### 19.3 ⑦ 检索质量基准：跑起来了，而且知道为什么这个数字不能用

`evaluate_retrieval()`（Recall@K / MRR）此前**在仓库里没有任何调用方**——指标算得出来、没人跑，于是"达到质量门槛"这条一直悬着（又一个"看起来有、实际没有"）。本轮接上可执行入口 `scripts/eval_retrieval.py` 并跑出基准，报告见《[职引-检索质量基准](../04-实施与验收/职引-检索质量基准-2026-09-19.md)》。

**实测（100 例，top_k=10，本地关键词通道）**：`Recall@K = 0.0000`、`MRR = 0.0000`、**期望 id 覆盖率 = 0/100**。

**这个 0 不是质量结论**，而是两个各自独立的原因：

1. 评测集的 `expected_ids` 指向一份**"测试知识快照"，而该快照从未落地**（`data/evaluation/` 下只有评测集与 schema，全仓搜不到第二个引用）；
2. **id 口径不一致**——发现的新缺陷：评测集期望一律写 `namespace:id`，而 `LocalSearchGateway` 返回**裸 id**。影响不止评测：同一篇文档在关键词通道与向量通道里 `evidence_id` 不同，**RRF 融合的去重键就是它**，于是同一篇文章会被当成两篇、重复证据挤占 `top_k`。已登记为[待决问题 D11](../04-实施与验收/职引-待决问题与改法选项-v1.0.md)（改它会同时动引用核对与跨通道融合，属需评审的检索契约调整）。

脚本还带一条**状态记录型测试**：一旦内容/快照就位、或 id 口径被统一，用例立刻变红，提醒出新的基准并冻结门槛——避免这件事再次悬空。

---

## 二十、2026-09-19 D7 ⑤ 故障注入：先补单测，再到真实基础设施里真的把通道打挂

### 20.1 单测：六个故障面各自有断言（`tests/test_m3_data_infrastructure.py`）

| 用例 | 锁住的行为 |
| --- | --- |
| 关键词通道故障 | 降级到向量通道，`degraded_channels == ["keyword"]` |
| Embedding 故障 | 降级到关键词通道，`degraded_channels == ["vector"]` |
| pgvector 故障 | 降级到关键词通道，`degraded_channels == ["vector"]` |
| 两条通道都故障 | **显式 `RuntimeError("请求的检索通道均不可用")`**，不得返回空列表冒充"没有相关内容" |
| 无命中 | 正常返回空列表，**不标记降级**（"没内容"与"故障"是两件事） |
| Rerank 未接入 | 检索照常可用，元数据如实只声明 `algorithm="rrf"` |
| 降级原因可分辨 | 维度不匹配 → `ValidationError`；连不上 → `UnavailableError` |
| 降级原因不含秘密 | 异常消息里的 DSN/口令不得进入检索元数据 |

### 20.2 顺手修掉一个真缺陷：降级"不可分辨"

原来两条通道的故障处理都是 `except Exception: degraded.append("<通道>")`，**异常被整个吞掉**。于是"向量维度配错"（改配置就好）与"向量库宕机"（要重启依赖）在结果里长得一模一样。现新增 `metadata["retrieval"]["degraded_reasons"]`：

- **只写异常类名，不写异常消息**——消息可能带 DSN、口令、上游地址，而检索 metadata 会随报告与接口响应外流（《AGENTS.md》§10）。类名已足够区分两类故障，细节按类名去查日志；
- 落点是 `RetrievalEvidence.metadata`（自由字典），**不动冻结的 `EvidencePacket`**（它的 `degraded_channels` 是"按 namespace 的失败清单"，与本字段不是一回事）。

### 20.3 容器内真实故障注入：`scripts/fault_injection_probe.py`（可复跑）

单测用假件抛异常，只能证明"分支写对了"，证明不了**真实驱动/网络故障下的行为**，也证明不了"故障真的被注入了"。故新增探针，在容器内用容器自己的装配制造真故障：

```
docker cp scripts/fault_injection_probe.py <container>:/tmp/probe.py
docker exec -w /app <container> python /tmp/probe.py
```

实测（`zhiyin-api` 容器，2026-09-19）：

| 步骤 | 结果 |
| --- | --- |
| 真实装配 | `search=RrfHybridSearchGateway`、`embedding=PamiEmbedGateway`、`vector=PgVectorGateway`、无 `rerank` 能力位 |
| ① 健康度体检 | 关键词通道可用（3 条）；真实 pgvector 可用（`theory` 命名空间 **0 条**） |
| ② 关键词真故障（`127.0.0.1:1`，真实连接被拒） | 不抛异常、审计 `degraded=True`；故障确认 `UnavailableError` |
| ③ 向量真故障（DSN host 指向不存在域名，真实 DNS 失败） | 不抛异常、返回 3 条关键词命中、审计 `degraded=True`；故障确认 `UnavailableError` |
| ④ 两条都真故障 | 如期 `RuntimeError: 请求的检索通道均不可用` |
| ⑤ 无命中 | 返回 0 条，审计 `degraded=False`（没有把"没内容"误报成"故障"） |
| ⑥ Rerank 未接入 | 照常返回 3 条，算法声明 `rrf` |
| 结论 | **全部通过** |

### 20.4 探针自己骗了两次——所以它现在必须先自证

这一段值得留下，因为它和 §16.6"工具骗人"是同一类错误：

1. **维度不匹配被当成"向量库宕机"**：第一版探针用 8 维向量去查要求 1024 维的 `PgVectorGateway`，而该网关在**连接之前**就抛 `ValidationError`。于是同时产生两个错误结论：把"健康但维度不匹配"判成"向量库坏了"，又让"死主机"用例根本没走到网络——**一个假故障掩盖了另一个假故障**。修正后探针先做健康度体检（§20.3 的①），再做注入。
2. **故障确认语句自己签名错了**：给 `PgVectorGateway.search(namespace, vector)` 传了一个 `RetrievalQuery`，拿到 `TypeError`，打印出来却像"故障已确认"。现在确认语句必须写明**预期异常类名**，不符也算失败。

这两次都是"**全绿/全红的结果要先怀疑工具**"的又一例，所以探针把这两步固化成了内建流程，而不是靠我每次记得。

### 20.5 存量普查：全系统的检索内容 = 6 张演示卡（新问题 → D12）

故障注入的副产品是"到底有多少内容"这个更根本的问题。容器内只读普查：

| 存储 | 结果 |
| --- | --- |
| 向量库 `zhiyin_vectors` | **0 行**（任何向量检索都不可能命中） |
| 权威文档表 `retrieval_document` | **0 行** |
| 检索审计表 `retrieval_log` | 30 行（`jd` 7 / `memory` 1 / `occupation` 5 / `report` 7 / `resume` 7 / `theory` 3）——检索确实发生过，只是没有内容 |
| 本地知识库 `data/knowledge` | `occupation` 3 条 + `theory` 3 条，**全部标注 DEMO**；`jd`/`memory`/`report`/`resume` 为空文件；`sources.json` 4 条来源里 3 条名字带"（演示数据）" |

这既印证了 §19.3 基准报告的两个根因，也暴露出一个**新问题**：容器里真实的关键词通道是 `LocalSearchGateway`，它提供的正是这批 DEMO 内容，而 `IMPLEMENTATION_STATUS="wired"` 让它**照常满足 phase 3 门禁**——`/healthz` 显示 `status=ok, placeholders=[]`，并不是它在说谎，而是系统根本不知道这批内容是演示的。

这是"**业务演示表已清空，但检索内容仍是演示卡**"的正面答案，已登记为[待决问题 **D12**](../04-实施与验收/职引-待决问题与改法选项-v1.0.md)（含 A/B/C 三条改法与代价，**需你拍板**：它会影响能否启动、报告可信度与门禁结论）。

### 20.6 验证

`python -m pytest -q` → **470 passed, 1 skipped**；`ruff` 全绿；OpenAPI 一致；容器内 `phase 1/2/3` 均 `passed=true`（`document_expiry` 已是相位 3/4 的必需 worker，重建镜像后复验通过）。

---

## 二十一、2026-09-19 平台侧解阻（经用户授权）：解析从 0/22 变成 22/22；检索仍卡在 **rerank 模型缺失**

> 本节与 §18.2/§18.3 直接衔接：当时把根因定位到平台的 `callback_url` 并**停手等平台负责人**。
> 本轮用户明确授权后，我把这一行改了并跑通了整条链路。**改动的仍然只有那一行配置**，
> 没有改平台任何代码或契约。

### 21.1 改的是哪一行、为什么这么改

`platform/wanwu/configs/microservice/bff-service/configs/config.yaml`：

```diff
-  callback_url: http://172.17.0.1:6668
+  callback_url: http://bff-service:6668
```

判断依据（三条独立证据，不是"试出来的"）：

1. **同一个文件里其它服务引用全都用 compose 服务名**（`iam-service:8888`、`rag-service:9640`、`workflow: agentscope-wanwu`…），只有这一行写宿主网关 `172.17.0.1`；
2. **`assistant-service` 的同类配置本来就是这个值**（`configs/microservice/assistant-service/configs/config.yaml:4` 已是 `http://bff-service:6668`）——说明原值是配置不一致，不是有意为之；
3. **代码路径**：`cmd/bff-service/main.go:87` 把该值交给 `mp.Init()`，`pkg/model-provider/model_params_api.go:20` 用它拼出回调地址 `/callback/v1/model/{id}`；`internal/bff-service/server/http/handler/callback/callback.go` 再把模型配置里的 `endpointUrl` 换成这个地址返回给调用方。**RAG 容器在 compose 网络内**，所以这个地址必须能从容器里解析——实测宿主网关地址 `Connection refused`，服务名 `200`。

**落地方式（重要，因为镜像里烤着配置）**：`Dockerfile.bff` 把 `configs/microservice/bff-service` `COPY` 进镜像，而 compose **没有挂载** configs。所以：

- **源码已改**（仓库里这一行现在是新值）→ 下次成功构建镜像即永久生效；
- **运行中的容器用 `docker cp` 打了同样的补丁并重启**（`docker compose ... up -d --build bff-service` 因**拉不到 `golang` 基础镜像**失败：本机到 Docker Hub 超时，属环境网络问题，非配置问题）；
- ⚠️ **因此当前容器是"补丁态"，不是"重建态"**：如果以后有人 `--force-recreate` 该容器而镜像未重建，这一行会退回旧值、解析会再次全失败。重建镜像前请确认网络可达。

### 21.2 结果：知识库解析 **0/22 → 22/22**

先删掉 21 份失败行 + 1 份旧 `.md` 行（平台状态 55 = RAG 侧解析异常，属终态，允许删除），再按 `.txt` 重新导入同样的 22 份**自有内容**（16 张 registry 理论卡 + 3 条 theory 切片 + 3 条 occupation 切片）：

| 阶段 | 结果 |
| --- | --- |
| 修复前 | 23 份文档，**全部** `status=5 / 文件解析服务异常` |
| 重导后第一轮 | **19 份解析成功**，3 份报 `已存在同名文件`（删除时它们正处"解析中"，平台拒绝删除 → 残留旧行撞名） |
| 清掉残留 + 补导 3 份 | **22/22 全部 `status=1`** |

这同时证明了三件此前"看起来有、其实没通"的事：**Kafka `doc-rag` 消费正常、链路 embedding 正常、ES 索引正常**。

### 21.3 已发布的 RAG 应用与 Key

| 步骤 | 端点（`/user/api/v1/`） | 结果 |
| --- | --- | --- |
| 建应用 | `POST /appspace/rag` | ✅ `ragId=2101255766085210112`（body 是 `AppBriefConfig`） |
| 写配置 | `PUT /appspace/rag/config` | ✅ 绑定知识库 + `qwen-plus`(modelId=3)。**踩坑**：平台**0 个 rerank 模型**，但 `RagConfig.Check()` 仍校验 provider，空串会命中 `ToModelParams` 的 default 分支并报「invalid provider: rerank」（错误消息里打印的其实是 modelType，容易误判）；给合法 provider + 空 config 才通过 |
| 发布 | `POST /appspace/app/publish` | ✅ `appType=rag, publishType=private` |
| 取 Key | `POST /appspace/app/key` | ✅ 已写入 **gitignored** 的 `deploy/.env`（`ZHIYIN_PAMI_RAG_API_KEY`，只记录长度，不进仓库/日志/聊天） |

`ZHIYIN_USE_PAMI_SEARCH` **保持 0**（按约定没有翻）。

### 21.4 新发现的硬阻塞：平台没有 rerank 模型，RAG 问答用不了

用 Key 调 OpenAPI `/service/api/openapi/v1/rag/chat` 的真实结果：

```
HTTP 200  code=1  message=rerank_model_id 不能为空，且 custom_model_info['llm_model_id'] 不能为空
HTTP 400（直接打 rag-wanwu 检索端点时同理）
```

逐层证实（不是猜）：

| 探针 | 结果 |
| --- | --- |
| 平台模型清单 | rerank **0 个**（llm 1 个、embedding 2 个） |
| `rag-wanwu:10891` 的 OpenAPI | **只有一个端点** `/rag/knowledge/stream/search`，**没有 rerank 端点**可指 |
| 直接打检索端点，`rerank_model_id=""` | `code=1`，同上（校验强制非空） |
| 直接打检索端点，`rerank_model_id="none"` | **HTTP 500**（非空但要能查到该模型） |
| 上游模型网关 | 是 **DashScope**；其 **OpenAI 兼容模式没有 `/rerank`（404）**，只有原生 rerank API（实测 HTTP 200，`gte-rerank-v2` 可用） |

平台模型供应商（`OpenAI-API-compatible` / `YuanJing`）都是 `POST <endpointUrl>/rerank` 的形状，因此**光加配置接不上** DashScope 的原生 rerank。结论：要让平台 RAG 问答可用，需要 **平台侧加一个 rerank 供应商适配**（或提供一个说 OpenAI rerank 协议的服务），这已超出"改一行配置"的授权范围，**我没有伪造一个假 rerank 配置来凑通**（那正是 §18.3 说的"看起来配好了、一问全空"的陷阱）。

**顺带更新一处旧结论**：§18.3 写的"改这一行就能让整条 RAG 链路通"**不完全准确**——它能让**解析**通（已验证 22/22），但**问答/检索**还差 rerank 这一环。

### 21.5 本轮平台侧遗留物（供你决定去留）

| 对象 | 数量 | 说明 |
| --- | --- | --- |
| 知识库 | 1 个（`knowledgeId=2101248408802037760`） | 22 份自有内容全部解析成功，**可复用** |
| 知识库文档行 | 22 行 | 全部 `status=1` |
| RAG 应用 | 1 个（`ragId=2101255766085210112`） | 已发布、已绑知识库、已发 Key；**在 rerank 到位前查不出内容** |
| 平台配置补丁 | 1 行 | 源码已改 + 运行容器已 `docker cp`；待镜像重建固化 |

### 21.6 验证

改配置 + 重导后：`pytest` 472 passed / 1 skipped、`ruff` 全绿、OpenAPI 一致、`phase 1/2/3` 均 `passed=true`（容器内复验）。平台侧实测命令与输出见本节各表。












