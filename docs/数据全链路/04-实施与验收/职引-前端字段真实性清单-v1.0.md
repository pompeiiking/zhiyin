# 职引 · 前端字段真实性清单（真实 / 接口 Mock / 前端演示）v1.0

| 项目 | 内容 |
| --- | --- |
| 文档用途 | 逐字段区分「真实接口数据 / 接口真实但内容为 Mock / 前端硬编码演示 / 前端兜底」，避免把「页面能渲染 + 接口 200」当成业务事实 |
| 产生方式 | 对 `test-yuan@c639b99` 工作区的独立复核（2026-09-19）：读 `zhiyin-web/src` 源码 + 对运行中后端发起只读 GET |
| 复核范围 | 前端全部页面、store 与公共组件；`GET /app/bootstrap`、`/app/sessions`、`/app/workspace`、`/app/report/full-text` |
| 与既有文档的关系 | 承接[第一期未闭合项与 Mock 标注清单](职引-第一期未闭合项与Mock标注清单.md) 的 MOCK-1、MOCK-2；本文补齐「逐字段」粒度，并记录本轮复核的新发现 |
| 交接来源 | [数据全链路智能体交接文档](../00-索引与变更/职引-数据全链路智能体交接文档-2026-09-19.md) 第六节第 7 条、第十节第 1 条 |
| 复核时运行状态 | 前端 `127.0.0.1:5173`、后端 `127.0.0.1:8000/healthz`、PAMI Web `127.0.0.1:8081` 均为 200 |
| 维护约定 | 某字段改接真实来源后，更新本文对应行；本文只记录事实与证据，不改写历史结论 |
| 最近就地更新 | 2026-09-20：按 §十四 第 1 条，就地对 §三 / §四 / §五 / §六 的失真行做了复核与改写（逐处记录见 [§十六](#十六更新记录2026-09-20-前端真实业务链路修复后就地复核)）。<br>2026-09-20（同日，第二轮）：新增 `GET /app/conversation/history` 与消息落库后，§16.3 中「第一期没有读历史会话接口」的前提**已失效**，相关行已就地改写（逐处记录见 §十七）。**§七（工作台）、§八（报告页）其余行、§十（汇总）、§十二（处理建议）仍未逐行复核，仍为 `c639b99` 快照，不要当作当前事实。** |

---

## 一、四类口径

| 判定 | 含义 | 能否当业务事实用 |
| --- | --- | --- |
| **真实** | 值由后端接口下发，且内容是真实业务数据 | 可以 |
| **接口真实 / 内容 Mock** | HTTP、MySQL/Redis/MinIO 链路真实，但内容由 `LocalModelGateway` 占位串产出 | **不可以** |
| **前端演示** | 前端硬编码，与后端无关；后端不返回该字段或返回 `null` 时仍照常渲染 | **不可以** |
| **前端兜底** | 接口失败时回退到演示数据（`preview=true`） | **不可以** |

核心区分：**「接口 200」不等于「内容是事实」。** 当前页面上多数可见字段属于第二类。

占位串单点定义在 `zhiyin-infrastructure/zhiyin_infrastructure/local/llm.py:28`：

```text
_PLACEHOLDER_TEXT = "（第一期 Mock 产出，未接真实模型）"
```

它已经流到用户可见位置（对话正文、理论引用、行为引导、画像字段、依赖图），所以本文的标注不是锦上添花，而是防止演示被误读的必要手段。

---

## 二、复核证据

### 2.1 只读接口实测（2026-09-19，`code=0`）

| 接口 | 实测要点 |
| --- | --- |
| `GET /api/v1/app/bootstrap` | 10 个字段齐全：`app_name`、`identity`、`menus`、`routes`、`task_entries`、`feature_flags`、`copy_bundle`、`trust_blocks`、`banners`、`faqs` |
| `GET /api/v1/app/sessions` | `sessions` + `current_task_id` |
| `GET /api/v1/app/workspace` | `axis_a_stage` = **null**；`profile_panel.coverage` = 0.5、`overall_confidence` = 0.0；`report_panel.evaluation` 等正文为占位串；`coach_messages` = `[]`；`blocks` 仅含 `available`、`achievement_badge_keys` |
| `GET /api/v1/app/report/full-text` | `report_id` = `rpt_e427d892dbc9`、`version` = 2、`toc` / `sections` 齐全，正文为占位串 |

复核脚本见第十三节；只发 GET，不写任何数据。

### 2.2 代码证据约定

下文每行给出 `文件:行` 证据，前端路径相对 `zhiyin-src/template/`。

---

## 三、全局外壳

| 字段 / 区块 | 来源 | 判定 | 证据 |
| --- | --- | --- | --- |
| 应用名 `app_name` | `GET /app/bootstrap` | 真实 | `zhiyin-web/src/stores/session.ts:81` |
| 身份 `identity` | `GET /app/bootstrap` | 真实 | `session.ts:82` |
| 顶栏菜单 | `bootstrap.menus`，为空时用本地同值清单兜底 | 真实 + 前端兜底 | `zhiyin-web/src/components/common/TopBar.vue:18-31` |
| 路由可访问性 | `bootstrap.routes` | 真实 | `session.ts:84` |
| 任务入口 7 条 | `bootstrap.task_entries` | 真实 | `session.ts:85`；消费方 `components/home/TaskCardGroup.vue:44`（此前无任何消费方，见 §十六） |
| 功能开关 | `bootstrap.feature_flags` | 真实 | `session.ts:87`；`ReportPage.vue:28` 用 `export` |
| 文案包 | `bootstrap.copy_bundle` | 真实 | `session.ts:88`；`components/conversation/PipelinePanel.vue:20`、`components/conversation/ProfileFields.vue:35`、`components/home/TaskCardGroup.vue:32`、`pages/WorkspacePage.vue:122,261` |
| **信任区 `trust_blocks`** | 已下发，**前端无任何渲染方** | 后端下发但未使用 | 全 `src` 仅 `session.ts:42,89` 出现（声明 + 赋值） |
| **横幅 `banners`** | 已下发，**前端无任何渲染方** | 后端下发但未使用 | 仅 `session.ts:43,90` |
| **FAQ `faqs`** | 已下发，**前端无任何渲染方** | 后端下发但未使用 | 仅 `session.ts:44,91` |
| 无后端时的整套演示 bootstrap | — | **已删除** | `session.ts:94-97`：`_fetchBootstrap` 失败只记 `error` 并保持空态，不再注入演示 bootstrap（`session.ts:102-103` 留有说明） |

> 注意：`trust_blocks` / `banners` / `faqs` 三项后端一直在下发，但首页当前渲染的是硬编码页脚，**这三个契约字段实际上没有消费者**。

---

## 四、首页

| 字段 / 区块 | 来源 | 判定 | 证据 |
| --- | --- | --- | --- |
| 任务入口卡片 | `bootstrap.task_entries`（配置 7 条） | 真实 | `zhiyin-web/src/components/home/TaskCardGroup.vue:44-59`；标题取文案包 `home.task_group_title`（`:32`）；页面接线 `pages/HomePage.vue:162` |
| 任务点击 → 进环节 | `POST /app/task/enter` | 真实 | `HomePage.vue:86-113`（`selectTask` 统一持有建任务 / 登录拦截 / 错误处理 / 跳转）；`stores/conversation.ts:45-53`（`enterTask`） |
| 五环节说明区 | 组件内硬编码**说明文案**（每环节产出什么、边界在哪），**不再含任何编造产出样本** | 前端静态文案（不声称业务事实，但也不再演示假产出） | `zhiyin-web/src/components/home/ShowcaseStage.vue:27`（`STAGES`）、`:14-17`（CTA 只抛 `@start`） |
| 智能体展示 | 卡片来自 `bootstrap.agents` 派生的 `useAgentCatalog()`；**内嵌演示对话已删除**，点卡片只进能力边界页 | 真实 | `zhiyin-web/src/components/home/AgentsShowcase.vue:14,16-22,37-49`；`stores/agents.ts:59-84` |
| 页脚「数据来源 / 方法论出处 / 内容说明」 | 前端硬编码 | 前端硬编码（内容是如实声明，不声称任何业务事实） | `HomePage.vue:207-211`；其中「页面不预置任何演示数据…」与现状一致 |
| 只读演示提示 | — | **已删除** | `session` store 已无 `preview` 字段，全 `src` 无读取方 |

---

## 五、核心对话页

页面本身只有三栏容器（`zhiyin-web/src/pages/ConversationPage.vue:1-18`）；左栏在挂载时拉 `GET /app/sessions`，中栏与右栏数据来自 `conversation` store，且 **state 初始值一律为空**（`stores/conversation.ts:16-29`）。

| 字段 / 区块 | 来源 | 判定 | 证据 |
| --- | --- | --- | --- |
| 左栏会话列表 | `GET /app/sessions`；失败保持空态（**不再保留演示会话**） | 真实 | `pages/ConversationPage.vue:15-17`；`stores/conversation.ts:33-42` |
| 当前任务 `currentTaskId` | 由 `task/enter` / `sessions` 写入；无当前任务时 `send()` 先按兜底任务入口建真实任务 | 真实 | `conversation.ts:20,38,51,57-63`；兜底入口判定口径 `stores/session.ts:63` |
| 中栏对话 `turns` | 初始为**空数组**，只由真实一轮对话（`POST /app/conversation/message`）写入 | 真实（未发生对话时如实空态） | `conversation.ts:21,66,96-102` |
| 右栏管线卡 `pipeline` | 初始为**空数组**，只由 `turn.pipeline_cards` 写入 | 真实（同上） | `conversation.ts:22,104-106` |
| 主理徽章 `badge` | 初始为 `null`，只由 `turn.badge` 写入 | 真实（同上） | `conversation.ts:24,103` |
| 画像字段卡 `profile` | — | **已删除** | `conversation` store 已无 `profile`；画像覆盖度 / 缺口改由 `GET /app/workspace` 的 `profile_panel` 经 `useProfileCoverage()` 下发（`composables/index.ts:31-53`、`components/conversation/AnalysisHandoff.vue:16-32`） |
| 显式告知 `disclosure` | 初始为 `null`，只由 `turn.disclosure` 写入；切换会话时清空 | 真实（有真实对话时） | `conversation.ts:26,107,85`（`agents.ts` 已无 `summon`，该动作已移除） |
| 行为引导 `guide` | 初始为 `null`，只由 `turn.guide` 写入 | 真实（有真实对话时） | `conversation.ts:28,108` |
| 未走接口时的假回复 | — | **已删除** | `components/conversation/ChatStream.vue:50-53`：不再合成任何假回复，接口失败如实报「这轮消息没有发送成功：…」（`:56-61`） |
| 「生成解析（演示补全）」按钮 | — | **已删除** | `components/conversation/AnalysisHandoff.vue:12-34`：不再有「生成解析」按钮，也不再自判「已达解析门槛」；只如实显示后端下发的覆盖度 / 置信度与待补缺口 |
| 理论引用默认文案 | 默认串改为中性「职业咨询方法」；名称 / 定义取 `theory.name` / `theory.def` | 真实（有真实对话时） | `components/conversation/TheoryTag.vue:9-17` |
| 画像覆盖阈值（6 / 0.8 / 0.7） | — | **已收口**：前端不再持有任何阈值 | `composables/index.ts:20-29`：覆盖度 / 置信度只读 `profile_panel`，「是否达标」由后端判定 |
| 中栏 / 右栏演示数据标注 | — | 已无演示数据可标注 | 全前端已无 `MockBadge` 组件（见 §十六）；中栏 / 右栏在未发生对话时进入空态分支 |

> 结论：核心对话页在**没有发生过任何真实一轮对话**时，整屏显示**空态**（中栏提示语 + 空管线 + 无徽章），不预置任何演示数据。左栏会话来自 `GET /app/sessions`；切换会话会清空中栏结论与右栏管线（第一期没有"读历史会话"接口，故如实空态，不拼假历史）。

---

## 六、智能体页

智能体页与首页展示区共用同一份能力池视图：`useAgentCatalog()` 只从 `bootstrap.agents` 派生展示字段，**store 内不再持有任何硬编码定义**（`stores/agents.ts` 现 90 行）。

| 字段 / 区块 | 来源 | 判定 | 证据 |
| --- | --- | --- | --- |
| 智能体卡片（名称/职责/理论包/工具/边界/主题色） | `bootstrap.agents` 派生的 `useAgentCatalog()` | 真实 | `stores/agents.ts:59-84`（`no` 按下标派生、`stages` 取 `copyBundle['stage.<code>.label']`、`theories` 取 `theory.name`、`boundary` 取 `not_to_do.join('、')`）；主题色按序取 `THEMES`（`:51`） |
| 卡片状态文案（已完成 / 解析已完成 / 进行中 / 持续运行 / 同步中） | — | **已删除** | `stores/agents.ts` 已无状态文案；能力池卡片不再展示"进行中/同步中"之类的进度措辞 |
| 15 维逐项得分 | — | **已删除** | `DEMO_SCORES` 与基于它的解析结论已在 `fea88ce` 一并清除；15 维改由后端报告正文下发（见 §十五） |
| 综合匹配度 | — | **已删除** | 同上；PRD 只要求方案级匹配度（`FR-DECIDE-002`），报告级综合分是展示层臆造 |
| 解析状态 / 进度 | — | **已删除** | `stores/agents.ts` 已无定时器模拟；`AgentScopePanel` 不再自造进度 |
| 召唤某智能体 | — | **已删除** | `stores/agents.ts` 已无 `summon`；`disclosure` 只由真实一轮对话的 `turn.disclosure` 写入（见 §五） |
| 页面标注 | — | 已无演示数据可标注 | 全前端已无 `MockBadge` 组件（详见 §九 与 §十六） |
| 页脚来源声明 | 前端静态文案，如实说明能力池由 `GET /app/bootstrap` 的 `agents` 下发（定义在 `data/registry/agents.json`） | 前端硬编码（内容是如实声明，不声称任何业务事实） | `pages/AgentsPage.vue` 页脚、`components/agents/AgentScopePanel.vue` 页脚 |

---

## 七、智能工作台

工作台是标注与数据**最不一致**的页面：仅 hero 区有一枚 `MockBadge`（`pages/WorkspacePage.vue:135`），其余区块全部无标注。

| 字段 / 区块 | 来源 | 判定 | 证据 |
| --- | --- | --- | --- |
| 轴 A 阶段 `axisAStage` | 接口 `axis_a_stage`，实测为 **null** → 保留硬编码「冲刺行动期」 | **前端演示**（接口未给值时） | `stores/workspace.ts:15`、`:108`；`WorkspacePage.vue:142` |
| 轴 A 依据 `axisABasis` | **视图无该字段**，永远保留硬编码「近 14 天完成 3 项行动任务，画像覆盖度 67%…」 | **前端演示** | `stores/workspace.ts:16`；`zhiyin-api/zhiyin_api/dto/workspace.py:49-68` 无此字段 |
| 画像面板 `profilePanel` | 接口 `profile_panel`（实测 coverage 0.5、confidence 0.0、字段名为占位串） | 接口真实 / 内容 Mock | `workspace.ts:109` |
| ②③④⑤ 面板 | 接口 `report_panel` / `plan_panel` / `action_panel` / `review_panel`（正文为占位串） | 接口真实 / 内容 Mock | `workspace.ts:110-113` |
| 教练建议 `coachMessages` | 接口 `coach_messages`，实测为 `[]` → 空状态 | 接口真实 | `workspace.ts:114`；`CoachMessageStream.vue:17,55` |
| 依赖图 `dependencies` | 接口下发，内容为占位串 | 接口真实 / 内容 Mock | `workspace.ts:115` |
| **功能块 `blocks`** | 接口已下发，**页面未使用** | 后端下发但未使用 | `workspace.ts:97,116`；全 `src` 无其他读取方 |
| **关键节点日历** | 页面内硬编码 4 条 | **前端演示** | `WorkspacePage.vue:85-90` |
| **成长与成就徽章** | 页面内硬编码 6 枚 | **前端演示** | `WorkspacePage.vue:93-100` |
| 智能体最近动态 | 页面内硬编码时间串 | **前端演示** | `WorkspacePage.vue:103-109` |
| 五环节闭环状态 | 页面内硬编码 done/done/done/current/next | **前端演示** | `WorkspacePage.vue:44-50` |
| 「陪伴第 18 天 · 累计 7 次对话」 | 页面内硬编码数字 | **前端演示** | `WorkspacePage.vue:158` |
| 轴 A 纠正提示 | 写死「已记录（演示）」 | 前端演示 | `WorkspacePage.vue:150` |
| 时间线 5 节点 | 结构硬编码，detail 部分取接口面板 | 混合 | `WorkspacePage.vue:71-77` |
| `workspace.agents` 能力池 state | 硬编码，**无任何读取方** | 死数据 | `workspace.ts:18-22` |

---

## 八、完整报告页

| 字段 / 区块 | 来源 | 判定 | 证据 |
| --- | --- | --- | --- |
| 报告正文 `report` | `GET /app/report/full-text`，仅当 `report_id` 非空时替换 | 接口真实 / 内容 Mock | `pages/ReportPage.vue:246-251`；`:66` |
| 无后端时的正文 | 组件内 `demoReport` | 前端兜底 | `ReportPage.vue:19-51` |
| 目录 / 章节 | 接口 `toc` / `sections` | 接口真实 / 内容 Mock | `ReportPage.vue:70-71` |
| 个人画像 3 条 | 组件内硬编码 | **前端演示** | `ReportPage.vue:54-58` |
| 行动计划 3 阶段 | 组件内硬编码 | **前端演示** | `ReportPage.vue:60-64` |
| 方向方案 A/B/C | 组件内硬编码（含 82%/74%/69%） | **前端演示** | `ReportPage.vue:123-128` |
| 三叶草注释 | 组件内硬编码 | **前端演示** | `ReportPage.vue:117-121` |
| 「关键窗口 8 周」 | 组件内硬编码 | **前端演示** | `ReportPage.vue:138` |
| 报告编号 `reportNo` | 前端按当天日期本地拼 | **前端演示** | `ReportPage.vue:77-80` |
| 报告人 | `session.identity.nickname`，兜底「体验用户」 | 真实 + 兜底 | `ReportPage.vue:76` |
| 导出 PDF / Word | 前端本地生成 `.doc` / 打印；后端 `POST /app/assets/export` 为占位 | **前端演示** | `ReportPage.vue:73,206-240` |
| 15 维解析区块 | 复用 `agents` store 的演示分数 | **前端演示** | `ReportPage.vue:84-115` |
| 页面标注 | `MockBadge source="demo"` **硬编码**，即使已取到真实 `report_id` 仍显示 | 标注存在但不会自动消失 | `ReportPage.vue:264` |
| 成长成果卡 / 分享 | 组件内硬编码分享链接与邀请码 | **前端演示** | `zhiyin-web/src/components/report/GrowthShareCard.vue:45,53` |

---

## 九、其他演示组件

| 组件 / 区块 | 判定 | 证据 |
| --- | --- | --- |
| 站内消息中心（消息列表、通知偏好、免打扰时段） | 前端演示 | `zhiyin-web/src/components/common/MessageCenter.vue:6,10,51,143` |
| 教练建议响应闭环（跳转 / 稍后 / 不再提醒） | 前端演示（仅发埋点） | `CoachMessageStream.vue:19-34` |
| MockBadge 自身 | 实现存在，但 `source` 由调用方写死 | `MockBadge.vue:3,8`：全仓 6 处调用**全部**是 `source="demo"` 字面量，`source="real"` 从未出现 |

> `MockBadge` 的原始设计是「后端把来源标记为 real 后本组件自动不渲染」，当前实现把这个判断交给了调用方写死的 prop，因此**它永远不会因为真实数据接入而消失**。这就是 MOCK-1 的现状。

---

## 十、汇总

| 判定 | 数量级 | 典型内容 |
| --- | ---: | --- |
| 真实 | 少 | bootstrap 的 menus / routes / task_entries / feature_flags / copy_bundle；sessions 列表；task/enter；track |
| 接口真实 / 内容 Mock | 中 | workspace 五个面板、dependencies、report/full-text 全文、对话产出 |
| 前端演示 | **最多** | 工作台日历/成就/动态/闭环/陪伴天数、报告页画像/计划/方向/三叶草/编号/导出、智能体卡片与 15 维分数、对话页 turns/pipeline/badge/profile/guide、首页示例区、消息中心、成长卡 |
| 前端兜底 | 中 | bootstrap 失败整包回退、工作台/会话列表/报告页失败保留演示 |
| 后端下发但前端未消费 | 3 项 | `trust_blocks`、`banners`、`faqs` |
| 后端下发但页面未使用 | 1 项 | workspace `blocks` |

---

## 十一、高风险项（按「会误导评审」的程度排序）

1. **轴 A 阶段会被当成系统推断结果。** 接口 `axis_a_stage` 实测为 `null`，前端显示硬编码「冲刺行动期」，旁边还有「不是这样，纠正」按钮 —— 看起来像系统推断出错，实际是演示值（`WorkspacePage.vue:142`、`workspace.ts:15`）。轴 A 依据 `axisABasis` 更严重：DTO 里根本没有该字段，永远是硬编码的「画像覆盖度 67%、整体置信度 78%」。
2. **工作台用硬编码覆盖后端已下发的功能块。** `blocks`（FR-WB-007，含成就徽章键等）后端已返回，页面却渲染硬编码日历与 6 枚成就徽章（`WorkspacePage.vue:85-100`）。
3. **`MockBadge` 不会随真实数据消失。** 6 处调用全部写死 `source="demo"`；报告页即使拿到了真实 `report_id` 仍标注「演示数据」。反向也成立：**没有任何机制能标注「这段是 Mock 模型产出」** —— 对话正文、画像字段里的占位串目前只能靠字符串匹配识别（MOCK-1）。
4. **核心对话页整屏无标注。** `ConversationPage.vue` 及其三个子组件都不含 `MockBadge`；在没发生过真实对话时，整屏演示内容与真实产出外观完全一致。
5. **同一份「15 维结论」有两套来源。** 前端用 `DEMO_SCORES`（`agents.ts:98-114`），后端 `report_panel` / 报告 sections 用 Mock 模型产出。工作台、报告页、智能体页都读前者，所以**前端展示的匹配度与后端报告正文可能互相矛盾**。
6. **前端硬编码业务阈值。** `composables/index.ts:30-32` 把关键字段 6、覆盖率 0.8、置信度 0.7 写成前端常量，与「阈值只在 `policy_params.json`、不得硬编码」的口径冲突（该文件注释已自认是演示期落地值）。
7. **画像口径存在第三处实现。** 已知 OPEN-6 记录了 `policies/profile.py` 与 `mappers.py` 两处；前端 `useProfileCoverage` 又按自己的常量算了一遍「覆盖率 × 6」。
8. **死数据与死契约。** `workspace.agents`（state）无读取方；`trust_blocks` / `banners` / `faqs` 无渲染方。它们会让「契约已接通」的结论偏乐观。

---

## 十二、处理建议

### 12.1 不需要改冻结契约、可先做

1. 给核心对话页、工作台各区块、报告页补 `MockBadge`，并把 `source` 从写死字面量改为**由数据来源决定**（哪怕先由 store 里一个 `isDemo` 标志驱动）。
2. 让工作台渲染后端已下发的 `blocks`，删除硬编码日历与成就徽章，或明确降级为「后端无数据时的空状态」。
3. 轴 A：接口返回 `null` 时不显示具体阶段名，改为「待系统确认」；同时删掉 `axisABasis` 硬编码依据，没有依据就不显示依据。
4. 报告页把 `profileItems` / `actionPhases` / `directions` / `cloverNotes` / 「8 周」/ `reportNo` 改为「接口有则用、无则不渲染」，不再用演示内容兜底真实页面。
5. 清理死数据：`workspace.agents`；确认 `trust_blocks` / `banners` / `faqs` 是「待接线」还是「放弃」，并同步契约或文档。

### 12.2 需要先协调、可能触碰冻结契约或共享写点

1. **MOCK-1：后端透出可机器判定的来源标记。** 会改 DTO、`docs` 侧契约与生成物（`contracts/openapi.json`、`zhiyin-web/src/api/types.ts`），按 `AGENTS.md` §11 先做契约评审。
2. **前端阈值下沉。** 若决定由 bootstrap 下发 `profile_collection` 规则参数，会动 `BootstrapView`（冻结 DTO）。
3. **统一 15 维来源。** 让前端读后端报告/解析产出而不是 `DEMO_SCORES`，涉及产出契约与 `(agent_id, stage)` 查找，需与业务编排负责人对齐。
4. **画像口径三处收敛**（OPEN-6）：涉及冻结 `WorkspaceView` / `ProfileService` 与共享写点 `zhiyin-api/zhiyin_api/dto/mappers.py`。

---

## 十三、复核脚本

只发 GET、只读响应，不写任何数据；可直接在仓库根目录执行（后端需在 `127.0.0.1:8000`）：

```python
import json, urllib.request

def get(path):
    with urllib.request.urlopen("http://127.0.0.1:8000/api/v1" + path, timeout=10) as r:
        return json.loads(r.read().decode("utf-8"))

for path in ["/app/bootstrap", "/app/sessions", "/app/workspace", "/app/report/full-text"]:
    body = get(path)
    data = body.get("data")
    print(path, "code=", body.get("code"),
          "keys=", sorted(data.keys()) if isinstance(data, dict) else type(data).__name__)
```

判定「接口真实 / 内容 Mock」的要点：把响应正文与 `zhiyin-infrastructure/zhiyin_infrastructure/local/llm.py:28` 的 `_PLACEHOLDER_TEXT` 比对；命中即为 Mock 产出。

---

## 十四、清理约定

1. 某字段改接真实来源后：更新本文对应行的「判定」和「证据」列，不删除历史行。
2. 新增页面或新增硬编码演示数据时，必须同步本文；本文与[第一期未闭合项与 Mock 标注清单](职引-第一期未闭合项与Mock标注清单.md) 必须合读。
3. 本文只做标注与核验，不改动任何业务逻辑；原始自评文档不改写。

---

## 十五、更新记录：2026-09-19 15 维呈现口径变更（本轮）

按 §十四 第 1 条约定更新。**本文其余章节是 `c639b99` 当时的事实快照，不回改**；本条记录与之不同的现状。

| 原行 | 原判定 | 现状 |
| --- | --- | --- |
| §三「15 维逐项得分」（第 118 行） | 前端演示（`DEMO_SCORES`，`stores/agents.ts:98-114`） | **已删除**。`DEMO_SCORES` 与基于它的解析结论在 `fea88ce` 一并清除；15 维改由后端报告正文下发（`GET /app/report/full-text` 的 `dimension-N` 分组） |
| §三「综合匹配度」（第 119 行） | 前端演示（由演示分数求均值） | **已删除**。PRD 只要求**方案级**匹配度（`FR-DECIDE-002`），从未要求报告级综合分；该字段与对应的环形图 `ov-ring` 是展示层臆造，现不存在 |
| §三「15 维解析区块」（第 165 行） | 前端演示（复用 `agents` store 的演示分数） | **已改为真实**：报告页渲染 `report/full-text` 的 `dimension-N` sections（实测 3 组 / 6+5+4 = 15 条，带 conclusion 与 evidence） |
| §十二 12.2 第 3 条「统一 15 维来源」 | 待协调 | **已完成**（`fea88ce` 删除前端分数，报告页改读接口） |

**本轮新增字段**：

| 字段 | 来源 | 判定 | 证据 |
| --- | --- | --- | --- |
| 15 维 `tag`（**证据充分度**） | 后端报告 sections 的 `items[].tag`，取值来自内核冻结枚举 `DimensionEvidenceLevel`：`confirmed` / `partial` / `pending` / `missing` | **真实**（模型产出 + 契约强制） | `zhiyin_kernel/enums.py`；`ReportDimensionItem.tag`；前端 `ReportPage.vue::LEVEL_LABELS` / `heat-grid` |
| 15 维证据覆盖热力图 | 由上面同一份 `tag` 计数与上色 | **真实**（派生，不引入新数据源） | `ReportPage.vue::heatCells` / `heatSummary` |
| 分组标题 | 内核 `ReportDimensionGroup.group`（英文码）→ 前端 `GROUP_LABELS` 映射中文 | **真实**（此前直接把 `SELF-PORTRAIT` 显示给用户，属缺陷，本轮修正） | `ReportPage.vue::GROUP_LABELS` |

**关于 `tag` 的口径（重要）**：它表示"这一维的结论有多少依据"，**不表示这一维的好坏**，也不代表任何 0-100 分。热力图上色只反映证据是否充分；某维显示为「无依据」是**如实反映证据缺口**（对应 `FR-DIAG-006`），不是错误。定案依据见[业务口径决策记录](../../评审/业务口径决策记录-v1.0.md) 决策 17 与[修改日志 §十六](../00-索引与变更/职引-数据全链路修改日志.md)。

**遗留**：`local/llm.py` 的 `_PLACEHOLDER_TEXT` 仍在（§一 与 §十三 依赖它判定 Mock）。它只属于本地/测试合成实现，部署环境走真实 PAMI 模型；但该实现未声明 `IMPLEMENTATION_STATUS` 的风险仍未收口（待决问题 D1）。

---

## 十六、更新记录：2026-09-20 前端真实业务链路修复后就地复核

按 §十四 第 1 条约定就地更新：不改写历史行原意，只把**已失真**的行改为与代码事实一致。

**触发原因**：2026-09-20 的前端端到端实测（真实浏览器操作 + 真实后端数据）共修复 8 个功能缺陷（详见[修改日志 §三十三](../00-索引与变更/职引-数据全链路修改日志.md)），这些修复把 §三 / §四 / §五 / §六 中的多行判定从「前端演示 / 前端兜底」变成了「真实」或「已删除」。本文若不同步，会把已修好的项继续标成演示，属反向失真。

**复核方法**：逐文件重读 `zhiyin-web/src` 现源码核对行号与分支，**不凭记忆引用旧行号**（旧文多行 `文件:行` 已随重构漂移）。

### 16.1 §三 全局外壳

| 原行 | 原判定 | 现状 |
| --- | --- | --- |
| 顶栏菜单 | 真实 + 前端兜底 | 判定不变。兜底清单落实为 `components/common/TopBar.vue:18-23`（`FALLBACK_NAV_ITEMS`：`/`、`/conv`、`/wb`、`/report`），`navItems` 取 `:25-31` |
| 任务入口 7 条 | 真实（**无消费方**） | 仍是真实，但**已补消费方**：`components/home/TaskCardGroup.vue:44`（此前无任何渲染方，见 §四） |
| 文案包 | 真实 | 判定不变；证据行号校准为 `session.ts:88`，消费方补齐（`PipelinePanel.vue:20`、`ProfileFields.vue:35`、`TaskCardGroup.vue:32`、`WorkspacePage.vue:122,261`） |
| `trust_blocks` / `banners` / `faqs` | 后端下发但前端未使用 | 判定不变；证据行号校准为 `session.ts:42,89` / `:43,90` / `:44,91`（旧文行号已漂移） |
| 应用名 / 身份 / 路由 / 功能开关 | 真实 | 判定不变；证据行号校准为 `session.ts:81` / `:82` / `:84` / `:87` |
| 「无后端时的整套演示 bootstrap」 | 前端兜底（`preview=true`） | **已删除**。`session.ts:94-97` 失败只记 `error` 并保持空态，不再注入演示 bootstrap（`:102-103` 留有说明）；`session.preview` 已不存在 |

### 16.2 §四 首页

| 原行 | 原判定 | 现状 |
| --- | --- | --- |
| 任务入口卡片 | 真实（未登记消费方） | 真实。`components/home/TaskCardGroup.vue:44-59`；标题取文案包 `home.task_group_title`（`:32`）；页面接线 `pages/HomePage.vue:162` |
| 任务点击 → 进环节 | 直接前端路由跳转 | **已改为真实建任务**。`HomePage.vue:86-113`（`selectTask` 统一持有建任务 / 登录拦截 / 错误处理 / 跳转）；`stores/conversation.ts:45-53`（`enterTask`）。此前裸 `router.push` 不带任务，进对话页后发消息必然失败（P0-2） |
| 五环节说明区 | 前端演示（含编造产出样本） | 前端静态**说明文案**（每环节产出什么、边界在哪），**不再含任何编造产出样本**；`ShowcaseStage.vue:27`（`STAGES`）、`:14-17`（CTA 只抛 `@start`） |
| 智能体展示 | 前端演示（内嵌演示对话） | **已改为真实**：卡片来自 `bootstrap.agents` 派生的 `useAgentCatalog()`；内嵌演示对话已删除，点卡片只进能力边界页。`AgentsShowcase.vue:14,16-22,37-49`、`stores/agents.ts:59-84` |
| 页脚「数据来源 / 方法论出处 / 内容说明」 | 前端硬编码 | 判定不变，但内容已改为如实声明（含「页面不预置任何演示数据…」）；`HomePage.vue:207-211` |
| 「只读演示提示」 | 前端兜底 | **已删除**。`session.preview` 已不存在，全 `src` 无读取方 |

### 16.3 §五 核心对话页

页面与 store 的初始值一律为空（`stores/conversation.ts:16-29`，注释明写「演示数据曾经让『接口没接通』看起来像『接口接通了』」）。

| 原行 | 原判定 | 现状 |
| --- | --- | --- |
| 左栏会话列表 | 前端演示（演示会话） | **已改为真实**：`GET /app/sessions`，失败保持空态；`ConversationPage.vue:15-17` 补上了此前缺失的加载调用点（P1-2） |
| 当前任务 | 前端演示 | **已改为真实**：由 `task/enter` / `sessions` 写入；无当前任务时 `send()` 先按兜底任务入口建**真实**任务（`conversation.ts:20,51,57-63`；兜底口径 `session.ts:63`） |
| 中栏对话 `turns` | 前端演示 | **已改为真实**：初始空数组，三处真实来源写入——本轮用户气泡本地回显（`conversation.ts:71`）、`POST /app/conversation/message` 经 `applyTurn()`（`:138-147`）、刷新/切换会话时 `GET /app/conversation/history` 经 `loadHistory()` 回填（`:102-108`） |
| 右栏管线卡 `pipeline` | 前端演示 | **已改为真实**：初始空数组，由 `turn.pipeline_cards`（`applyTurn()`，`:149-151`）与 `GET /app/conversation/history` 的 `pipeline_cards`（`loadHistory()`，`:109-111`）写入 |
| 主理徽章 `badge` | 前端演示 | **已改为真实**：初始 `null`，由 `turn.badge`（`:148`）与历史接口的 `badge`（`:112-113`）写入 |
| 画像字段卡 `profile` | 前端演示 | **已删除**。`conversation` store 已无该 state；画像覆盖度 / 缺口改由 `GET /app/workspace` 的 `profile_panel` 经 `useProfileCoverage()` 下发（`composables/index.ts:31-53`、`AnalysisHandoff.vue:16-32`） |
| 显式告知 `disclosure` | 前端演示（`summon` 本地改写） | **已改为真实**：初始 `null`，只由 `turn.disclosure` 写入；切换 / 刷新会话时 `loadHistory()` **显式置回 `null`**（历史接口不含该项，它属于"上一轮刚发生的事"，不拿历史文案顶替）（`:129-135,152`）；`agents.ts` 已无 `summon` |
| 行为引导 `guide` | 前端演示 | **已改为真实**：初始 `null`，只由 `turn.guide` 写入；切换 / 刷新会话时同样由 `loadHistory()` 置回 `null`（`:129-135,153`） |
| 「未走接口时的假回复」 | 前端演示 | **已删除**。`ChatStream.vue:50-53` 不再合成任何假回复，接口失败如实报错（`:56-61`）；同时修掉双用户气泡（P2-1） |
| 「生成解析（演示补全）」按钮 | 前端演示 | **已删除**。`AnalysisHandoff.vue:12-34` 不再有该按钮，也不再自判「已达解析门槛」 |
| 理论引用默认文案 | 默认串含「（演示数据）」 | **已中性化**：默认名改为「职业咨询方法」，名称 / 定义取 `theory.name` / `theory.def`（`TheoryTag.vue:9-17`） |
| 画像覆盖阈值 6 / 0.8 / 0.7 | **前端硬编码业务阈值** | **已收口**：前端不再持有任何阈值，只读 `profile_panel` 的覆盖度 / 置信度（`composables/index.ts:20-29`），「是否达标」由后端判定 —— §十一 第 6 条与 §十二 12.1 第 7 条随之失效 |
| 中栏 / 右栏演示数据标注 `MockBadge` | 标注存在 | 已无演示数据可标注；**全前端已无 `MockBadge` 组件**（见 16.5） |

> 结论更新（2026-09-20 第二次改写）：核心对话页在**没有发生过任何真实一轮对话**时，整屏显示**空态**，不预置任何演示数据。
>
> ~~切换会话会清空中栏结论与右栏管线 —— 第一期没有「读历史会话」接口，故如实空态，不拼假历史。~~ **该前提已失效**：本轮补齐 `GET /app/conversation/history` 与消息落库后，**刷新页面与切换会话都会从后端读回该会话的真实消息、所处环节与管线卡**（`conversation.ts::loadHistory`，由 `ConversationPage.vue` 挂载时与 `SessionList` 点击时调用）。只有**该会话确实没有任何消息**时才显示空态；读历史失败会如实报"这段会话的历史没有读到"，并清空中栏，**不退回空态假装"这段对话本来就没有内容"**。
>
> 右栏管线面板的空态也按同一口径区分两种情形（`PipelinePanel.vue:44-54`）：未选中会话 vs 已选中但后端确实无进度，不再合成一句会导致误判的文案。

### 16.4 §六 智能体页

`stores/agents.ts` 现共 90 行，硬编码定义、演示分数、定时器、召唤动作**均已不存在**。

| 原行 | 原判定 | 现状 |
| --- | --- | --- |
| 智能体卡片（名称/职责/理论包/工具/边界/主题色） | 前端硬编码（`agents.ts:125-201`） | **已改为真实**：只由 `bootstrap.agents` 派生（`stores/agents.ts:59-84`）；`no` 按下标派生，`stages` 取 `copyBundle['stage.<code>.label']`，空数组回落 `copyBundle['agent.stage.none']`，`theories: theory.name`，`boundary: not_to_do.join('、')`；主题色按序取 `THEMES`（`:51`） |
| 卡片状态文案（已完成 / 解析已完成 / 进行中 / 持续运行 / 同步中） | 前端硬编码 | **已删除**：能力池卡片不再展示进度措辞 |
| 15 维逐项得分 | 前端演示（`DEMO_SCORES`） | **已删除**（`fea88ce` 清除，见 §十五）：15 维改由后端报告正文下发 |
| 综合匹配度 | 前端演示（演示分数求均值） | **已删除**（同上） |
| 解析状态 / 进度 | 前端演示（定时器模拟） | **已删除**：`agents.ts` 已无定时器，`AgentScopePanel` 不再自造进度 |
| 召唤某智能体 | 前端演示（未持久化） | **已删除**：`agents.ts` 已无 `summon` |
| 页面标注 | `MockBadge source="demo"` | 已无演示数据可标注；`MockBadge` 组件本身已删除（见 16.5） |
| （新增登记）页脚来源声明 | — | 前端静态文案，如实说明能力池由 `GET /app/bootstrap` 的 `agents` 下发（定义在 `data/registry/agents.json`） |

### 16.5 附带复核：§九 的组件级删除（只记事实，不逐行重写 §九 表）

本轮核对全仓引用后确认，§九 的三行中**有两行已整体失真**（组件文件已不存在，全仓仅 `README.md:34` 留有「已删除」说明）：

| §九 原行 | 原判定 | 现状 |
| --- | --- | --- |
| 站内消息中心 `MessageCenter.vue` | 前端演示 | **组件已整体删除**，无渲染方 |
| 教练建议响应闭环 ⇄ `CoachMessageStream.vue` | 前端演示（仅发埋点） | 判定不变（组件仍在，仍是组件内闭环 + `trackEvent('review_warning_response')`），本轮未改 |
| `MockBadge` 自身（「实现存在，但 `source` 由调用方写死」） | 实现存在 | **组件已整体删除**：全仓无 `MockBadge` 文件、无调用方。§十一 第 3 条的「6 处调用全部写死 `source="demo"`」随之失效 |
| §八「成长成果卡 / 分享」`GrowthShareCard.vue` | 前端演示 | **组件已整体删除**（该行属 §八，本轮未逐行复核 §八 其余内容） |

### 16.6 本轮**未**复核的部分（不要当现状）

以下章节本轮**未逐行核对**，内容仍为 `c639b99` 快照，其中已有部分行确定失真（例如 §七 的 `axisABasis` / 硬编码日历与成就徽章、§八 的 `profileItems` / `actionPhases` / `directions` / `cloverNotes` / 「8 周」/ `reportNo` 等，在现行代码中均已改由 `GET /app/workspace` 或 `report/full-text` 的 sections 下发）：

- §七 智能工作台（其余行）
- §八 完整报告页（其余行）
- §十 汇总（数量级统计基于旧快照）
- §十一 高风险项（第 3、6、7 条已随本轮修复失效；其余条目未复核）
- §十二 处理建议（12.1 第 1、4、7 条已部分落地，未逐条复核）

> 需要精确现状时，请以**本节 16.1–16.5** 与代码为准，不要引用上述未复核章节作为验收依据。

---

## 十七、更新记录：2026-09-20 读历史接口落地后就地复核（本轮）

按 §十四 第 1 条与 §十四 第 3 条（只标注与核验，不改动业务逻辑）更新。本轮**唯一的前提级变化**是：`GET /app/conversation/history` 上线 + 对话消息真正落库，§16.3 中那句「第一期没有『读历史会话』接口」**不再是事实**。

### 17.1 本轮就地改写的行

| 位置 | 原判定 / 原现状 | 现状 |
| --- | --- | --- |
| §16.3 中栏对话 `turns` | 「只由 `POST /app/conversation/message` 写入」 | **写入来源增加一处**：刷新 / 切换会话时 `GET /app/conversation/history` 经 `loadHistory()` 回填（`stores/conversation.ts:102-108`） |
| §16.3 右栏管线卡 `pipeline` | 「只由 `turn.pipeline_cards` 写入」 | **写入来源增加一处**：历史接口的 `pipeline_cards`（`loadHistory()`，`:109-111`） |
| §16.3 主理徽章 `badge` | 「只由 `turn.badge` 写入」 | **写入来源增加一处**：历史接口的 `badge`（`:112-113`），跨交接的气泡各自带当时主理名 |
| §16.3 显式告知 `disclosure` | 「切换会话时清空」 | **口径写实**：`loadHistory()` **显式置回 `null`**；历史接口不含该字段（属"上一轮刚发生的事"），不拿历史文案顶替 |
| §16.3 行为引导 `guide` | 「只由 `turn.guide` 写入」 | **同上**：`loadHistory()` 显式置回 `null` |
| §16.3 结论段 | 「切换会话会清空中栏与右栏 —— 第一期没有『读历史会话』接口，故如实空态」 | **前提已失效**：刷新与切换会话都从后端恢复真实历史与环节进度；仅"该会话确实没有消息"才空态 |

### 17.2 证据（可复核）

- 后端：`GET /api/v1/app/conversation/history?task_id=` → `ConversationHistoryView`（`task_id/stage/stage_label/badge/messages/pipeline_cards`）；未知会话 **404**、非本人会话 **403**，不返回空历史冒充成功。
- 落库：每轮用户消息与主理消息写入 `conversation_message` 表（只追加）；表由迁移 `20260920_0004` 建立（生产 `DatabaseContext` 是 `auto_create=False`，库表全由 alembic 管理）。
- 前端：`ConversationPage.vue:17-22` 挂载时、`SessionList.vue:10` 点击时调用；`loadHistory` 内**加载期间已切走则丢弃结果**，避免旧响应覆盖新会话。
- 真实浏览器：刷新后历史仍在（2 user + 2 agent）；切到另一条会话时中栏标题与五环节高亮随之切换、中栏如实空态；再切回原会话历史原样恢复。

### 17.3 本轮仍未复核的部分（不要当现状）

- §七（工作台）、§八（报告页）其余行、§十（汇总）、§十一（高风险项）其余条目、§十二（处理建议）——仍是 `c639b99` 快照。
- §16.6 的"未复核清单"继续有效，本轮**未缩小也未扩大**其范围：本轮只改了与对话历史直接相关的 5 行与 1 段结论。

> 完整改动明细、迁移根因与门禁结果见[修改日志 §三十五](../00-索引与变更/职引-数据全链路修改日志.md)。

