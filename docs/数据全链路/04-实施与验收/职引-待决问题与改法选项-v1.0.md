# 职引 · 待决问题与改法选项 v1.0

| 项目 | 内容 |
| --- | --- |
| 文档用途 | 集中登记「**要不要修、怎么修都需要人拍板**」的问题，并给出可选改法与各自代价 |
| 适用对象 | 架构负责人、业务口径负责人、产品负责人、平台负责人、前端负责人（每条注明决策人） |
| 建立日期 | 2026-09-19 |
| 产生方式 | 2026-09-19 对 `test-yuan` 工作区的架构与业务合规复查（见[修改日志](../00-索引与变更/职引-数据全链路修改日志.md)第九至十一节） |
| 与既有文档的关系 | 本文只收**改法未定**的问题；已闭合的未闭合项见[第一期未闭合项与 Mock 标注清单](职引-第一期未闭合项与Mock标注清单.md)，架构缺口的收口进展见[架构与结构评估-最终版](../../评审/职引-架构与结构评估-最终版.md) §7.1 |
| 维护约定 | 定了之后把结论写回本文对应条（标 `已决` + 决策人 + 日期），并同步修改日志；**不要**在没拍板前由实现者自选一条默默做掉 |

---

## 一、为什么单独一份

复查中出现的下列情况，实现者无法自行决定，硬做等于替业务或架构拍板：

1. **改法本身有取舍**：两条路都能自圆其说，代价落在不同的契约或团队上；
2. **要动冻结契约或共享形状**：按《AGENTS.md》§2 / §5 / §11.2，必须先说明影响并取得确认；
3. **本质是口径问题**：例如"报告里要不要再放一份画像"是产品口径，不是技术选择；
4. **需要平台或内容方先到位**：代码已就绪，缺的是凭据或内容。

本文把这些集中起来，让每一次决策都有落点，而不是散在聊天记录或被"顺手做了"。

---

## 二、状态说明

| 状态 | 含义 |
| --- | --- |
| 待决 | 尚未拍板；实现者不得自行选择改法 |
| 已决待实施 | 已拍板改法，尚未落地 |
| 已关闭 | 已实施或明确不做，注明依据 |

---

## 三、待决问题

### D1 · 本地合成模型没有如实声明实现状态，占位产出可能不被拦住 —— 已关闭（2026-09-19）

> **已关闭**：取三段做法——装配期**硬失败**（新增 `ZHIYIN_ALLOW_PLACEHOLDER_LLM`，默认拒绝启动；判定不按 env 名字，因为部署环境就是 `local`）+ **如实上报**（`AssemblyReport.placeholders`、`/healthz` 降级、`--check` 可见）+ **应答内提示**（`model_degraded` 在成功路径也透出，文案走动态资源）。**未采纳「标 skeleton」**：它是完整实现、不是骨架，标 skeleton 是另一种不如实。实施见[修改日志](../00-索引与变更/职引-数据全链路修改日志.md) §17.3。

| 项 | 内容 |
| --- | --- |
| 现象 | `LocalOrMockLLM` 未声明 `IMPLEMENTATION_STATUS`，装配报告按兜底逻辑把它报成 `wired`；它只在 `LLMResult.degraded=True` 里自述降级，而**该标记目前没有任何消费方** |
| 证据 | `zhiyin-infrastructure/zhiyin_infrastructure/local/llm.py`（类无状态声明；`chat()` 返回 `degraded=True`、`usage={"mock": True}`）；`zhiyin-boot/zhiyin_boot/report.py::_status_of` 的兜底分支；`zhiyin-boot/zhiyin_boot/container/ports.py` 把 `llm` 列为受门禁评分的能力位 |
| 影响 | 当前部署已接真实 PAMI 模型、用户可见输出不含占位内容；但**若某环境漏配 `ZHIYIN_USE_PAMI_LLM`，占位产出不会被门禁、装配报告或界面任何一处拦住** |
| 为什么要斟酌 | 三条路各有真实代价，且都动共享面 |
| 改法 A | 把该类标为 `skeleton`。代价：与"第一期可独立跑通"的既有设计冲突；本地/CI 下 Phase 2/3 门禁会红；`tests/test_container.py:82` 现有断言会红 |
| 改法 B | 把 `degraded` 透出到接口层（`ConversationTurnView` 加来源/降级标记）。代价：改公开 DTO + OpenAPI + 前端类型，等于把已"失去对象"的 MOCK-1 重新打开 |
| 改法 C | 装配期显式失败：未配置真实模型且未显式 opt-in 时**拒绝启动**。代价：改动运行前置，影响所有本地开发与 CI 的既有用法 |
| 建议 | **C 的变体**（加显式 opt-in 开关，默认拒绝启动）——它把"忘记开开关"变成启动即失败，不依赖任何界面标注，也不与"本地可跑通"冲突（本地开发显式开 opt-in 即可） |
| 决策人 | 架构负责人 + 共享装配负责人 |
| 不决策的后果 | 漏配环境会把占位内容当业务结果呈现，且现有全部门禁都不会报警 |

### D2 · 报告正文缺「个人画像」章节 —— 已关闭（2026-09-19）

> **已关闭**：原建议 B（改 DTO 描述为不含画像）**不成立**——《前端页面设计》§4.4 第 243 行明确要求报告含个人画像、第 245 行要求本页「读取资产版本」。定案为 `Report.profile_snapshot`（生成时的画像**冻结快照**，`ProfileField` 已是内核形状），旧报告为空则不生成章节、无需迁移。实施见[修改日志](../00-索引与变更/职引-数据全链路修改日志.md) §17.5。

| 项 | 内容 |
| --- | --- |
| 现象 | `ReportFullTextView.sections` 的字段描述承诺"15 维全景 / 方案 / 行动计划 / **个人画像**"。2026-09-19 已补上方案与行动计划（修改日志第十节），**个人画像仍未产出** |
| 证据 | `zhiyin-api/zhiyin_api/dto/asset.py:35-37`；`mappers.report_full_text_view` 实际产出的章节 id 只有 `verdict` / `swot` / `dimension-*` / `directions` / `action` |
| 为什么要斟酌 | 画像数据已在 `GET /app/workspace` 的 `profile_panel` 下发，工作台与对话页都在展示；报告正文再放一份是**同一份数据的第二处展示**，而且二者性质不同 |
| 改法 A | 报告加 `profile` 章节（facade 从工作台聚合或读黑板）。代价：与工作台/对话页重复；且要先定"报告里的画像是版本快照还是活状态"——报告是**版本化只读资产**，画像会继续变化，混在一起会出现"报告里的画像与工作台不一致" |
| 改法 B | 把 DTO 描述改为实际产出（"15 维全景 / 方案 / 行动计划"），报告不重复画像。代价：与《前端页面设计》§4.4 的报告页章节清单不一致，需同步那份文档 |
| 建议 | **B**——报告是版本化快照、画像是活状态，二者不该混；若要"报告里的画像"，应作为报告版本的一部分明确冻结，而不是实时读当前画像 |
| 决策人 | 产品/业务负责人 |

### D3 · `LoopCoordinator.advance` 仍无生产调用方 —— 已关闭（2026-09-19）

> **已关闭**：取**删除**（比原建议 B「留文档」更彻底）。`advance` 只落库、不发 `loop_stage_changed`、不写会话记忆、不组告知，是「落库成功而事件失败」的不自洽入口，且与 `_perform_handoff` 重复；阶段变更的唯一入口是冻结的 `Orchestrator.handoff`。实施见[修改日志](../00-索引与变更/职引-数据全链路修改日志.md) §17.4。

| 项 | 内容 |
| --- | --- |
| 现象 | 2026-09-19 已把 `run_stage` 接上生产路径（修改日志第九节），但**阶段推进**仍由编排器 `_perform_handoff` 完成——它要同一处同步发 `loop_stage_changed`、写会话记忆、组告知文案。`advance` 依旧只在测试里被调用 |
| 证据 | 全仓 `.advance(` 仅出现于 `tests/test_business_loop.py:194,205`；`zhiyin-business/ports/loop.py:126` 是冻结 Port 方法 |
| 为什么要斟酌 | 冻结 Port 的方法长期空转，会被下一次复查再次判成"看起来有、实际没有"；但把推进改走 `advance` 会把它变成"落库但不发事件"的半个职责，失败一致性更难说清 |
| 改法 A | 编排器改用 `advance` 做阶段落库，事件与告知仍留在编排器。代价：职责切两处；`advance` 落库成功而事件失败时状态不自洽 |
| 改法 B | 在 Port 文档里明确 `advance` 的定位（供独立 Worker / 会话续接使用），并如实说明它当前不在对话路径上。代价：不改代码，靠文档防误判 |
| 改法 C | 删除 `advance`。代价：冻结 Port 变更，需确认无外部调用方 |
| 建议 | **B**（先如实说明），等出现真实调用方再定去留 |
| 决策人 | 架构负责人 + 业务编排负责人 |

### D4 · 决策 5 的「加权平均」与实现的「等权平均」不一致 —— 已关闭（2026-09-19）

> **已关闭**：取 B。决策记录第 5 行与 `policy_params.json::profile_collection.note` 均改为「**等权平均**」（实现与它自己的 docstring 本来就是等权），并补「若将来要加权，须先给出每字段权重依据」。实施见[修改日志](../00-索引与变更/职引-数据全链路修改日志.md) §17.1。

| 项 | 内容 |
| --- | --- |
| 现象 | 决策记录与动态资源的 note 都写"关键字段覆盖 + **加权**平均"，实现是**等权**平均 |
| 证据 | `docs/评审/业务口径决策记录-v1.0.md` 决策 5 行；`data/registry/policy_params.json` 的 `profile_collection.note`；`zhiyin-business/policies/profile.py::calculate_overall_confidence`（`sum(present) / len(present)`） |
| 为什么要斟酌 | 要么给权重、要么改正文。措辞不改会让人以为存在权重，而实现里没有任何权重依据 |
| 改法 A | `policy_params.profile_collection` 增加每字段权重，实现改加权。代价：需要业务给出权重值；历史数据的口径会变 |
| 改法 B | 把决策记录与 note 的措辞改为"等权平均"。代价：纯文案，但属修改业务口径文档，需口径定稿人确认 |
| 建议 | **B**——当前实现简单可解释，而"加权"没有任何权重依据可循 |
| 决策人 | 业务口径负责人（该文档定稿人） |

### D5 · 对话内没有「进入诊断」的显式入口 —— 已关闭（2026-09-19）

> **已关闭**：比原建议 B 更小——`VERIFY_DIRECTION → DIAGNOSE` 的映射**本来就存在**，缺的只是表达。往 `intent_keywords.verify_direction` 追加「开始诊断 / 进入诊断 / 做诊断 / 档案够了 / 进入下一环节」即可，**零代码、不需要新意图或新端点**。注意不要用「下一步」（它属 `how_to_act` 且键序更前）。实施见[修改日志](../00-索引与变更/职引-数据全链路修改日志.md) §17.1。

| 项 | 内容 |
| --- | --- |
| 现象 | 从①到②目前有两条路：首页任务卡 `verify_direction`（`task_entries.json` 里 `target_stage=diagnose`）；以及①达标后**自动交接**（2026-09-19 实现）。缺的是用户在对话里主动说"档案够了，开始诊断"这类显式动作——它不命中任何路由关键词，会被判为留在① |
| 为什么要斟酌 | 属产品交互口径；加动作要多一个端点或一组消息约定 |
| 改法 A | 不加，靠自动交接 + 首页入口。代价：用户想提前进②只能换入口 |
| 改法 B | 给 `policy_params.routing.intent_keywords` 加一组"请求进入下一环节"的表达。代价：纯数据改动，但要定表达集合 |
| 改法 C | 新增显式接口（如 `POST /app/task/advance`）。代价：公开 API + OpenAPI + 前端类型变更 |
| 建议 | 先 **B**（零契约成本、可随时调整），观察真实用法后再考虑 C |
| 决策人 | 产品/业务负责人 |

### D6 · npm 依赖告警（2 中危 + 2 高危）—— 已关闭（2026-09-19）

> **已关闭**：审计确认 4 个告警包**全部 dev-only**、产物不含它们，且**没有任何非破坏性修复**（`fixAvailable` 全为 major）。分两步：先 `openapi-typescript` 6.7.6 → 7.13.0（undici 随之消失，清 1 高 1 中），再 `vite` 5.4.21 → 7.3.6 + `@vitejs/plugin-vue` 6.0.9（清 vite 高危与 esbuild 中危）。**实测 `npm audit` 归零**。未跑 `npm audit fix --force`（会把 plugin-vue 的 peer 打坏）。实施见[修改日志](../00-索引与变更/职引-数据全链路修改日志.md) §17.7。

| 项 | 内容 |
| --- | --- |
| 现象 | `npm install` 报告 2 中危、2 高危依赖问题；至今未执行可能带来破坏性升级的 `npm audit fix --force` |
| 为什么要斟酌 | 前端生态（Vite / Vue 工具链）大版本升级常带破坏性变更，需要逐项评估而不是批量强升 |
| 改法 A | 先做**只读评估**：列出可安全升级项与需评估项（含影响面），再逐项升级并跑前端门禁 |
| 改法 B | 暂不处理，登记为已知风险 |
| 建议 | **A 的第一步**（只读评估清单）先做，升级单独提交，便于回滚 |
| 决策人 | 前端负责人 |

### D7 · PAMI RAG 应用与知识库内容未就绪 —— **进行中（2026-09-19 已拆解并推进平台侧）**

> **进展（2026-09-19）**：拆成 8 个子项后逐项核实，并已实际推进平台侧——**知识库已建成、22 份自有内容文档已导入**。但**解析全部失败**，根因已定位到平台自身配置：`bff-service` 的 `callback_url` 指向 Docker 宿主网关 `http://172.17.0.1:6668`，RAG 服务取 embedding 时连不上（实测 `Connection refused`；换成 `bff-service:6668` 返回 200）。平台是 vendored 第三方工程，《AGENTS.md》§10 禁止职引侧自行改其契约，故**需平台负责人修**。**未创建 RAG 应用**——库解析全失败时发布应用只会制造"看起来配好了、一问全空"的陷阱。
>
> **需你确认**：① 理论卡内容定稿（16 张卡缺方法步骤/边界/示例/来源）；② 招聘来源条款/robots/字段范围/频率/保存期审核。
>
> **未完成但可做**：JD 过期与下架机制、ES/Embedding/pgvector/Rerank 故障注入、引用溯源核对、基准评测报告。
>
> **已完成（同日续做）**：上列 4 项**全部做完**——④ JD 过期下架（新增 `document_expiry` worker，相位门禁与归属同步）、⑤ 故障注入（6 条单测 + 容器内真实故障注入探针 `scripts/fault_injection_probe.py`）、⑥ 引用溯源补**版本**（`Report.source_versions`）、⑦ 基准评测（`scripts/eval_retrieval.py` + [基准报告](职引-检索质量基准-2026-09-19.md)）。另：⑤ 过程中发现**新问题 D12**（线上关键词通道正在提供标注 DEMO 的内容，却被门禁算作已接线），已登记待你拍板。
>
> 完整实测见[修改日志 §十八](../00-索引与变更/职引-数据全链路修改日志.md)；代码侧收口见 §十九、§二十。

| 项 | 内容 |
| --- | --- |
| 现象 | `ZHIYIN_USE_PAMI_SEARCH=0`，检索走本地关键词实现。**实现是真的，内容不是**：它读 `data/knowledge/*.json`，而那批卡片自述为 DEMO（见 **D12**）。先前本文写的"是真实实现、不是 Mock"只对代码成立，对内容不成立，已更正；PAMI 侧 RAG 应用与知识库仍为空 |
| 为什么要斟酌 | 需要平台侧发布应用 + 内容侧补齐（第三期理论卡、职业/专业/行业/政策来源台账），不是代码问题；职引侧的 `PamiSearchGateway` 代码已就绪 |
| 改法 A | 平台发布 RAG 应用并导入知识后，配置 Key 与开关接通。代价：依赖平台与内容排期 |
| 改法 B | 保持本地关键词检索，等内容补齐再切。代价：检索质量停留在本地实现水平 |
| 建议 | **B → A**：先补内容（没有内容时接通 RAG 只会返回空证据），内容到位后一次切换并跑固定评测集 |
| 决策人 | 平台负责人 + 数据能力负责人 |

### D8 · 动态资源里仍有「演示入口」能力位，且整批首页载荷前端并未消费 —— 已关闭（2026-09-19）

> **已关闭**：取 A（清演示残留）**并修掉一处原记未提到的 §8 违规**——`workspace.available_blocks` 原是写死在 Python 里的清单，把 `export`/`mentor`（`enabled:false`）与 `demo` 都列成 available；现改为按 `feature_flags` 过滤派生（available 就是 enabled）。演示残留四处已删，两条库内残留行经人工确认后下架。B（接线消费）/ C（停止下发）仍**另行评估**。实施见[修改日志](../00-索引与变更/职引-数据全链路修改日志.md) §17.2。

| 项 | 内容 |
| --- | --- |
| 现象 | 2026-09-19 全链路复查实测：`GET /app/bootstrap` 仍下发 `feature_flags.demo = true`、`banners[0] = demo_entry`（正文"用脱敏演示数据走完首页 → 对话 → 工作台，不写真实黑板"）、`trust_blocks[1].expandable_ref = "demo_report"`；`GET /app/workspace` 的 `blocks.available` 仍含 `"demo"`；`faqs.data_safety` 的答案写"本期只使用演示与脱敏数据…" |
| 证据 | `data/registry/feature_flags.json:9`、`data/registry/banners.json:5-11`、`data/registry/trust_blocks.json:16`、`zhiyin-business/services/workspace.py` 的 `available_blocks`、`data/registry/faqs.json:14` |
| 关键事实 | **这批载荷前端一处都没有消费**：`featureFlags` 只在 store 里存取、`blocks` 只读 `achievement_badge_keys`、`trust_blocks` / `banners` / `faqs` 无任何渲染方。所以"演示入口"既没被清掉、也没被看到——这也是它此前没被发现的原因 |
| 为什么要斟酌 | ① 删"演示能力位"是实现层可定的清理，但**要先确认它们没有别的用途**（例如 `feature_flags.demo` 是否预留给未来的只读演示模式）；② `faqs.data_safety` 是**合规声明**，措辞需按当前真实口径重写，不能由实现者编；③ 整批首页载荷前端不消费，是"继续下发"还是"停止下发"，属产品与前端口径 |
| 改法 A | 清理演示残留：删除 `feature_flags.demo`、`blocks.available` 里的 `"demo"`、`demo_entry` 横幅、`expandable_ref: demo_report`；`faqs.data_safety` 改为按当前口径的合规说明。代价：需确认 `demo` 开关无其他用途 |
| 改法 B | 接线消费：把 `trust_blocks` / `banners` / `faqs` / `feature_flags` 接进首页与工作台。代价：前端工作量，且要重新决定"演示入口"在产品里的去留 |
| 改法 C | 停止下发：既然前端不消费，就从 bootstrap 移除这几项。代价：改公开 DTO + OpenAPI + 前端类型，并丢掉已设计好的首页内容能力 |
| 建议 | 先 **A**（清演示残留，风险最低，与"不允许留 Mock"一致），再单独评估 B/C（首页内容要不要真正做出来） |
| 决策人 | 产品/业务负责人（内容口径）+ 前端负责人（是否接线） |

> 口径澄清：本文**不把 `data/registry/*.json` 里 `_note` 的"（DEMO）"标记算作 Mock 数据**——那是"这批动态资源是演示种子、后续迁 DB"的资源标记，不进响应载荷。同样，`faqs.data_safety` 属于合规声明（《AGENTS.md》§10 要求第一阶段只用 DEMO、脱敏或本地测试数据），只是措辞需要按现状态更新。

### D9 · 能力池（智能体定义）由谁下发 —— 已关闭（2026-09-19）

> **已关闭**：**直接做了 A**，因为原记的两个形状难点有一个不存在——`stages` 用**已有的** `get_output_contract(agent_id, stage)` 逐环节探测即可推出，无需给 Repository 加 `list_output_contracts`；理论名有现成的 `list_theory_cards`。最终是**一个**聚合方法 `RegistryService.list_agent_capabilities()` + `BootstrapView.agents`；`no`/`shortName`/`theme` 由前端派生、不进契约。做了 A 之后 B（守卫比对）不再需要，但补了一条「前端不得硬编码能力池」的守卫。实施见[修改日志](../00-索引与变更/职引-数据全链路修改日志.md) §17.6。

| 项 | 内容 |
| --- | --- |
| 现象 | 前端 `zhiyin-web/src/stores/agents.ts::agentCatalog` **硬编码**五个智能体的名称 / 职责 / 理论包 / 工具 / 边界；而 `data/registry/agents.json` 被声明为能力池的唯一事实来源。`GET /app/bootstrap` **没有 `agents` 字段**，两处靠人工保持一致 |
| 证据 | `zhiyin-web/src/stores/agents.ts`（目录常量）；`zhiyin-api/zhiyin_api/dto/bootstrap.py`（无 agents 字段）；`data/registry/agents.json`；`AGENTS.md` §8 明确禁止把智能体硬编码进 Vue/TS |
| 为什么要斟酌 | 这不是"接线"就能完事的：① `RegistryService`（冻结业务 Port）**没有 `list_agents`**，`RegistryRepository` 有；② `BootstrapView`（公开 DTO）需加字段，会改动 `contracts/openapi.json` 与前端 `types.ts`；③ **载荷形状需要设计**——见下 |
| 形状难点（已核实） | `AgentDescriptor` 只有 `id / name / role_summary / theory_packages / tools / call_scenarios / not_to_do / status`。前端还需要：<br>· `stages`「负责哪一环节」——registry 里**没有**，但可从 `output_contracts.json` 的 `(agent_id, stage)` 推出（`profile_analyst→collect`、`career_advisor→diagnose+decide`、`path_planner→act`、`companion_coach→review`、`info_scout→无`），而 `RegistryRepository` 目前**没有 `list_output_contracts`**；<br>· `theory_packages` 是**理论卡 id**，前端要显示的是中文名（如"帕森斯 · 了解自我"），需后端用 `list_theory_cards` 解析；<br>· `no` / `shortName` / `theme` 属纯展示，可由前端按下标与名称派生，不必进契约 |
| 改法 A | 后端下发：`RegistryService.list_agents()` +（为推环节）`list_output_contracts()` + `BootstrapView.agents`，后端解析理论中文名；前端 `agentCatalog` 改为读 bootstrap。代价：**动两处冻结契约 + 一个生成物**，且要定上面的载荷形状；收益是彻底消除硬编码与人工同步 |
| 改法 B | 保持前端静态配置，但在文件头与界面上如实标注"与 `agents.json` 同值、需人工同步"，并加一条守卫比对两处的 id / name 集合是否一致。代价：仍不是单一事实来源，但漂移会被测试拦住 |
| 建议 | **先 B 止血、再评估 A**：B 成本低且能立刻防漂移；A 的载荷形状需要与前端负责人一起定（尤其"负责哪一环节"要不要作为契约字段下发） |
| 决策人 | 前端负责人 + 架构负责人 |
| 当前状态 | 本轮已先把界面文案从不实的"能力池来自动态资源"改为如实措辞（它此前声称的能力实际不存在） |

### D10 · 15 维用什么呈现（分值 vs 等级） —— 已关闭（2026-09-19 拍板并实施）

| 项 | 内容 |
| --- | --- |
| 现象 | 报告页曾用**前端硬编码的 0-100 分**画雷达图/热力图/维度条（`radarTarget = [82, 82, 78, 80, 75, 76]`、`matchScore` 取自 `agents.analysis`）。这些分数在契约里**根本不存在**，属展示层臆造；删除后，依赖这些数字的样式成了死 CSS |
| 关键查证 | ① 15 维**有**契约（`zhiyin_kernel/assets.py::ReportDimensionGroup/ReportDimensionItem`），运行期**真在产出** 3 组 6+5+4 = 15 条（查容器内 `report.payload`）；② 但 `ReportDimensionItem` 只有 `index/name/tag/conclusion/evidence`，**没有分数字段**；<br>③ PRD `FR-DIAG-001`（P0）只要求"每维有结论与证据引用"，**从未要求分值**；④ 《前端改版需求说明》§2 要求"15 维热力图**或**雷达图"，且该文件 §1 自己规定"若实现新界面需要新数据，按 §9.3 提出" |
| 曾考虑的改法 | A 给 `ReportDimensionItem` 加 `score`/`level` 分值字段并由诊断环节产出；B 保持现状、改设计文档为不做分数可视化；C 用有序等级替代分数（不新增数值字段） |
| **定案（取 C）** | 把 `tag` 由自由字符串**收紧为闭合枚举** `DimensionEvidenceLevel`：`confirmed` / `partial` / `pending` / `missing`。热力图按等级上色；**不做雷达图**（半径必须是数值，用等级硬撑即假精确；需求写的是"热力图或雷达图"，选热力图在授权内） |
| 为什么不选 A | 加分值等于为 PRD 未要求的轴凭空造口径，且"分从哪来、如何可解释、是否可溯源"都没有依据，模型很容易给出"看起来精确"的数字——正是本轮要清掉的东西 |
| 为什么不选 B | 设计要求里有这张图；而"证据够不够"是报告能自证、用户能逐条核对的**真实事实**，用它做可视化不算编造，没必要放弃整个视图 |
| 决策人 | 用户（本轮直接拍板"按 1 做"） |
| 实施 | 内核枚举 + `ReportDimensionItem.tag` 收紧 + 提示词四档判据 + 前端「15 维证据覆盖」热力图；实证见[修改日志 §十六](../00-索引与变更/职引-数据全链路修改日志.md) |
| 实证 | 真实 PAMI 模型跑 ②诊断：`degraded=False`，3 组 / 15 维，tag 全在四档内，契约校验通过 |
| 遗留 | `tag` 字段名语义偏窄（装的是"证据充分度"），改名 `evidence_level` 属更大的契约变更，未做；JOB-MARKET 组普遍 `missing` 是本地检索未接 RAG 内容的**如实反映**（对应 `FR-DIAG-006`），内容补齐后会自然上升 |

---

### D11 · 检索证据的 id 口径不一致（本地关键词通道返回裸 id） —— 已关闭（2026-09-19，用户选 A）

> **已关闭**：用户拍板改法 **A**（统一为 `namespace:id`）并已落地：
>
> - 三条出 id 的通道全部统一——`LocalSearchGateway`（裸 id → `namespace:id`）、`PamiSearchGateway`（`pami:xxx` → `namespace:xxx`）、`RrfHybridSearchGateway._vector`（读时给向量库**历史裸 id** 兜底补齐，幂等，不会出现 `theory:theory:x`）；
> - **RRF 去重因此成立**：同一篇文档在关键词与向量通道用同一个键，不再被当成两篇（新增用例锁这条不变式）；
> - `TheoryRef.theory_id` 与"检索地址"**解耦**：它是指向**理论卡**的 id，不该带 namespace 前缀（原实现 `theory_id=hit.evidence_id` 只是巧合能跑）；
> - 基准脚本的诊断**改为实测通道返回的 id**（原来从语料 JSON 推断，于是修好后它仍然报"不一致"——记录型断言的观测对象错了），现在报告 `id 口径一致` 并给出样例；
> - 已知限制（写入 PAMI 适配器注释）：PAMI 只回 title/snippet、不回我们的 `source_id`，所以它的 synthetic id 仍无法与向量通道的同一篇文档对上——这是数据侧限制，不是本次改法能解决的。


| 项 | 内容 |
| --- | --- |
| 现象 | 评测集的期望 id **一律**写成 `namespace:id`（如 `resume:benchmark-001`），而 `LocalSearchGateway` 返回的 `evidence_id` 是 JSON 里的**裸 id**（`raw["id"]`，如 `occ-2-06-03-02`） |
| 证据 | `zhiyin-infrastructure/zhiyin_infrastructure/local/knowledge.py:56`（`evidence_id=str(raw.get("id") …)`）；`data/evaluation/retrieval_cases.json`（100 条期望全部带前缀）；`zhiyin-infrastructure/zhiyin_infrastructure/search.py:132`（RRF 去重键 = `hit.evidence_id`）；`RetrievalDocumentStore` 侧唯一键是 `(namespace, source_id, version)`、其 `id` 在用例中是 `namespace:id` |
| 影响①：评测跑不动 | 期望覆盖率恒为 0，Recall/MRR 无意义——这是"检索质量门槛一直没冻结"的直接原因之一。实测见[检索质量基准](职引-检索质量基准-2026-09-19.md) |
| 影响②：证据会重复 | 关键词通道与向量通道的**同一篇文档会被当成两篇**（去重键不同），RRF 融合后重复证据挤占 `top_k` |
| 为什么要斟酌 | 它不是"改个字符串"：`evidence_id` 会进证据包、被模型当引用键（引用核对的 `allowed` 集合就是用这些值）、也会进 RRF 去重与 `theory_refs` 校验。改口径会同时影响**引用核对**与**跨通道融合**两处行为，属需评审的检索契约调整 |
| 改法 A | 统一为 `namespace:id`：`LocalSearchGateway.evidence_id = f"{namespace}:{raw_id}"`，与权威存储、评测集、向量通道对齐。代价：需同步复核引用核对与 RRF 去重，并更新受影响的用例 |
| 改法 B | 反过来把评测集与权威存储改成裸 id。代价：裸 id 在六个 namespace 间**不保证唯一**（`mj-0809` 这类学职代码可能跨域重名），会削弱"稳定 ID"的前提，不建议 |
| 建议 | **A**（唯一键必须含 namespace）。但要与引用核对、RRF 去重、评测集三处一起改并跑回归，故登记待评审 |
| 决策人 | 数据能力负责人 + 架构负责人 |

---

### D12 · 线上容器的关键词检索通道**正在提供标注为 DEMO 的内容**，而且它被门禁算作已接线 —— **已关闭（2026-09-19，用户选 A）**

> **已关闭**：用户选 **A**（先如实上报，再给证据打标、引用核对拒绝演示证据），已落地为三层：
>
> | 层 | 做法 | 为什么必须在这一层做 |
> | --- | --- | --- |
> | 装配报告 | 新增 `AssemblyReport.demo_content`，并把"演示内容"并入 `serves_fabricated_content` → `/healthz` 降级 | 门禁只问"能力位装没装上"，所以要让"内容是不是真的"变成**可观测**的第二个维度 |
> | 逐条证据 | 命中带 `metadata["demo"]`（按语料 `_note` 判定，**按 namespace** 而非整目录） | 真实内容会一个域一个域补进来，一刀切会把后来的真实内容也标成演示 |
> | 引用核对 | `_report_sources` / `_report_source_versions` / `_theory_refs_from_hits` 排除演示证据 | 原来只校验"引用在不在证据包里"，不校验证据本身是不是演示数据——报告会"有出处"，出处却是本地演示 JSON |
> | 用户可见 | 应答追加 `copies.json::notice.demo_evidence` | 只拦不说是更糟的骗法：报告突然没有出处，用户不知道原因 |
>
> **刻意没有做**（与 D1 的差别）：**不参与启动硬校验**。占位实现假在**代码**（合成结果），内容问题假在**数据**——内容缺失不等于服务不可用，故不阻断启动。判定依据是**语料自己的声明**（`_note` 含 DEMO/演示），不靠路径或 `env` 猜。
>
> **实现时发现的坑**：容器里装配的 `search` 是 `RrfHybridSearchGateway`（包装类），如果只让被包装的 `LocalSearchGateway` 有这个属性，装配报告会**漏报**——所以包装类必须透传该信号。
>
> **顺带修正一处自相矛盾的用例**：e2e 验收项 8 原本拿演示语料验证"报告有引用"，与 D12 直接冲突（一边禁演示引用，一边要求出现引用），已改为使用**真实语料 fixture**。


| 项 | 内容 |
| --- | --- |
| 发现方式 | 做 D7 ⑤ 故障注入时先跑"通道健康度体检"，结果发现容器内**真实的关键词通道不是 PAMI RAG，而是 `LocalSearchGateway`**，且它对"职业兴趣"返回 3 条命中 |
| 现象 | `data/knowledge/*.json` 自述为演示数据（`theory.json` 的 `_note`："示例（DEMO，仅用于本地链路验证，不是真实采集结果）"；`sources.json` 里 3 条来源名带"（演示数据）"）。这些卡片会作为 `RetrievalEvidence` 进入证据包，进而**被报告当引用**（`_report_sources()` 只校验"引用是否出现在证据包里"，不校验证据本身是不是演示数据） |
| 存量实测（容器内普查） | 向量库 `zhiyin_vectors`：**0 行**；权威文档表 `retrieval_document`：**0 行**；本地知识库：`occupation` 3 条 + `theory` 3 条（**全部标注 DEMO**），其余 4 个 namespace 是空文件。也就是说**全系统的检索内容 = 6 张演示卡** |
| 为什么门禁没拦住 | 相位门禁问的是"`search` 能力位有没有装配"，`LocalSearchGateway.IMPLEMENTATION_STATUS = "wired"`，于是 phase 3 照常 `passed=true`。**"实现装上了"与"内容是真的"是两条不同的轴**，门禁只覆盖了前一条 |
| 与 D1 的关系 | D1 把"占位产出"用 `placeholders` + 显式 opt-in 管住了（`llm`）；检索侧的演示内容**没有对应的机制**，所以 `/healthz` 显示 `status=ok, placeholders=[]`——它并不在说谎，因为系统确实不知道这批内容是演示的 |
| 影响面 | ① 报告可能引用演示卡片（**这就是"还有没有 mock"的正面答案**：业务演示表已清空，但检索内容仍是演示卡）；② 平台 RAG 通之前，这条通道是唯一内容来源，关掉它检索就彻底为空；③ 与 D7⑦ 基线的 `Recall@K=0` 同源 |
| **⚠️ 上线后修正（2026-09-19，见[修改日志 §22.4](../00-索引与变更/职引-数据全链路修改日志.md)）** | 上一条"报告可能引用演示卡片"是**通道层**事实，需限定：实测真实装配的 `search` 对任何问题返回 **0 条**，因为命中在 `hydrate()` 权威回填时被丢掉（权威表 0 行）。也就是说线上今天**不是**在拿演示卡当依据，而是**一条证据都拿不到**。D12 的处理仍然必要（权威表一旦被填入演示来源，它们立刻可被引用），但"检索恒空"的病根是另一条，已另立 **D13** |
| 改法 A（硬失败 + 显式 opt-in） | 与 D1 同款：把本地演示知识库标为占位能力位，非 `local` 环境未显式 opt-in（如 `ZHIYIN_ALLOW_DEMO_KNOWLEDGE=1`）时**装配期硬失败**。代价：当前容器将无法启动（或需在 `deploy/.env` 显式打开开关），phase 3 门禁会变成"未满足" |
| 改法 B（可识别降级：给证据打演示标记） | 命中时在 metadata/证据上标 `demo=true`（来源 `source_id` 已含 `-demo`），并让引用核对**拒绝**用演示证据支撑结论、或至少在报告里显式标注"演示内容"。代价：要动引用核对与报告呈现，属检索契约 + 呈现口径变更 |
| 改法 C（只记录，不动） | 在文档与基线报告里写明"当前内容=6 张演示卡"，等平台 RAG 通了由真实内容替换。代价：在此之前任何报告仍可能引用演示卡片，且没人被系统提醒 |
| 建议 | **A + B 组合，但分两步**：先做 A 的"如实上报"部分（让 `/healthz` 与装配报告把"检索内容来自演示库"显式标出来，不改变能否启动），再做 B（证据里打标、引用核对拒绝演示证据）。**不要只做 C**——它与"不允许留 mock"矛盾；**也不要一上来就硬失败**——那会让容器启动不了、把"内容缺失"变成"服务不可用"，需要你先确认可接受性 |
| 为什么必须你拍板 | 三条改法都改变运行行为（能否启动 / 报告可信度 / 门禁结论），且 A 会让当前"phase 1/2/3 全绿"的运行态变成不满足 |
| 决策人 | 产品负责人 + 数据能力负责人 |

---

### D13 · 权威回填**静默丢弃全部命中**："没有内容"与"内容都不权威"不可分辨 —— **已按建议落地改法 A（2026-09-19，用户"按你的建议来"）**

> **已落地（零 schema 变更）**：
>
> - `RetrievalDocumentStore.hydrate()` 返回值由裸列表改为 `HydrationOutcome(hits, checked, dropped)`，把**丢弃量**交出去（没有用"store 上记一个 last_dropped"——同一个 store 被并发请求共享，那样会串味）；
> - `RrfHybridSearchGateway` 把丢弃量写进**审计 `degraded=True`**（主通道：一条都没幸存时 metadata 无处可写）与幸存命中的 `metadata["retrieval"]["dropped_non_authoritative"]`（补充）；
> - 三条用例锁住：全丢光 → 审计必须 `degraded=True`；部分丢 → 幸存命中带计数；没丢 → **不得**标降级。
> - 实测前后对照：改动前 46 行审计**全部** `degraded=0`（其中多行 `命中=0`，与"没命中"同形）；改动后同一次真实检索写入 `degraded=1, 命中=0`。
>
> **残留（需你确认，故未做）**：要在一个字段里**分别**记"通道故障"与"权威门挡"，需要给 `retrieval_log` 加一列——属**数据库语义变更**，且本项目 `DatabaseContext.ensure_ready()` 只 `create_all`（不补列），未跑迁移的容器写新列会**让检索本身失败**。现只做零 schema 变更的最小改动：`degraded=True` 已能回答"这次空结果是不是被门挡掉的"。

| 项 | 内容 |
| --- | --- |
| 发现方式 | D12 上线后做真实验证：真实装配的 `search` 对任何问题都返回 **0 条**，而同一个问题在"摘掉 authority"后返回 2 条。用同进程内挂/摘对照实验隔离，非推测 |
| 现象 | `RrfHybridSearchGateway` 在返回前调用 `RetrievalDocumentStore.hydrate()` 做权威回填；权威表 `retrieval_document` 为空时，`_hydrate_sync` 把**每一条**命中都判为"非权威"并丢弃 |
| 实测（容器内） | ① 真实装配：0 条；② 摘掉 authority：2 条 `['theory:theory-holland-riasec','theory:theory-clover']`；③ 关键词通道单独：同样 2 条且 `demo=True`；④ `retrieval_document`：**0 行** |
| 影响①：线上检索恒空 | 这解释了真实报告里 `sources` 为空——不是"没有内容"这么简单，而是"有内容但一条都不权威"。D7⑦ 基线的 `Recall@K=0` 也叠加了这一层 |
| 影响②：失败被静默 | 返回的就是一个**空列表**：`degraded=False`、审计里看不出、`/healthz` 也正常。调用方无法区分"确实没有命中"与"命中全被权威门挡掉"。这与《AGENTS.md》§5/§10"降级必须可识别，不得将失败静默伪装为成功"直接冲突 |
| 改法 A（已采用） | **只加可观测性，不改丢弃策略**：丢弃量进结果 metadata 与审计 `degraded`。**未动**"什么算证据"的口径 |
| 改法 B（未做） | 改回填策略：权威命中优先，非权威命中作为**弱证据**保留（带 `authoritative=false`），由引用核对决定是否可引用。代价：改变检索口径，需业务/架构确认并重跑基准。**建议等内容接入后按实测再定** |
| 改法 C（未做） | 只记录不改——与"降级必须可识别"冲突，未采用 |
| 决策人 | 数据能力负责人 + 架构负责人（改法 B 与"审计加列"仍需拍板） |

---

## 四、已按现有口径实施、但口径本身仍需确认（记录用）

以下不是"没做"，而是**已经按某条既有口径做了，但该口径没有明文覆盖这种情形**。记录下来供口径定稿人复核：

| 项 | 已实施的做法 | 依据 | 需确认什么 |
| --- | --- | --- | --- |
| 交接口径 | ①只有 `ready_to_handoff=True` 才自动交接给②；⑤按 `next_handoff_stage` 再入环；**②③④一律等用户表达意图**，不自动推进 | `FR-COLLECT-007`（采集结束条件是达到最低置信度）+ 决策 4=A（意图→环节） | 产品是否希望"②诊断完成后自动进③"。当前按"方向必须由用户显式选定"处理；若希望自动推进，需要新增口径 |
| 换环节告知文案 | 由编排器组装：主理展示名取 `agents.json`、环节中文名取 `copies.json::stage.<code>.label` | `FR-ORCH-003`（换主理必须显式告知）+ §8（文案不得硬编码） | 告知模板是否要做成可配置模板（目前是句子拼接，不是模板） |
| 报告章节 | 只下发**有数据**的章节，目录与正文保持一致；不生成空章节 | `ReportFullTextView` 的语义（只读资产视图） | 是否需要在无数据时也保留章节并显示空态（会影响"报告看起来有多完整"） |

---

## 五、架构复核：逐条最优解建议（2026-09-19，**待拍板**）

本节是**实现者的复核建议**，不是决议：§三 各条状态仍为"待决"，请按下表的"决策人"拍板后再实施。
复核方式是回到代码与设计文档重新取证；**其中 5 条的最优解与 §三 原记建议不同**，差异都标了出来。

### D1 · 建议：显式 opt-in + 如实上报占位 + 成功路径也透出模型级降级（**不改标 skeleton**）

| 项 | 内容 |
| --- | --- |
| 原记前提需修正 ① | "`degraded` 目前没有任何消费方"**不准确**：`orchestrator.py:491` 在 `"degraded" in loop_result.output` 时不落库、不发资产事件、只回兜底引导 |
| 原记前提需修正 ② | 但那只覆盖**失败路径**（`loop.py::_degraded_result` 是唯一写入 `output["degraded"]` 的地方）。`LocalOrMockLLM` 每次都返回 `LLMResult.degraded=True`，而 `loop.py:378/384` 只在 `not result.valid` 时把 `result.degraded` 传下去——**成功路径把模型级降级丢掉了**，占位产出会被当真实资产落库 |
| 原记前提需修正 ③ | 装配报告报 `wired` 不是"缺一个常量"，而是 `report.py:86` 的兜底规则 `skeleton_markers=(".pami.",)`：本地适配器不含 `.pami.` 就落到 `else WIRED`。是**判定规则**的问题 |
| 不采纳 A 的理由 | 把 `LocalOrMockLLM` 标成 `skeleton` **是另一种不如实**：它是 LLMGateway 的完整实现（方法体齐全、产出过契约），只是后端不是真模型。骨架 = 方法体未填，两者不是一回事 |
| **最优解** | (a) **装配期显式 opt-in**：既未开 `ZHIYIN_USE_PAMI_LLM`、又未显式声明"允许占位"时**拒绝启动**——这是唯一不依赖人去看的拦截点；(b) **如实上报**：装配报告增加"占位实现"信号（复用既有 `healthy`/`degraded` 通道，让 `/healthz` 与 `--check` 看得见），仍不改 `skeleton` 语义；(c) **成功路径也透出模型级降级**：`LoopResult` 带上 `model_degraded`，编排器据此在应答里注明本次为演示产出 |
| 成本 | `settings` 新开关 + `gateways` 装配分支 + `report` 状态口径 + `loop`/`orchestrator` 降级传递；`tests/test_container.py:82` 的断言按新口径调整。**不改公开 DTO**（(c) 止于编排器层） |
| 决策人 | 架构负责人（你） |

### D2 · 建议：给报告加**画像快照**，形状不必新造（**原记的 B 不成立**）

| 项 | 内容 |
| --- | --- |
| 原记 B 为何不成立 | 《前端页面设计》§4.4 第 243 行明确要求报告全文"含 15 维全景、方案、行动计划、**个人画像**"，且第 245 行写"报告全文页**读取资产版本**"；该文档自述是页面结构**唯一口径**。把 DTO 描述改成不含画像会与唯一口径冲突 |
| 但 A 也不能"实时读当前画像" | 同上第 245 行：页面读的是**资产版本**。实时读当前画像会出现"报告 v1 里显示的是今天的画像" |
| **最优解** | `Report` 增加 `profile_snapshot: list[ProfileField]`（`ProfileField` 已是内核形状，**无需新造**），在②诊断时把当次所用画像冻结进该报告版本；`mappers.report_full_text_view` 在快照非空时追加 `profile` 章节，前端渲染为"本版本依据"。旧报告该字段为空 → **不生成章节**，向后兼容、无需数据迁移 |
| 成本 | 内核 `Report` 加字段（`extra="forbid"` 下属契约变更）+ 诊断环节填充 + mapper + 前端章节；`ReportFullTextView.sections` 是 `list[dict[str, Any]]`，**公开契约与生成物理论上不变** |
| 决策人 | 产品（要不要画像）+ 架构（快照形态） |

### D3 · 建议：**删除 `advance`**（原记的 B 太软）

| 项 | 内容 |
| --- | --- |
| 复核发现 | `advance` 的落库动作与 `orchestrator._perform_handoff` **完全重复**（两处都直接调 `sessions.update_stage`）；而 `_perform_handoff` 还要发 `loop_stage_changed`、写会话记忆、组告知文案——这些是"阶段变更"不可分割的部分 |
| 调用面 | 全仓 `advance` 仅 `tests/test_business_loop.py:194,205` 两处；**无内部调用方** |
| 未来调用方该走哪 | 若⑤/主动干预 Worker 要推进环节，正确入口是**冻结的 `Orchestrator.handoff`**——它已经是公开且语义完整的阶段变更 API（含事件、记忆、告知） |
| **最优解** | 删除 `LoopCoordinator.advance`（Port + 实现），2 处测试改走 `handoff`。理由写进 Port 文档：**阶段变更的语义完整性属于 Orchestrator，不属于状态机**；`advance` 这种"落库但不发事件"的半职责方法，正是本项目反复被"看起来有、实际没有"咬到的形态 |
| 成本 | 冻结 Port 删除一个方法；风险低、可即时回滚 |
| 决策人 | 架构负责人（你） |

### D4 · 建议：B（改措辞为等权），且 note 与决策行一起改

复核：`policies/profile.py::calculate_overall_confidence` 的 docstring 已写"**等权**平均"、实现是已采集关键字段置信度的算术平均；错的是另两处——决策记录第 5 行与 `policy_params.json::profile_collection.note` 都写"**加权**平均"。
**最优解**：两处改为"等权平均（已采集关键字段置信度的算术平均）"，并补一句"若将来要加权，须先给出每字段权重依据"。改 JSON 的 note 后需跑 registry sync。
成本：纯文案 + 一次 sync。决策人：业务口径定稿人（你）。

### D5 · 建议：只往现有 `verify_direction` 加词（**比原记的 B 更小，不需要新意图**）

| 项 | 内容 |
| --- | --- |
| 复核发现 | `DefaultStagePolicy._STAGES` 里 **`VERIFY_DIRECTION → DIAGNOSE` 已经存在**；`policy_params.routing.intent_keywords.verify_direction` 已有（验证 / 适不适合 / 方向行不行 / 行不行） |
| 所以缺的只是"表达" | 用户说"开始诊断""档案够了"命中不了任何关键词，于是留在① |
| **最优解** | 给 `intent_keywords.verify_direction` 追加：`开始诊断` / `进入诊断` / `做诊断` / `档案够了` / `进入下一步`。**纯数据 + 一次 sync，零代码**，不需要新 `IntentType`、也不需要 `POST /app/task/advance` |
| 注意 | `KeywordIntentPolicy` 是**包含匹配**且按 JSON 顺序取首个命中；新词不得与 `confused`（迷茫 / 不清楚 / 适合什么 / 没方向）等组冲突 |
| 决策人 | 产品（词表由你定） |

### D6 · 建议：分两步升级，先 `openapi-typescript` 再 `vite`（原记 A 的第一步已做完）

审计结论（`npm audit --json` 实测）：4 个告警包 **全部是 dev-only**，浏览器产物不含它们（`dist/` 无 undici/esbuild 痕迹，`package-lock` 标 `dev: true`）；`fixAvailable.isSemVerMajor` **全为 true**，**没有任何非破坏性修复**；vite 5.4.21、undici 5.29.0、openapi-typescript 6.7.6 均为各自 major 的最后一版，无 backport。

| 包 | 版本 | 级别 | 来源 | 清除方式 |
| --- | --- | --- | --- | --- |
| vite | 5.4.21 | 高危 | 直接 devDep（`server.fs.deny` 绕过 / `.map` 遍历 / launch-editor UNC-NTLM） | vite → ^7.3.6 或 ^8.3.0 |
| esbuild | 0.21.5 | 中危 | 经 vite 传递 | 随 vite 升级 |
| undici | 5.29.0 | 高危 | 经 openapi-typescript 传递（12 条公告） | openapi-typescript → ^7.13.0 |
| openapi-typescript | 6.7.6 | 中危 | 直接 devDep | → ^7.13.0 |

**最优解**：① 先 `openapi-typescript ^6.7.0 → ^7.13.0`（一次清掉 undici 的 1 高 + 1 中；只影响 `npm run gen:api`，跑 `check:api` 验证）；② 再 `vite ^5.2.0 → ^7.3.6` + `@vitejs/plugin-vue ^5.0.0 → ^6.0.9`（plugin-vue 6.0.9 的 peer 覆盖 vite 5–8）。**不要**跑 `npm audit fix`（无效）或 `--force`（会把 plugin-vue 5.x 的 peer 打坏）。两次都单独提交。
剩余风险：仅在 dev server 暴露给不可信网络时有实际影响（含 Windows 的 UNC/NTLM 向量），本地开发可接受。决策人：前端负责人（你）。

### D7 · 建议：保持本地检索，把"内容"当真正的待办，并先定评测门槛

复核：`data/knowledge/` 实际内容为 `occupation.json` **3 条**、`theory.json` **3 条**，`jd` / `report` / `memory` / `resume` **均为 0 条**；`sources.json` 4 条来源台账（含 `allowed` / `access_basis` / `retention` / `removal_method`，说明"招聘来源条款"这层已经建好）；本地 `LocalSearchGateway` 是真实实现（`IMPLEMENTATION_STATUS = "wired"`）。
**本轮带来的关键变化**：15 维改成证据等级后，**JOB-MARKET 那 5 维实测普遍为 `missing`**——D7 从"看不见"变成"用户能看见"，且这正是 `FR-DIAG-006`（证据不足不硬给结论）要的行为。
**最优解**：维持 B（暂不切 RAG）。真正的活是**内容补齐 + 来源合规**；切换前必须先定"固定评测集 + 门槛"（第三期设计文档把它列为验收项，但阈值尚未拍板）——没有内容时接通 RAG 只会返回空证据。
成本：不在代码侧；需平台与内容排期。决策人：平台 + 数据能力负责人。

### D8 · 建议：A + 一处**架构修正**（原记未提到）

| 项 | 内容 |
| --- | --- |
| 复核新增① | `workspace.py:155` 的 `available_blocks` 是**Python 里写死的字面量列表**，把 `export` / `mentor`（`enabled: false`）与 `demo` 一并列为"available"。这既违反 §8（功能开关不得硬编码），语义也错：**available ≠ enabled** |
| 复核新增② | `demo` 在**身份层**另有独立来源：`local/auth.py:31` 给本地 stub 主体打 `raw_claims["demo"]="true"`，`identity.py:43` 据此给 `DEMO-000` 手机号。那是本地身份桩，**与 `feature_flags.demo` 无关**，删旗标不影响它 |
| **最优解** | (a) 删 `feature_flags.demo`、`banners.demo_entry`、`trust_blocks` 的 `expandable_ref: demo_report`（三处均无消费方）；(b) `available_blocks` 改为**由 `RegistryService.feature_flags()` 过滤 enabled 派生**，不再硬编码；(c) `faqs.data_safety` 的合规措辞给出建议稿由你定稿 |
| (c) 建议稿 | 现文"本期只使用演示与脱敏数据，不接收真实手机号、简历等敏感信息…"已与现状不符（真实存储已落 MySQL/pgvector）。建议改为陈述：画像与行为数据**只用于生成你自己的报告、方案与计划**；**不接收真实简历、身份证件等敏感材料**（如需上传简历属后续单独立项）；数据存储位置与保留策略按隐私政策执行 |
| 成本 | registry 改动 + 一次 sync；`available_blocks` 派生是业务服务小改；**不动公开 DTO**。B（接线消费）/ C（停止下发）另议 |
| 决策人 | 产品（内容口径）+ 前端负责人（是否接线） |

### D9 · 建议：**直接做 A**——原记认为的两个形状难点，有一个不存在

| 项 | 内容 |
| --- | --- |
| 难点①其实不存在 | `stages`（负责哪一环节）**不需要**给 `RegistryRepository` 新增 `list_output_contracts`：现有冻结方法 `get_output_contract(agent_id, stage)` 逐环节探测（5 环节 × 5 智能体 = 25 次）即可推出——`profile_analyst→collect`、`career_advisor→diagnose+decide`、`path_planner→act`、`companion_coach→review`、`info_scout→无` |
| 难点②有现成解 | 理论包中文名：`data/registry/theory_cards.json` 有 **16 张卡**（含 `name`，如"帕森斯 · 了解自我"），`RegistryRepository` **已有** `list_theory_cards`，只差把它暴露到 `RegistryService` |
| 难点③已铺好 | 门面 `facade/application.py::bootstrap` **本来就在**用 `get_agent` 组装 agents 字典（只用于解析 `lead_agent_name`）。"下发能力池"是加一个**列表字段**，不是新建数据通道 |
| **最优解** | A：`RegistryService` 增 `list_agents()` 与 `list_theory_cards()`；`BootstrapView` 增 `agents: list[AgentView]`，字段 `id / name / role_summary / stages / theories[{id,name}] / tools / not_to_do`（`no` / `shortName` / `theme` 由前端派生，不进契约）；后端解析 stages 与理论中文名；前端 `agentCatalog` 改读 bootstrap；重新导出 OpenAPI 与前端类型 + registry sync。若做 A，则 B（守卫比对）**不再需要** |
| 成本 | 两处冻结契约（业务 Port + 公开 DTO）+ 一个生成物；**比原记预估的低** |
| 决策人 | 你（既有架构也含前端） |

### 五.1 建议执行顺序

| 顺序 | 条目 | 为什么这个顺序 | 需要你先给什么 |
| --- | --- | --- | --- |
| 1 | **D4 + D5** | 零代码、纯数据，一轮交付；D5 让"我想开始诊断"立刻可用 | D5 的词表确认、D4 措辞确认 |
| 2 | **D8 (a)(b)** | 与"不允许留 Mock"直接一致，且修掉一处 §8 违规 | (c) 合规措辞定稿 |
| 3 | **D1** | 唯一的 P0：漏配环境会把占位产出当业务结果，现有门禁全不报警 | 批准三段做法（尤其"拒绝启动"） |
| 4 | **D3** | 删一个无调用方的半职责方法，小且立刻消除误判源 | 批准删除冻结 Port 方法 |
| 5 | **D2** | 中等；依赖"报告是否含画像"的产品确认（设计文档已要求含） | 确认快照形态 |
| 6 | **D9** | 中等偏大；消除前端硬编码，是 §8 的硬要求 | 批准两处契约变更 |
| 7 | **D6** | 与业务无关，独立提交便于回滚 | 批准升级范围 |
| 8 | **D7** | 非代码，内容与平台排期 | 内容/评测门槛的排期 |

---

## 六、维护约定

1. 新出现"改法需拍板"的问题，追加到 §三 并标注决策人；不要在没拍板前由实现者自选一条做掉。
2. 拍板后：把该条状态改为 `已决待实施`（写明选了哪条、谁定的、日期），实施完成改为 `已关闭`。
3. 重要决策同步[修改日志](../00-索引与变更/职引-数据全链路修改日志.md)；若涉及业务口径，同步[业务口径决策记录](../../评审/业务口径决策记录-v1.0.md)。
4. 本文不记录密钥、凭据或任何敏感配置。
