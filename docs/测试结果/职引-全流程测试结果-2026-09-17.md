# 职引 · Service / Frontend / Database 全流程测试报告

## 〇、报告摘要

| 项目 | 内容 |
| --- | --- |
| 测试日期 | 2026-09-17 |
| 当前工作分支 | `data-yuan` |
| 上游基线 | `origin/develop@4ba79ed`（service PR #6、frontend PR #7 均已合并） |
| 实际受测对象 | `develop@4ba79ed` + 当前已暂存的数据层、依赖、部署和 Docker 变更 |
| 测试范围 | Python 后端、五环节业务主干、Vue 前端、OpenAPI、MySQL、Redis、MinIO、PostgreSQL + pgvector、PAMI 统一 Compose、Docker HTTP 流程 |
| 明确不纳入完成范围 | 真实 Embedding、VectorSync 运行装配、真实向量检索闭环、PAMI Agent/RAG 真实凭据调用 |
| 总体结论 | M1、M2 通过；前端构建通过；Docker 统一部署及真实存储通过；五环节 HTTP 流程通过；M3 因团队明确暂缓向量检索而保持未通过 |
| 提交状态 | 本地已暂存，尚未提交、推送或创建 PR |

本轮可以准确表述为：

> **service、frontend、database 在明确标记 Mock 节点的前提下，已完成本地和 Docker 端到端联调；真实模型与向量检索闭环尚未完成。**

不能表述为“二期全部真实能力完成”，原因是 Docker `/healthz` 仍如实报告
`gateways.embedding=skeleton` 和 `workers.vector_sync=not_wired`。

---

## 一、版本基线与 frontend 合并影响核验

### 1.1 提交关系

| 提交 | 含义 |
| --- | --- |
| `93bd759` | service PR #6 合入 develop |
| `00130aa` | frontend 分支拉取最新 develop 并完成冲突处理后的 tip |
| `4ba79ed` | frontend PR #7 合入 develop |

frontend 合并前后的两个提交具有完全相同的 Git tree：

```text
00130aa tree = 6adf33208c82b6df7403d864a1176e7748d75e70
4ba79ed tree = 6adf33208c82b6df7403d864a1176e7748d75e70
```

并且：

```text
git diff --name-status 00130aa..4ba79ed
# 无输出
```

结论：PR #7 只新增合并提交，没有在合并阶段再次修改代码。因此，先前基于
`00130aa` 完成的 Docker 镜像和测试在文件内容层面与 `4ba79ed` 一致。

### 1.2 develop 合并后的重新验证

为了避免仅凭 tree 相同就判断“不会影响”，仍在 `develop@4ba79ed` 基线上重新执行：

| 验证项 | develop 合并后结果 |
| --- | --- |
| `python -m pytest tests/e2e -v -rxXs` | `10 passed, 1 xfailed` |
| `python -m zhiyin_boot --check --phase=2` | `passed=true, unmet=[]` |
| `python scripts/export_openapi.py --check` | 快照一致 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过，152 个模块完成转换 |

所以 frontend 合并没有引入端到端回归。

### 1.3 受测版本的可复现性说明

当前数据层修改尚未提交，因此“受测版本”不能只用一个 Git 提交号表示。准确基线是：

```text
origin/develop@4ba79ed
+ 当前 data-yuan 暂存区中的数据层、依赖、部署、Dockerfile 和测试变更
```

后续正式提交后，应在本报告中补充最终提交号，并以该提交重新跑一次最小合并门禁，避免暂存区继续变化造成证据漂移。

---

## 二、测试环境

### 2.1 本机工具链

| 工具 | 实测版本 |
| --- | --- |
| 操作系统 | Windows |
| Python | 3.11.9 |
| pytest | 9.0.3 |
| Node.js | 24.14.1 |
| npm | 11.13.0 |
| Git | 2.49.0.windows.1 |
| Docker Client / Server | 29.8.0 / 29.8.0 |
| Docker Compose | v5.5.1 |

### 2.2 Docker 关键镜像

| 能力 | 镜像/版本 |
| --- | --- |
| MySQL | `mysql:8.0.37` |
| Redis | `redis:7.0.15` |
| MinIO | `minio/minio:RELEASE.2024-08-26T15-33-07Z` |
| Kafka | `bitnami/kafka:3.9` |
| Elasticsearch | `8.12.2` |
| PostgreSQL | 16 |
| pgvector | 0.8.6 |
| 职引后端 | 本地 `zhiyin/backend:dev`，由当前工作区重新构建 |
| 职引契约测试 | 本地 `zhiyin/contracts:dev` |

### 2.3 M3 Python 依赖组

Dockerfile 使用 `pip install ".[m3]"` 安装运行依赖，主要包括：

- SQLAlchemy async、asyncmy、PyMySQL；
- Alembic；
- asyncpg、pgvector；
- aiokafka；
- Elasticsearch async client；
- MinIO client。

契约测试镜像在运行镜像之上追加 `.[dev]`，用于 pytest、pytest-asyncio、aiosqlite 和 Ruff。

---

## 三、范围与验收口径

### 3.1 本轮纳入范围

1. service 合并后的 Orchestrator、资产正文保存和知识检索接线。
2. frontend 合并后的首页、对话页、会话状态和 Mock/Demo 展示组件。
3. database 的 MySQL Repository、事务、RawQuery、迁移与真实契约。
4. Redis、MinIO、pgvector 的真实运行适配。
5. 五环节 collect / diagnose / decide / act / review。
6. 会话列表、工作台、报告全文读回。
7. Dockerfile、Compose、依赖安装、数据库初始化和迁移。
8. PAMI + 职引统一网络下的运行健康检查。

### 3.2 本轮不纳入“通过”的能力

| 能力 | 当前状态 | 不计入通过的原因 |
| --- | --- | --- |
| 真实 LLM 产出 | Mock | 五环节仍由 `LocalOrMockLLM` 按 Schema 合成结果 |
| 真实 Embedding | Skeleton | PAMI 模型 ID、组织 ID/API Key 尚未配置 |
| VectorSync 运行装配 | Not Wired | 真实 Embedding 未就绪，不能写入伪向量冒充完成 |
| 真实向量检索业务闭环 | 暂缓 | 团队明确要求先不做向量检索 |
| PAMI Agent/RAG 真实调用 | 待凭据 | 当前只完成适配器契约测试，未完成真实应用调用 |

详细节点、流向和退出条件见
[职引-Mock节点标记清单-2026-09-17.md](职引-Mock节点标记清单-2026-09-17.md)。

---

## 四、测试结果总表

| 层级 | 命令或场景 | 实测结果 | 判定 |
| --- | --- | --- | --- |
| 后端全量 | `python -m pytest -q -rsx` | `394 passed, 1 skipped, 1 xfailed, 1 warning` | 通过，非通过项均有明确原因 |
| E2E | `python -m pytest tests/e2e -v -rxXs` | `10 passed, 1 xfailed` | 主路径通过，Mock 来源字段未闭合 |
| 静态检查 | `python -m ruff check .` | `All checks passed` | 通过 |
| M1 门禁 | `python -m zhiyin_boot --check --phase=1` | `passed=true, unmet=[]` | 通过 |
| M2 门禁 | `python -m zhiyin_boot --check --phase=2` | `passed=true, unmet=[]` | 通过 |
| OpenAPI | `python scripts/export_openapi.py --check` | 快照一致 | 通过 |
| 前端类型 | `npm run typecheck` | 无错误 | 通过 |
| 前端 API | `npm run check:api` | 生成后无语义差异 | 通过 |
| 前端生产构建 | `npm run build` | 152 modules transformed | 通过 |
| Compose 配置 | 两份 Compose 合并后 `config --quiet` | 无错误 | 通过 |
| Docker 镜像 | `zhiyin-src/template/deploy/up.ps1` | 后端、pgvector 镜像构建成功 | 通过 |
| 部署基线 | `zhiyin-src/template/deploy/verify.ps1` | `unified deployment baseline is ready` | 通过 |
| 真实存储 | `zhiyin-runtime-verify` | MySQL、Redis、pgvector、MinIO 全部通过 | 通过 |
| MySQL 契约 | `zhiyin-mysql-contract-verify` | `21 passed` | 通过 |
| Docker HTTP | 五环节 + sessions + workspace + report | 全部成功 | 通过 |
| M3 门禁 | 容器内 `--check --phase=3` | 两项 unmet | 未通过，符合暂缓约定 |

---

## 五、后端全量与端到端测试明细

### 5.1 全量测试

```text
394 passed, 1 skipped, 1 xfailed, 1 warning
```

全量测试覆盖架构守卫、动态资源闭合、Port 契约、本地/SQLite/真实实现共用语义、业务 Service、Worker、API、PAMI 边界、M3 数据基础设施和 E2E。

### 5.2 E2E 用例明细

| 用例 | 验证内容 | 结果 |
| --- | --- | --- |
| `test_profile_event_propagates_content_and_workspace_reads_it` | 画像事件传播、资产内容更新、工作台读回 | PASS |
| `test_duplicate_profile_event_and_user_data_are_isolated` | 重复事件幂等、用户数据隔离 | PASS |
| `test_acceptance_1_home_task_routes_to_target_stage` | 首页任务入口路由到目标环节 | PASS |
| `test_acceptance_2_resume_from_any_stage_keeps_prior_assets` | 从任意环节进入并继承前序资产 | PASS |
| `test_acceptance_3_handoff_changes_lead_and_discloses` | 换主理与 Disclosure 同步 | PASS |
| `test_acceptance_4_blackboard_is_shared_across_sessions` | 多会话共享同一黑板 | PASS |
| `test_acceptance_5_profile_update_propagates_only_affected_assets` | 只重算受影响资产 | PASS |
| `test_acceptance_6_action_loop_writes_behavior_log` | 行动闭环写行为日志 | PASS |
| `test_acceptance_7_stall_triggers_coach_message` | 停滞触发教练消息 | PASS |
| `test_acceptance_8_orchestrator_persists_full_assets_and_knowledge` | 资产正文落库、知识引用可回读 | PASS |
| `test_mock_content_carries_machine_readable_source` | 后端产出提供机器可判来源 | XFAIL |

唯一 xfail 是有意保留的未闭合验收项：当前 DTO 没有 `source`、`is_mock` 或
`content_source` 字段。它不会阻塞 M2 主流程，但会阻止前端可靠展示五环节 Mock 徽标，
因此不能删除 xfail 或将其写成通过。

### 5.3 OPEN-1 / OPEN-2 复验

service 合并后，以下第一期遗留已闭合：

- OPEN-1：Orchestrator 已调用正式资产保存能力，报告、方向方案、行动计划正文与版本同步持久化；
- OPEN-2：诊断和决策已调用 KnowledgeGateway，理论引用来自知识数据而非悬空占位；
- 原 xfail 已迁移为正式 E2E，并在当前 develop 基线通过。

---

## 六、前端验证明细

### 6.1 类型与 API 契约

- `npm run typecheck`：通过；
- `npm run check:api`：根据 `contracts/openapi.json` 重新生成类型后无语义差异；
- 没有手工编辑 `contracts/openapi.json` 或 `src/api/types.ts`。

### 6.2 生产构建

```text
vite v5.4.21 building for production...
✓ 152 modules transformed
✓ built in 1.04s
```

主要页面与异步块均成功生成，包括 HomePage、ConversationPage、WorkspacePage、ReportPage 和 AuthPage。

### 6.3 前端 Mock 展示边界

`MockBadge.vue` 已支持 `mock`、`demo`、`real` 三种来源，但当前只有首页 Preview/Demo
内容能显式传入 `source="demo"`。五环节返回对象缺少后端来源字段，因此对话、工作台和报告尚不能可靠传入 `source="mock"`。这不是 frontend 合并回归，而是既有 MOCK-1 契约缺口。

---

## 七、Docker 构建与部署明细

### 7.1 构建内容

本轮已实际构建并运行：

| 文件 | 用途 | 结果 |
| --- | --- | --- |
| `zhiyin-src/template/Dockerfile` | 职引 API 与迁移运行镜像，安装 `.[m3]` | 构建成功 |
| `zhiyin-src/template/Dockerfile.contracts` | 真实 MySQL 契约测试镜像 | 构建成功 |
| `zhiyin-src/template/deploy/postgres/Dockerfile` | PostgreSQL 16 + pgvector 0.8.6 | 构建成功 |
| `zhiyin-src/template/deploy/compose.yaml` | PAMI 覆盖、职引 API、迁移、验证、备份恢复任务 | Compose 校验及启动成功 |

### 7.2 启动顺序

实际启动链路为：

```text
MySQL healthy
→ 创建 zhiyin_service 与独立账号
→ Alembic upgrade head
→ pgvector healthy
→ PAMI Nginx healthy
→ zhiyin-api 启动并通过健康探针
```

迁移与初始化任务均以退出码 0 完成，没有通过忽略错误继续启动 API。

### 7.3 容器健康状态

以下带健康探针的常驻服务均为 `healthy`：

- PAMI：AgentScope、App Service、Assistant Service、BFF、IAM、Knowledge、MCP、Model、RAG Service、Nginx；
- 中间件：MySQL、Redis、Kafka、Elasticsearch、MinIO；
- 职引：PostgreSQL + pgvector、`zhiyin-api`。

PAMI 的 `agent` 和 `rag` 容器为 `running`，但其 Compose 定义没有健康探针，所以报告只记录为 running，不写成 healthy。

### 7.4 部署基线验证

`zhiyin-src/template/deploy/verify.ps1` 同时验证：

1. 两份 Compose 合并后的配置合法；
2. PAMI Web/BFF 业务就绪接口可访问；
3. Docker 内部的职引 `/healthz` 可访问并返回装配报告。

最终输出：

```text
unified deployment baseline is ready
```

---

## 八、真实数据基础设施验证

### 8.1 运行验证结果

```text
mysql_repository=ok
redis_adapter=ok
pgvector_query_plan=hnsw
pgvector_gateway=ok
minio_gateway=ok
```

### 8.2 MySQL

验证内容包括：

- `zhiyin_service` 数据库与职引独立账号可用；
- Alembic migration 可执行；
- Repository 真实写入与读回；
- 事务提交与回滚；
- 受限 RawQuery 的参数化查询路径；
- 隔离测试库创建和测试后清理。

真实 MySQL 契约测试结果：

```text
21 passed in 1.59s
```

### 8.3 Redis

Docker 运行验证确认 Redis Adapter 能使用真实 PAMI Redis 实例完成读写。全量 pytest 中
`tests/test_redis_first_phase.py` 的一个独立实例用例因本机未设置 `ZHIYIN_TEST_REDIS_URL`
而 skip；Docker 运行验证已覆盖真实连接，所以该 skip 不表示 Redis 未验证。

### 8.4 PostgreSQL + pgvector

验证内容包括：

- pgvector 扩展可用；
- 向量 Gateway 写、读、删；
- namespace 与 model_version 隔离字段；
- JSONB metadata；
- 查询计划命中 HNSW，而不是只确认索引文件存在。

需要特别区分：pgvector Gateway 真实可用，不等于真实向量检索闭环完成。当前还缺真实 Embedding 输入和 VectorSync 运行装配。

### 8.5 MinIO

验证确认职引 ObjectStore 使用真实 MinIO 完成对象写入与读取；不是本地文件替身。

---

## 九、Docker HTTP 端到端冒烟

### 9.1 测试方式

测试在 `zhiyin-api` 容器内通过 HTTP 请求 `http://127.0.0.1:8000`，没有直接调用 Facade，覆盖真实 Controller、Facade、Service、Repository 与数据库链路。

调用顺序：

```text
GET  /healthz
GET  /api/v1/app/bootstrap
POST /api/v1/app/task/enter
POST /api/v1/app/conversation/message
GET  /api/v1/app/sessions
GET  /api/v1/app/workspace
GET  /api/v1/app/report/full-text
```

### 9.2 五环节结果

| 任务入口 | 目标环节 | 验证结果 |
| --- | --- | --- |
| `confused` | collect | 进入采集并返回一轮消息 |
| `verify_direction` | diagnose | 进入诊断，正文落库，报告全文可读 |
| `undecided` | decide | 进入决策并返回一轮消息 |
| `how_to_act` | act | 进入行动并返回一轮消息 |
| `stuck` | review | 进入复盘并返回一轮消息 |

附加结果：

- 五条测试会话可从 sessions 读取；
- workspace 聚合成功；
- diagnose 产生的报告全文可读取；
- HTTP 信封 `code=0`；
- API 容器保持 healthy。

这里的“HTTP 流程成功”只说明链路和持久化成功；消息正文仍可能由 Mock LLM 生成，不能据此声称真实模型通过。

---

## 十、装配状态与阶段门禁

### 10.1 Docker 当前装配

| 分组 | 状态摘要 |
| --- | --- |
| Gateways | 除 Embedding 为 skeleton 外，其余本轮所需能力均 wired；RawQuery 已 wired |
| Repositories | 7/7 wired |
| Transaction | TransactionManager wired |
| Orchestration | 6/6 wired |
| Services | 11/11 wired |
| Workers | Impact、ActiveEvent wired；VectorSync not_wired |

合计：`41 wired / 1 skeleton / 1 not_wired`。

### 10.2 门禁结果

| 门禁 | 结果 | 说明 |
| --- | --- | --- |
| M1 框架可验证 | 通过 | 架构、契约和基础装配满足要求 |
| M2 业务主干端到端 | 通过 | 五环节业务主路径与规则验收满足要求 |
| M3 真实存储 | 未通过 | Embedding 和 VectorSync 不满足禁止 skeleton/not_wired 的退出条件 |

M3 实测未满足项：

```text
gateways.embedding=skeleton
workers.vector_sync=not_wired
```

这两项未通过不是测试故障，而是团队明确暂缓向量检索后的真实装配状态。报告保留失败，避免把“代码存在”或“pgvector 可用”错误等同于“完整向量闭环完成”。

---

## 十一、非通过项、警告与风险

| 等级 | 项目 | 当前事实 | 是否阻塞本轮 | 后续处理 |
| --- | --- | --- | --- | --- |
| 未闭合 | Mock 来源字段 | 后端 DTO 无机器可判来源 | 不阻塞 M2，阻塞可靠 Mock 展示 | 契约评审后新增来源字段并重新生成 OpenAPI/前端类型 |
| 暂缓 | Embedding | skeleton | 阻塞 M3 | 获取 PAMI 模型配置后真实联调 |
| 暂缓 | VectorSync | not_wired | 阻塞 M3 | Embedding 可用后装配并验证重试补偿 |
| 暂缓 | 真实向量检索 | 未做业务闭环 | 阻塞 M3 完成声明 | 按团队通知恢复开发 |
| 环境 skip | 本地真实 Redis 用例 | 未设置 `ZHIYIN_TEST_REDIS_URL` | 否 | Docker 已完成真实 Redis 验证 |
| 依赖 warning | Starlette TestClient | httpx 兼容性弃用提示 | 否 | 后续依赖升级时处理 |
| 平台配置 | PAMI Agent/RAG/模型凭据 | 未提供实际应用配置 | 阻塞平台真实调用 | 由平台侧提供配置，秘密只写入忽略的 `zhiyin-src/template/deploy/.env` |

当前没有发现以下问题：

- frontend 合并冲突残留；
- OpenAPI 快照漂移；
- 前端 TypeScript 编译错误；
- Dockerfile 构建失败；
- Alembic 迁移失败；
- MySQL Repository 契约失败；
- 五环节路由回归；
- 工作台或报告正文无法读取。

---

## 十二、最终验收结论

### 12.1 已达到要求

- service 与 frontend 均已进入最新 develop；
- database 分支已同步最新 develop；
- 本地全量和 E2E 通过；
- 前端类型、API 契约和生产构建通过；
- Dockerfile、依赖和 Compose 已完成真实构建；
- 统一 Docker 栈启动成功；
- MySQL、Redis、pgvector、MinIO 真实运行验证通过；
- 五环节 HTTP、会话、工作台、报告全文链路通过；
- Mock 节点已单独记录，没有被伪装为真实能力。

### 12.2 尚未达到要求

- 真实 PAMI Embedding 调用；
- VectorSyncWorker 运行装配；
- 真实向量检索端到端；
- 五环节响应的机器可判 Mock 来源字段；
- Phase 3 `unmet=[]`。

### 12.3 当前发布判断

| 目标 | 是否可交付 |
| --- | --- |
| 第一/二期业务主干联调版 | 可以 |
| 带明确 Mock 标记的演示环境 | 主链可运行，但五环节内容来源字段仍需补齐后才能做到完整 UI 标记 |
| 真实存储基础设施 | 可以，已完成 Docker 运行验证 |
| 真实模型与向量检索版本 | 不可以，仍受暂缓项与凭据阻塞 |
| 宣称“二期全部完成” | 不可以，应表述为“数据基础设施完成，真实模型/向量闭环待完成” |

---

## 十三、复验命令

### 13.1 本地门禁

```powershell
cd zhiyin-src/template
python -m pytest -q -rsx
python -m pytest tests/e2e -v -rxXs
python -m ruff check .
python -m zhiyin_boot --check --phase=1
python -m zhiyin_boot --check --phase=2
python scripts/export_openapi.py --check
```

### 13.2 前端

```powershell
cd zhiyin-src/template/zhiyin-web
npm run typecheck
npm run check:api
npm run build
```

### 13.3 Docker

```powershell
cd <repository-root>
powershell -NoProfile -ExecutionPolicy Bypass -File zhiyin-src/template/deploy/up.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File zhiyin-src/template/deploy/verify.ps1
```

真实存储和契约验证通过 Compose 的 `verify` profile 执行：

```text
zhiyin-runtime-verify
zhiyin-mysql-contract-verify
```

恢复向量工作后，还必须在容器内执行：

```powershell
python -m zhiyin_boot --check --phase=3
```

只有当 Phase 3 返回 `passed=true` 且 `unmet=[]`，并补齐真实 Embedding/VectorSync/检索证据后，才能把本报告的 M3 结论更新为通过。
